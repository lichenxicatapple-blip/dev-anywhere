// Session list 页面占位，Task 2 将替换为完整实现
import { View, Text } from "@tarojs/components";

export default function SessionList() {
  return (
    <View style={{ padding: "32px", textAlign: "center" }}>
      <Text>Session list placeholder</Text>
    </View>
  );
}
