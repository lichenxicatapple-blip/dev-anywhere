import { describe, it } from "vitest";

// sendPtySnapshot and pty_snapshot have been removed in Plan 05.1
// This file is scheduled for deletion
describe("send-pty-snapshot (removed)", () => {
  it("placeholder - file scheduled for deletion", () => {});
});
