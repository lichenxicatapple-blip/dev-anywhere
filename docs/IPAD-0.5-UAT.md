# iPad 0.5 UAT

This document is the execution checklist for iPad acceptance before `0.5.0`.
It is intentionally separate from `RELEASE-0.5-READINESS.md`: that file defines
release gates; this file defines what to do on the real iPad, in what order,
and who confirms each result.

## Rules Of Engagement

- Do not mark an item passed from a DOM read or screenshot alone when the item
  depends on feel, focus, keyboard behavior, touch selection, or visual comfort.
- The agent may use WebKit inspection, screenshots, logs, and DOM geometry as
  evidence, but the user is the final judge for human-visible behavior.
- Before each manual step, the agent states the current device state, the action
  requested from the user, and the expected result.
- The user answers pass/fail or describes the observed mismatch. The agent then
  records the status before moving on.
- Do not jump to a different device class during this UAT. Android, desktop, and
  release smoke checks are separate gates.
- If new iPad scope is mentioned during UAT, update this checklist before
  continuing so the scope is not lost in chat history.

## Status Legend

- `NOT_STARTED`: not touched in this UAT run.
- `OBSERVED`: inspected by the agent only; not accepted by the user.
- `PASS`: user confirmed, or the item is purely technical and objective.
- `FAIL`: user observed a mismatch or automation found a clear defect.
- `RETEST`: a previous attempt was invalid because the setup was contaminated.
- `BLOCKED`: cannot continue without user action or a broken test harness.

## Test Context

- Device: real iPad Pro 11-inch (M4), iPadOS 26.5.
- Browser: Safari on iPad.
- Current URL: local DEV Anywhere web at `http://192.168.1.2:5174`.
- Current orientation at plan creation: landscape.
- Current keyboard state at plan creation: no hardware keyboard attached.
- WebKit inspection: available through `ios_webkit_debug_proxy` when stable.

## Evidence Policy

Evidence can include:

- Screenshot path.
- WebKit DOM/geometry snapshot.
- PTY output before/after.
- User confirmation text.
- Bug note with reproduction steps.

Screenshots and DOM snapshots are supporting evidence only for interactive or
visual quality items.

## Checklist

### A. Harness And Baseline

| ID  | Item                                     | Agent checks                                            | User confirms                                                       | Status | Evidence                                                                                       |
| --- | ---------------------------------------- | ------------------------------------------------------- | ------------------------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------------- |
| A1  | iPad is connected and inspectable        | Device appears in `devicectl`; Safari tab can be listed | Page shown on the physical iPad is the expected DEV Anywhere page   | PASS   | `devicectl` showed physical iPad; WebKit listed DEV Anywhere tab                               |
| A2  | Test URL and proxy are correct           | URL is local DEV Anywhere page; current session is PTY  | User agrees this is the intended test target                        | PASS   | Current WebKit target is local `http://192.168.1.2:5174/#/chat/-41gxTM4Z5o73mxHDD1y_?mode=pty` |
| A3  | Orientation and keyboard state are known | Read viewport and keyboard state                        | User confirms physical orientation and whether keyboard is attached | PASS   | Landscape viewport `1210x748`; keyboard offset `0`; user said no hardware keyboard             |

### B. Landscape, No Hardware Keyboard

| ID  | Item                     | Agent checks                                                                     | User confirms                                                                  | Status | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --- | ------------------------ | -------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| B1  | Sidebar layout           | Sidebar width, brand area, session list, bottom actions visible                  | Layout feels usable; no important content is squeezed out                      | PASS   | Landscape viewport `1210x748`; sidebar `280x748`, brand/proxy area `279x128`, session list and both terminal rows visible, with no geometry overflow. User accepted the live landscape baseline on 2026-07-14. Screenshot: `/tmp/dev-anywhere-ipad-b3-create-menu-live-system.png`.                                                                                                                                                                                                  |
| B2  | Bottom actions           | New and Settings buttons are complete and visually balanced                      | Buttons look intentional and tappable                                          | PASS   | `新建` and `设置` are both `128x46`, aligned at the bottom and fully visible. User accepted the live landscape baseline on 2026-07-14. Screenshot: `/tmp/dev-anywhere-ipad-b3-create-menu-live-system.png`.                                                                                                                                                                                                                                                                          |
| B3  | Create menu              | New menu opens; options are Agent session and Terminal session                   | Labels are clear; no duplicated wording in visible UI                          | PASS   | Live menu contained `Agent 会话` and `终端会话`, with no duplicated `新建` wording. User accepted the live landscape baseline on 2026-07-14. Screenshot: `/tmp/dev-anywhere-ipad-b3-create-menu-live-system.png`.                                                                                                                                                                                                                                                                    |
| B4  | PTY readability          | PTY area is visible and not overlapped                                           | Text is readable at normal viewing distance                                    | PASS   | PTY occupied the full right rail below the header, remained unobstructed, and showed colored terminal text. User accepted its live readability on 2026-07-14. Screenshot: `/tmp/dev-anywhere-ipad-b3-create-menu-live-system.png`.                                                                                                                                                                                                                                                   |
| B5  | Back-to-bottom placement | Button is in upper-right PTY area, not over bottom controls                      | Position feels discoverable and not annoying                                   | PASS   | A dedicated Codex PTY in `/private/tmp/dev-anywhere-uat-0.5` produced sufficient history. At `520px` away from bottom, the upper-right `36x36` button was visibly rendered (`opacity: 1`), interactive, and hit-tested to itself. User tapped it; container gap became `0px`, xterm `viewportY` matched `baseY=120`, and the button became inert with `opacity: 0`. User accepted placement and interaction on 2026-07-14. Screenshot: `/tmp/dev-anywhere-ipad-b5-agent-retest.png`. |
| B6  | Settings main dialog     | Dialog has safe margins and consistent structure                                 | Visual hierarchy and spacing are acceptable                                    | PASS   | Main settings dialog stayed inside the `1210x748` viewport, used internal scrolling for lower options, and kept the first-screen hierarchy intact. User accepted the live dialog on 2026-07-14. Screenshot: `/tmp/dev-anywhere-ipad-b6-settings-live-system.png`.                                                                                                                                                                                                                    |
| B7  | Settings subdialogs      | Relay Token, Client Management, Voice Pilot use consistent title/subtitle/layout | Copy and alignment feel consistent                                             | PASS   | Relay Token, Client Management, and Voice Pilot all kept safe landscape margins and left-aligned title/description structure. Client actions remained aligned and equal-sized. User accepted all three live subdialogs on 2026-07-14. Screenshots: `/tmp/dev-anywhere-ipad-b7-relay-token-live-system.png`, `/tmp/dev-anywhere-ipad-b7-client-management-live-system.png`, `/tmp/dev-anywhere-ipad-b7-voice-live-system.png`.                                                        |
| B8  | Input mode setting       | `输入方式` shows `自动 / 触控优先 / 实体键盘优先`                                | Copy is understandable                                                         | PASS   | Main settings showed `自动`, `触控优先`, and `实体键盘优先` in one fitted row. User accepted the live wording on 2026-07-14. Screenshot: `/tmp/dev-anywhere-ipad-b6-settings-live-system.png`.                                                                                                                                                                                                                                                                                       |
| B9  | Font size setting        | Font size controls are reachable and state changes apply without overflow        | Text size controls feel usable on iPad and do not make chat/PTY layout awkward | PASS   | PTY header menu opened via keyboard-accessible trigger; screenshot `/tmp/dev-anywhere-ipad-b9-font-menu-open.png`. Font size changed 16 → 17 (`xterm.options.fontSize` and `localStorage`), then reset to 16. Menu stayed within viewport.                                                                                                                                                                                                                                           |

### C. Landscape Touch Input, No Hardware Keyboard

| ID  | Item                         | Agent checks                                                | User confirms                                 | Status | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --- | ---------------------------- | ----------------------------------------------------------- | --------------------------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C1  | Tapping PTY focuses terminal | Active element becomes terminal textarea                    | Tap target feels natural                      | PASS   | With the hardware keyboard attached, the user tapped the dedicated Codex PTY. The active element became the visible `TEXTAREA[aria-label="Terminal input"]`, and the user accepted the tap target on 2026-07-14.                                                                                                                                                                                                                                                                                                                                         |
| C2  | Soft keyboard behavior       | Keyboard and layout response matches the active input setup | Soft keyboard appears only when expected      | PASS   | In the same hardware-keyboard run, `innerHeight` and `visualViewport.height` both remained `748px`, keyboard offset/inset stayed unset, no mobile auxiliary controls were rendered, and `html/body` remained scroll-locked. The user confirmed there was no soft keyboard, auxiliary bar, or layout jump on 2026-07-14.                                                                                                                                                                                                                                  |
| C3  | Mobile auxiliary bar         | Controls appear above keyboard and fit viewport             | Controls are visible, not clipped, and usable | PASS   | Real iPad Safari landscape now renders one `57px` row above the keyboard. All 14 keys are `44px` high, arrow keys are contiguous as `← ↑ ↓ →`, and top/bottom padding is balanced at `6px` without duplicate safe-area space. The floating-keyboard hint was visible and the user accepted the final layout. Screenshot: `/tmp/dev-anywhere-ipad-controls-grouped-live.png`.                                                                                                                                                                             |
| C4  | Auxiliary Enter              | Pressing `回车` sends Enter to PTY                          | User sees expected prompt/output behavior     | PASS   | User tapped the live auxiliary `回车` key. The xterm buffer advanced from the empty prompt on row 8 to a fresh prompt on row 9, with the cursor at the new prompt, confirming that PTY received Enter.                                                                                                                                                                                                                                                                                                                                                   |
| C5  | Auxiliary control keys       | Esc, Tab, Shift+Tab, Ctrl-C/B/S/T behave reasonably         | No key feels broken or hidden                 | PASS   | User tapped the real iPad controls through raw PTY probes. Received `Esc=1b`, `Tab=09`, `Shift+Tab=1b 5b 5a`, `Ctrl-B=02`, `Ctrl-S=13`, `Ctrl-T=14`, and `Ctrl-C=03`. The temporary probe's terminal modes were restored and externally verified as `icanon/isig/ixon`.                                                                                                                                                                                                                                                                                  |
| C6  | Keyboard dismissal           | Closing keyboard restores layout without drift              | Selection/content does not jump unexpectedly  | PASS   | With the full soft keyboard open, the user attached the hardware keyboard. iPadOS dismissed the soft keyboard; `visualViewport.height` restored `320 → 748`, keyboard offset/layout inset restored `428 → 0`, auxiliary controls disappeared, PTY retained focus, and the content area returned to full height. Without another screen tap, the user typed `echo hw-switch`; the first key was retained, output was `hw-switch`, and adaptive modality changed `touch → hardware`. Screenshot: `/tmp/dev-anywhere-ipad-soft-to-hardware-transition.png`. |

### D. Landscape With Hardware Keyboard

This section is not optional. It should run immediately after the no-keyboard
landscape baseline so differences are easy to compare.

| ID  | Item                                      | Agent checks                                                            | User confirms                                     | Status | Evidence                                                                                                                                                                                                                                          |
| --- | ----------------------------------------- | ----------------------------------------------------------------------- | ------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | Hardware keyboard attached state is known | Re-read viewport/media/focus state after attach                         | User confirms keyboard is physically attached     | PASS   | User said keyboard is attached; WebKit state stayed landscape `1210x748`, `keyboardOffset=0`, `keyboardLayoutInset=0`                                                                                                                             |
| D2  | No accidental soft keyboard               | Tap/focus PTY with hardware keyboard attached                           | Soft keyboard does not unexpectedly cover content | PASS   | User physically tapped PTY with hardware keyboard attached; WebKit state after tap: active `TEXTAREA` / `Terminal input`, `visualViewport.height=748`, `keyboardOffset=0`, no `pty-mobile-controls`                                               |
| D3  | First key is not lost                     | User types a short command from keyboard without tapping extra controls | First character appears and command can run       | PASS   | User typed through hardware keyboard; PTY DOM tail showed `echo ipad-kbd-ok` and output `ipad-kbd-ok`                                                                                                                                             |
| D4  | Enter and Backspace                       | User types, deletes, and submits                                        | Behavior matches terminal expectations            | PASS   | User typed `echo ipad-kbdx`, Backspace, `-ok`, Enter; resulting command/output was `ipad-kbd-ok`; `keyboardOffset=0`, no mobile controls                                                                                                          |
| D5  | Tab and Shift+Tab                         | User presses Tab and Shift+Tab                                          | No focus trap or broken terminal input            | PASS   | User pressed Tab and Shift+Tab; PTY showed shell completion output, active element stayed `TEXTAREA` / `Terminal input`, `keyboardOffset=0`, no mobile controls                                                                                   |
| D6  | Arrow keys                                | User presses arrow keys in PTY                                          | Cursor/history behavior is acceptable             | PASS   | User pressed arrow keys after Tab checks; focus stayed in terminal textarea, page did not move focus to sidebar/buttons, `keyboardOffset=0`, no mobile controls                                                                                   |
| D7  | Touch plus keyboard mixing                | User touches terminal or selection, then types again                    | Focus remains predictable; no mode confusion      | PASS   | Retested from a clean prompt. User tapped PTY and typed `echo ipad-mix-clean`; WebKit/xterm buffer showed exact command and `ipad-mix-clean` output, active element stayed `TEXTAREA` / `Terminal input`, `keyboardOffset=0`, no mobile controls. |
| D8  | Switching sessions                        | User switches session and types again                                   | Hardware keyboard behavior still works            | PASS   | After switching to another terminal session, user typed `echo ipad-switch`; active keepalive entry showed `echo ipad-switch` and output `ipad-switch`, focus stayed `TEXTAREA`, `keyboardOffset=0`, no mobile controls                            |

### E. Portrait, No Hardware Keyboard

| ID  | Item                     | Agent checks                                                                                   | User confirms                                  | Status | Evidence                                                                                                                                                                                                                                                                                                              |
| --- | ------------------------ | ---------------------------------------------------------------------------------------------- | ---------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| E1  | Portrait shell layout    | Sidebar/mobile layout and session list fit                                                     | Important controls are not hidden or cramped   | PASS   | Real iPad Safari portrait viewport `834x1124`; sidebar `280x1124`; both `128x46` bottom actions and the active session row were fully visible. User accepted E1-E5 on 2026-07-14. Screenshot: `/tmp/dev-anywhere-ipad-e1-portrait-current.png`.                                                                       |
| E2  | Portrait create flow     | New session entry opens correctly                                                              | Sheet/dialog copy and spacing are acceptable   | PASS   | Create menu opened above the bottom action at `x=8,y=959,w=208,h=82`; both `Agent 会话` and `终端会话` were fully visible. User accepted E1-E5 on 2026-07-14. Screenshot: `/tmp/dev-anywhere-ipad-e2-portrait-create-webkit.png`.                                                                                     |
| E3  | Portrait settings flow   | Settings and subdialogs have safe margins                                                      | No edge-touching or overflow                   | PASS   | Main settings dialog was `440x895` at `x=197,y=114.5`; Relay Token subdialog was `440x258` at `x=197,y=433`; neither touched viewport edges. User accepted E1-E5 on 2026-07-14. Screenshots: `/tmp/dev-anywhere-ipad-e3-portrait-settings.png`, `/tmp/dev-anywhere-ipad-e3-portrait-relay-token.png`.                 |
| E4  | Portrait PTY readability | PTY view is readable and not clipped                                                           | Text and controls feel usable                  | PASS   | PTY occupied the full right rail below the `49px` header; existing command and prompt remained visible without horizontal page overflow. User accepted E1-E5 on 2026-07-14. Screenshot: `/tmp/dev-anywhere-ipad-e1-portrait-current.png`.                                                                             |
| E5  | Portrait soft keyboard   | PTY focus raises keyboard and adjusts viewport                                                 | Output area and auxiliary bar stay visible     | PASS   | After a trusted user tap, `visualViewport.height` changed `1124 → 784`; keyboard offset/layout inset became `340px`; terminal output stayed above the keyboard and the full auxiliary bar fit at `y=654..784`. User accepted E1-E5 on 2026-07-14. Screenshot: `/tmp/dev-anywhere-ipad-e5-portrait-soft-keyboard.png`. |
| E6  | Device-specific settings | iPad shows the input-mode override; non-iPad clients hide it; diagnostics use advanced wording | Settings remain relevant to the current device | PASS   | Real iPad Safari showed `交互 → 输入方式` and `高级诊断`. System Google Chrome `150.0.7871.115` reported `0` input-mode groups, `0` interaction headings, and `1` advanced-diagnostics heading. Screenshots: `/tmp/dev-anywhere-ipad-e6-device-settings.png`, `/tmp/dev-anywhere-google-chrome-settings-e6.png`.      |

### F. Touch Selection And Content Operations

| ID  | Item                       | Agent checks                                                                 | User confirms                                                | Status | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --- | -------------------------- | ---------------------------------------------------------------------------- | ------------------------------------------------------------ | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| F1  | Long-press selection       | Selection handles/toolbars appear near selected text                         | Selection does not drift when keyboard appears or disappears | PASS   | Real iPad Safari, portrait without hardware keyboard: long-pressed `F1-COPY-TEXT-20260714`; both handles and the nearby Copy toolbar were visible. Layout remained at `keyboardOffset=0`, `scrollY=0`. Screenshot: `/tmp/dev-anywhere-ipad-f1-long-press.png`.                                                                                                                                                                                                                                         |
| F2  | Copy selected text         | Clipboard operation can be completed                                         | Copied content is correct enough                             | PASS   | Real iPad Safari over local HTTP: active-gesture legacy clipboard fallback returned the exact 21-character selection; a second native paste reproduced `F1-COPY-TEXT-20260714` in full, and the user confirmed the final `已复制` success toast. Real Android Chrome gate: 6 passed, 2 skipped.                                                                                                                                                                                                        |
| F3  | Image preview              | Known local image path can preview                                           | Preview fits screen, including wide images                   | PASS   | Real iPad Safari portrait: Preview opened from the wrapped-path selection, decoded the actual `1179x2556` image with no error, and fit it inside the dialog without page overflow. Screenshots: `/tmp/dev-anywhere-ipad-f3-image-preview-ready.png`, `/tmp/dev-anywhere-ipad-f3-wrapped-image-preview-fixed.png`.                                                                                                                                                                                      |
| F4  | File download              | Known file path can download through formal link                             | Download flow is understandable and not timeout-prone        | PASS   | Real iPad Safari portrait selection resolved `README.md` to Copy/Download. Safari showed its native download confirmation; after the user confirmed, the download manager showed a completed `README` entry at `9 KB`, and opening it displayed the actual README source. User confirmed the native download/open actions on 2026-07-14. Screenshots: `/tmp/dev-anywhere-ipad-f4-download-system.png`, `/tmp/dev-anywhere-ipad-f4-download-list.png`, `/tmp/dev-anywhere-ipad-f4-download-opened.png`. |
| F5  | Selection fallback actions | Selecting a path-like text exposes preview/download fallback when applicable | Fallback is discoverable when auto-linking fails             | PASS   | Real iPad Safari portrait: the user long-pressed the upper physical row of a 69-character absolute image path whose `.png` extension wrapped onto the next row. The resulting selection spanned buffer `30:0` through `31:3`, returned the exact full path, and exposed `复制` / `预览`. Screenshot: `/tmp/dev-anywhere-ipad-f5-wrapped-image-selection-fixed.png`.                                                                                                                                    |

### G. Agent Sessions And Management

Use a local test agent session where possible. A Codex session is acceptable for
agent-management coverage; do not use production work that the user cares about.

| ID  | Item                                | Agent checks                                                                    | User confirms                                                     | Status | Evidence                                                                                                                                                                                                                                                                                                                                                                                                       |
| --- | ----------------------------------- | ------------------------------------------------------------------------------- | ----------------------------------------------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| G1  | Create Agent session                | New Agent session can be created from iPad                                      | Creation flow is understandable and has feedback while connecting | PASS   | Created chat-mode Codex test session `iPad UAT Codex` from iPad. Sidebar active count changed 2 → 3 and route became `/chat/tXLJAz9cJ0FA1YHrsNpnZ?mode=json`. Screenshots: `/tmp/dev-anywhere-ipad-g1-agent-create-dialog.png`, `/tmp/dev-anywhere-ipad-g1-agent-created.png`.                                                                                                                                 |
| G2  | Agent session list/status           | Sidebar shows agent session status accurately                                   | Labels and status do not conflict with terminal sessions          | PASS   | Sidebar showed `终端 2` for terminal sessions and `Codex 1` for the agent group. Test row text was `iPad UAT Codex · Codex · 空闲 · 刚刚`; no terminal/agent wording conflict observed.                                                                                                                                                                                                                        |
| G3  | Switch Agent sessions               | Switching between Agent and terminal sessions preserves context                 | User does not feel lost; focus and scroll are predictable         | PASS   | Switched from `iPad UAT Codex` JSON chat to terminal PTY and back. URL changed to `mode=pty` then back to `mode=json`; PTY refocused `Terminal input`, Agent view returned to empty `开始对话` state, `keyboardOffset=0` throughout.                                                                                                                                                                           |
| G4  | Agent management menu               | Session menu exposes expected actions without misleading terminal/agent wording | Management actions are clear and not dangerous by accident        | PASS   | Opened `iPad UAT Codex` row menu. Menu text was `重命名` / `终止会话`; no terminal wording appeared for the Agent session. Screenshot: `/tmp/dev-anywhere-ipad-g4-agent-menu.png`.                                                                                                                                                                                                                             |
| G5  | Approval banner and Always Yes      | Approval prompt, Always Yes, and menu state stay consistent                     | Banner position and behavior are acceptable on iPad               | PASS   | In strict-approval Codex chat session, `/tmp` write command showed approval banner and sidebar state `等待审批` with `始终允许` / `拒绝` / `允许`. Tapped `始终允许`; command completed and later `/tmp` write ran without another banner. Verified file contents, then removed temp files. Screenshots: `/tmp/dev-anywhere-ipad-g5-approval-banner.png`, `/tmp/dev-anywhere-ipad-g5-always-yes-complete.png`. |
| G6  | Agent session termination or detach | Management action works for test session only                                   | Copy matches Agent vs terminal semantics                          | PASS   | Terminated only the test Codex session. Confirmation dialog used Agent copy: `这会停止当前 Agent 进程...`; after confirming, route returned to `/sessions`, active count returned 3 → 2, and `iPad UAT Codex` disappeared. Screenshots: `/tmp/dev-anywhere-ipad-g6-agent-terminate-dialog.png`, `/tmp/dev-anywhere-ipad-g6-agent-terminated.png`.                                                              |

#### Agent Permission-Mode Protocol

Permission-mode acceptance must use disposable sessions and files. Create a
dedicated test directory plus one harmless marker outside the session working
directory, verify every side effect from the shell, and remove all markers when
the case ends. A frontend selection or successful create response alone does
not prove that a mode works.

Every visible provider, session-mode, and permission-mode combination is in
scope. Passing a JSON session does not stand in for PTY, and passing Claude Code
does not stand in for Codex.

| Provider    | Chat/JSON modes to run                   | PTY modes to run                         |
| ----------- | ---------------------------------------- | ---------------------------------------- |
| Claude Code | Strict, Auto, Accept Edits, Plan, Bypass | Strict, Auto, Accept Edits, Plan, Bypass |
| Codex       | Strict, Auto, Bypass                     | Strict, Auto, Bypass                     |

For each runtime case, record the session ID, selected mode, observed approval
UI, decision taken, resulting file/command side effect, and cleanup result.

| ID  | Item                            | Agent checks                                                                                                                                           | User confirms                                                                                      | Status      | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --- | ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| G7  | Permission option matrix        | Claude exposes Strict/Auto/Accept Edits/Plan/Bypass; Codex exposes Strict/Auto/Bypass in both Chat and PTY create flows                                | Labels make the safety choices understandable to the intended Agent-familiar user                  | PASS        | Real iPad Safari DOM verified all four Provider x Chat/PTY combinations: Claude exposes `default/auto/acceptEdits/plan/bypassPermissions`; Codex exposes `default/auto/bypassPermissions`. The user reviewed the live menu and accepted the concise labels without additional descriptions on 2026-07-14. Screenshot: `/tmp/dev-anywhere-ipad-g7-claude-permissions.png`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| G8  | Provider/mode switching         | Switching provider or Chat/PTY removes unsupported values, preserves valid choices, and never submits a hidden stale permission mode                   | The form does not unexpectedly reset unrelated fields                                              | PASS        | On the real iPad, Claude `plan` normalized to Codex `default`; Codex `auto` survived switching Chat -> PTY -> Claude. Test name `G8 preserve` and CWD `/tmp/dev-anywhere-uat-0.5` remained unchanged throughout. No unsupported option remained in the active native select.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| G9  | Strict runtime matrix           | In all four Provider x Chat/PTY combinations, a protected mutation waits for approval; Allow runs once, Deny leaves no side effect                     | Approval UI identifies the operation and the session without ambiguity                             | PASS        | All four strict-mode combinations passed on the real iPad Safari in CWD `/tmp/dev-anywhere-uat-0.5`. Codex PTY session `deW6bLe-VQpffzFIaFJYv`: Allow produced exactly `one` once, Deny left no marker, and an 8-second/154-sample probe found no approval-row state or geometry flips. Codex Chat session `3xJ6lYbc0hvV8bLY7zanD`: Allow produced the exact marker `cdx`, Deny left no marker; the supplemental command returned `cdx-output`, wrote exactly one 11-byte line, and the final response was `FINAL: cdx-output`. Claude Chat session `wPLrnEnbWuyYm4eoldJNR`: Allow wrote exactly 3 bytes (`cla`) and replied `已执行。`; Deny left no marker and replied `命令被拒绝，未执行。`. Claude PTY session `KONOmNqAna7lCqEeIHkyc`: startup was Idle, approval remained stable, Allow wrote exactly 3 bytes (`clp`, hex `636c70`) and displayed `FINAL: clp`, Deny left no marker, and both completed turns remained Idle after subsequent TUI redraws. Approval screenshot: `/tmp/dev-anywhere-ipad-g9-claude-pty-approval-final.png`. The user accepted the restrained approval breathing effect on 2026-07-14.                                                                             |
| G10 | Auto runtime matrix             | In all four Provider x Chat/PTY combinations, ordinary sandboxed work proceeds; an escalation still asks when the provider requires approval           | The mode feels useful without silently becoming Bypass                                             | PASS        | All four Auto combinations passed on the real iPad Safari in canonical CWD `/private/tmp/dev-anywhere-uat-0.5`. Codex Chat session `n-0KLOMQyuL1FUdC8kaXx` wrote exactly `cc-auto`; Codex PTY session `bytRhfiEiETvruapKOgwa` wrote exactly `cp-auto`. Both completed ordinary work without approval, then requested approval for a home-directory mutation; Deny left both outside-CWD markers absent. Claude Chat session `jaLt_zofgSZzeTPM0PriO` wrote exactly `hc-auto` and returned `FINAL: hc-auto`; Claude PTY session `TxYoEb7xkdkqaUX6kYsPp` showed native `auto mode on`, wrote exactly `hp-auto`, and displayed `FINAL: hp-auto`, with no approval in either ordinary turn. Process arguments confirmed `--permission-mode auto`, not Bypass. A strict-mode Claude Chat regression in session `wPLrnEnbWuyYm4eoldJNR` still produced a real approval; Deny left its marker absent. This run exposed and fixed a structural defect where the DEV Anywhere `PreToolUse` hook unconditionally returned `permissionDecision: ask`, overriding Claude's native Auto semantics. Screenshots: `/tmp/dev-anywhere-ipad-g10-claude-chat-auto.png`, `/tmp/dev-anywhere-ipad-g10-claude-pty-auto.png`. |
| G11 | Claude Accept Edits matrix      | In Claude Chat and PTY, Edit/Write operations proceed automatically while a non-edit privileged command still follows the normal approval path         | The distinction between edits and other tools is understandable                                    | PASS        | Both Claude Accept Edits combinations passed on the real iPad Safari in canonical CWD `/private/tmp/dev-anywhere-uat-0.5`. Chat session `-qkRQJjDWeKnVlNorcyKc` started with `--permission-mode acceptEdits`: Write created `g11-claude-chat-edit.txt` with exact bytes `he-edit` without approval, while privileged Bash targeting the home directory produced a real web approval; Deny left the outside-CWD marker absent and the session returned Idle. PTY session `2ENmToPJYbL3__UR1SCNr` displayed native `accept edits on`: Write created `g11-claude-pty-edit.txt` with exact bytes `hp-edit` and displayed `FINAL: hp-edit` without approval, while privileged Bash produced Claude's native approval. Selecting `3. No` without an extra Enter left both outside-CWD probe markers absent and the sidebar stably returned Idle. An initial automation attempt sent a redundant Enter after the single-key menu selection and was discarded as harness contamination, not treated as product behavior. Screenshots: `/tmp/dev-anywhere-ipad-g11-claude-pty-accept-edits-approval.png`, `/tmp/dev-anywhere-ipad-g11-claude-pty-accept-edits-denied.png`.                                      |
| G12 | Claude Plan matrix              | In Claude Chat and PTY, read-only planning remains usable; mutation/execution creates no side effect and does not leave the turn hanging               | No separate wording check: G7 covers app-owned labels; runtime explanations are Provider-generated | PASS        | Both runtime combinations passed on the real iPad Safari in canonical CWD `/private/tmp/dev-anywhere-uat-0.5`. Chat session `MRQYLh5rd6H5pr-G40sN9` ran with `--permission-mode plan`: Read returned exact fixture text `READ: PLAN-READ-OK`; a requested Write was blocked, left `g12-claude-chat-mutation.txt` absent, and returned Idle. PTY session `f4WI8tEU0l_iJaJzdnxXY` displayed native `plan mode on` and also ran with `--permission-mode plan`: Read returned exact fixture text; a requested Write was blocked, left `g12-claude-pty-mutation.txt` absent, and returned Idle. The explanatory prose shown after each request was generated by Claude, so it was not treated as DEV Anywhere UI copy or as manual UX evidence. The first PTY Read request received no Provider response for over two minutes and was cancelled; a clean retry completed in 12 seconds, so the contaminated attempt was not used as the mode result. Screenshots: `/tmp/dev-anywhere-ipad-g12-claude-chat-plan.png`, `/tmp/dev-anywhere-ipad-g12-claude-pty-plan.png`.                                                                                                                                      |
| G13 | Bypass runtime matrix           | In all four Provider x Chat/PTY combinations, the destructive warning appears before creation and test mutations run without approval                  | Warning is prominent, specific, and difficult to accept accidentally                               | PASS        | All four Bypass combinations passed on the real iPad Safari in canonical CWD `/private/tmp/dev-anywhere-uat-0.5`. The first run exposed a real defect: selecting Bypass created the session immediately without a second confirmation. Creation and history restore now use a two-step warning surface; no Relay request is sent until the red `确认` action, `返回` preserves the form, and the destructive action is not default-focused. The user accepted the compact provider-specific warning on 2026-07-15. Claude Chat session `QL2wVyDfESa2vOufWiNRe` ran with `--permission-mode bypassPermissions`, wrote exact `claude-chat-ok` outside the CWD, returned `FINAL: claude-chat-ok`, and became Idle without approval. Claude PTY session `RcfNRVbglifJiwgKMghii` displayed native `bypass permissions on`, wrote exact `claude-pty-ok`, returned the expected final, and became Idle without tool approval. Codex Chat session `69z-WSXA8RW-6ONMA3VRX` ran with worker mode `bypassPermissions`, wrote exact `codex-chat-ok`, returned the expected final, and became Idle without approval. Codex PTY session `sRmH0tJ8QxRCVc4Q35RXs` launched with `--dangerously-bypass-approvals-and-sandbox`, displayed native `permissions: YOLO mode`, wrote exact `codex-pty-ok`, returned the expected final, and became Idle without approval. All probe files were removed. Screenshots: `/tmp/dev-anywhere-ipad-g13-bypass-confirm-compact.png`, `/tmp/dev-anywhere-ipad-g13-claude-chat-runtime.png`, `/tmp/dev-anywhere-ipad-g13-claude-pty-runtime.png`, `/tmp/dev-anywhere-ipad-g13-codex-chat-runtime.png`, `/tmp/dev-anywhere-ipad-g13-codex-pty-runtime.png`. |
| G14 | Approval decisions and queue    | Allow, Always Allow, and Deny resolve the intended request exactly once; two queued approvals retain order and do not duplicate cards or banners       | Queue position and resolved state are understandable                                               | PASS        | Real-provider decisions passed on the real iPad Safari in strict Claude Chat session `OO2baQgSR6SNm6uUNxTJv`. Claude emitted two independent Bash calls in one turn; the Provider SDK delivered their approvals serially in generation order. Allowing A wrote exact `queue-a`, then B appeared without a duplicate card; denying B left its marker absent and the session returned Idle. A separate one-shot append produced exactly one byte (`A`). `始终允许` produced exactly one byte (`C`), and a later Bash call appended `D` without another approval, leaving exact bytes `CD` and returning Idle. Because the supported Provider serialized real approval delivery, the simultaneous two-card layout was exercised separately on the same real iPad by injecting two protocol-valid pending approvals into the live frontend store: the DOM contained exactly two unique cards, the summary read `2 个工具审批待处理`, and the cards were labelled `1/2` and `2/2`. The user confirmed that the queue order and action ownership were clear on 2026-07-15. The injected state and every probe file were removed afterward. Screenshots: `/tmp/dev-anywhere-ipad-g14-always-approval.png`, `/tmp/dev-anywhere-ipad-g14-approval-queue-visual.png`. |
| G15 | Always Yes scope and background | Always Yes survives switching away from its session and keeps handling that session; another session and another development machine do not inherit it | Menu/banner state stays synchronized when returning to the original session                        | PASS        | Real iPad Safari and real Claude Code PTY verification used two strict-approval sessions on proxy `6s-wnL2U0bY8nVNVOuJsg`. Always Yes was enabled only for session `V0FAVKwCczI4vXMVmLPOx`; after submitting a Bash append and immediately navigating to another session, the original session completed in the background and wrote exactly one byte (`B`, hex `42`). Returning to it showed no approval banner and the overflow-menu checkbox remained checked. Session `SdpgEthsrj3j7wH_iFnl4` did not inherit the setting: the same class of Bash mutation remained in real `approval_wait`, its banner showed `等待审批` and `Always yes`, and its marker stayed absent until the request was denied. Only one development machine was online, so cross-machine isolation was not represented as a dual-machine run; controller regressions instead prove that neither another session nor the same session ID under another proxy key receives auto-enter. The full web suite passed with 122 files and 963 tests. Both sessions returned Idle, the setting was disabled, and all probe files were removed. Screenshot: `/tmp/dev-anywhere-ipad-g15-always-yes-return.png`. |
| G16 | Approval refresh and reconnect  | Refresh or Relay/proxy reconnect replays a pending approval once, preserves the correct session state, and accepts a decision after recovery           | No stale Working/Idle label or duplicate approval is visible                                       | PASS        | Both recovery paths passed on the real iPad Safari with strict Claude Code PTY session `SdpgEthsrj3j7wH_iFnl4`. For page refresh, the pre-refresh session and PTY states were `waiting_approval` / `approval_wait` with exactly one `等待审批` banner and one matching sidebar row. After a full page reload, the route and those states were restored, the DOM still contained exactly one banner, and one Allow decision wrote exactly one byte (`R`, hex `52`) before the session returned Idle. For a real Relay reconnect, the live WebSocket was closed while a second Bash request was pending. The recorded app-store trace moved from connected through `connected=false`, `proxyOnline=false`, and `phase=reconnecting`, then back to `connected=true`, `proxyOnline=true`, and `phase=session_browsing`. The same pending request remained `waiting_approval`, still rendered one banner and one sidebar state, and one post-reconnect Allow wrote exactly one byte (`C`, hex `43`) before returning Idle. No stale Working/Idle label or duplicate prompt appeared. Both probe files and the temporary state subscriber were removed. |
| G17 | Historical session restore mode | Restoring each supported provider with a selected permission mode starts with that mode; Bypass requires confirmation; cancel leaves history intact    | Restore wording clearly separates session history from permission choice                           | PASS        | Real iPad Safari verification passed for both providers, both Chat/Terminal views, and all three supported restore permission modes. Terminal restores launched Claude Code with `--permission-mode default/auto/bypassPermissions --resume ...`; Codex launched with `--ask-for-approval untrusted/on-request resume ...` or `--dangerously-bypass-approvals-and-sandbox resume ...`. After the user noticed that Chat incorrectly hid permission choices, restore permissions were made independent of the presentation mode. The repaired Chat matrix then launched six real proxy workers: Claude Code and Codex each emitted `session_create` with `mode=json`, the selected `permissionMode`, and the correct resume ID; every persisted worker process contained the matching `--permission-mode default/auto/bypassPermissions --resume ...` arguments. Chat and Terminal now share the same permission controls, switching views preserves the selection, and Bypass in either view requires the provider-specific second confirmation. In every Bypass path, the first action left the route and active-session count unchanged. Cancel closed the dialog without navigating, creating a session, or removing its history row. The dialog now focuses its surface on open, so a stale blue keyboard-focus ring no longer competes with the amber selected state; the primary action is consistently labelled `恢复`. The user accepted the final live wording and state hierarchy on 2026-07-15. All 12 temporary restore sessions were terminated and absent from persisted session state, and no Provider history file was added. The web suite passed with 122 files and 964 tests, typecheck passed, and the targeted mobile restore-sheet Playwright case passed. Screenshots: `/tmp/dev-anywhere-ipad-g17-restore-bypass-confirm.png`, `/tmp/dev-anywhere-ipad-g17-restore-focus-fixed.png`, `/tmp/dev-anywhere-ipad-g17-chat-permissions.png`. |
| G18 | Permission failure cleanup      | Creation timeout, agent exit, denied request, and terminated session clear pending approval state and temporary files                                  | Failure feedback is actionable and does not strand the user                                        | PASS        | Real iPad Safari and local process inspection covered each failure boundary. An invalid CWD remained in the create dialog with the exact inaccessible path and did not add a session. With Claude temporarily pointed at `/usr/bin/false`, the worker exited before opening its control socket and the repaired startup transaction returned an actionable failure in about 204 ms instead of waiting for the 30-second deadline; no session or temporary directory remained, and the configured Claude path was restored afterward. In strict Claude Chat session `TvO2JX3NXjj5knVUKn04_`, killing worker PID `82562` while a Bash approval was pending immediately removed the active session, approval, status, hook directory, and marker without waiting for the reaper; the iPad showed the terminal session-ended panel. In session `77WYMXj2plwFxV62uAElf`, Deny removed the approval and left the marker absent, while terminating the session during a second pending request returned to the session list and removed the approval, session directory, and marker. Backend cleanup now has one canonical session-removal lifecycle; JSON and hosted-PTY startup are unpublished transactions with bounded deadlines and explicit abort cleanup, and stale frontend approval/status state is pruned by authoritative session updates. Full regression: 226 test files passed plus 1 skipped; 1896 tests passed plus 1 skipped. |

### H. Voice Pilot

Voice behavior requires human judgment for permission prompts, timing, and
whether the UI feels distracting.

| ID  | Item                         | Agent checks                                                                     | User confirms                                            | Status | Evidence                                                                                                                                                                                                                                                                                                              |
| --- | ---------------------------- | -------------------------------------------------------------------------------- | -------------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| H1  | Voice Pilot settings         | Voice Pilot settings page opens with safe margins                                | Title/subtitle/alignment match other subdialogs          | PASS   | Opened Voice Pilot settings from the iPad settings dialog. Dialog stayed centered in landscape, title/subtitle were left-aligned, form scrolled independently, and footer buttons stayed fixed. Screenshot: `/tmp/dev-anywhere-ipad-h1-voice-settings.png`.                                                           |
| H2  | Insecure-context degradation | HTTP/non-mediaDevices environment refuses startup with a clear recoverable error | No confusing permission prompt or stuck state            | PASS   | On the local HTTP URL, Safari reported `isSecureContext=false` and no `navigator.mediaDevices`; pressing `开启 Voice Pilot` showed `当前浏览器不支持麦克风访问。` and left the dialog cancellable. Screenshots: `/tmp/dev-anywhere-ipad-h2-voice-confirm.png`, `/tmp/dev-anywhere-ipad-h2-voice-mic-unsupported.png`. |
| H3  | Injected control layout      | Dev-only state injection renders the active panel without viewport overflow      | Controls do not collide with keyboard or chat input      | PASS   | Injected `聆听` state rendered above the chat input with a visible waveform and stop button. This is visual coverage only, not microphone E2E. Screenshot: `/tmp/dev-anywhere-ipad-h3-voice-listening-panel.png`.                                                                                                     |
| H4  | Injected feedback visuals    | Dev-only state injection renders Listening/Speaking/Error states                 | State labels and motion are readable and not distracting | PASS   | Injected `聆听`, `播报`, and `异常` states remained inside the panel. This is visual coverage only, not provider E2E. Screenshots: `/tmp/dev-anywhere-ipad-h4-voice-speaking-panel.png`, `/tmp/dev-anywhere-ipad-h4-voice-error-panel.png`.                                                                           |

#### Voice Pilot End-To-End Protocol

The remaining Voice Pilot cases require a real HTTPS page, a configured voice
provider, actual microphone capture, and audible playback. Dev-store injection,
mock WebSockets, or screenshots cannot satisfy these items. Use a disposable
Agent session and record the provider/model, session ID, browser permission
state, exact utterance, recognized text, spoken output, and recovery result.

Run the layout-sensitive cases in portrait and landscape, first without the
hardware keyboard and then with it attached. Keep the user involved for audio
quality, timing, permission prompts, and whether feedback is distracting.

| ID  | Item                              | Agent checks                                                                                                                                                          | User confirms                                                                   | Status      | Evidence |
| --- | --------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- | ----------- | -------- |
| H5  | Provider configuration            | Load capabilities; select region/ASR/TTS/voice; validate idle timeout; test playback; save; reopen and confirm persisted non-secret configuration                     | Labels, voice sample, save feedback, and secret handling are understandable     | PASS        | Real iPad Safari loaded the configured China region, Qwen3 ASR Flash Realtime, CosyVoice V3 Flash, Long Anhuan voice, and 3-second idle timeout. The user confirmed that the live test playback was complete, clear, and good quality. Saving showed `已保存`; after returning to Settings and reopening Voice Pilot, every non-secret selection remained unchanged while the stored API key stayed non-readable. Screenshot: `/tmp/dev-anywhere-ipad-h5-playback-pass.png`. |
| H6  | HTTPS microphone grant and start  | From a clean Safari permission state, enable Voice Pilot, grant microphone access, open ASR/TTS sockets, acquire wake lock, and reach Listening                       | Native prompt appears at the right time and startup feedback is clear           | PASS        | On the real iPad Safari HTTPS UAT page, the user enabled Voice Pilot, accepted the microphone flow, heard the listening-start cue, and confirmed that startup reached Listening without a stuck preparation state or error. Diagnostics recorded `wake-lock-acquired`, TTS and ASR WebSocket readiness, a `starting -> listening` transition, a real microphone source at 16 kHz, and `capture:listening`. The later disappearance of the panel was user-initiated Exit, not a runtime failure; cleanup released capture, playback, and the audio session. Screenshot: `/tmp/dev-anywhere-ipad-h6-listening.png`. |
| H7  | Permission denial and recovery    | Deny microphone, verify clean failure, grant it later in Safari settings, and start successfully without reloading or losing the typed draft                          | Recovery instructions are accurate and not repetitive                           | PASS        | A fresh Quick Tunnel origin forced a real first-use Safari permission prompt. After the user chose Don't Allow, startup showed `没有麦克风权限，请在浏览器里允许访问麦克风。`, Voice Pilot remained disabled, the startup Wake Lock transaction rolled back, and draft `H7-DRAFT-KEEP-20260716` remained intact. The user then changed the same origin to Allow and retried from the still-open confirmation dialog without reloading. Voice Pilot entered Listening, diagnostics recorded a real 16 kHz microphone source and `capture:listening`, and the draft was still unchanged. Earlier attempts were discarded because an existing origin retained usable microphone access despite a contradictory Permissions API state, and a stale UAT Service Worker referenced a removed VAD chunk. Screenshots: `/tmp/dev-anywhere-ipad-h7-native-denied.png`, `/tmp/dev-anywhere-ipad-h7-recovered-listening.png`. |
| H8  | Live Chinese ASR turn             | Speak a known Chinese sentence; partial bubble updates in place; final text matches; earcon plays; one JSON user turn is submitted after buffering                    | Recognition, submission timing, and visible transcript feel correct             | PASS        | On the real iPad Safari fresh-origin run, the user said `现在几点钟啊`. The visible final transcript was `现在几点钟啊？`, matching the intended utterance. Diagnostics recorded partial updates growing from 1 to 7 characters, exactly one ASR final, one routed text turn, one Agent response, and one completed TTS request. The user confirmed that the Agent replied only once and playback was clear. Draft `H7-DRAFT-KEEP-20260716` remained unchanged. |
| H9  | ASR segmentation and noise        | Test a short utterance, multiple final segments, pause-before-final, silence, and ambient noise; no empty/duplicate/early turn is submitted                           | Normal pauses do not make the interaction feel unreliable                       | PASS        | Real iPad Safari testing covered sustained silence, household noise, a short nasal sound, empty provider results, and the normal sentence `测试一下语音识别`. Silence produced no ASR turn. The provider VAD threshold was moved to the noise-resistant `0.8` profile: short non-speech attempts usually completed without text, while the normal sentence was transcribed exactly and submitted once. Bailian `session.finished` now closes the attempt normally instead of leaving the client on a timeout. Empty attempts rearm the existing speech pipeline without releasing the microphone, clearing the waveform, or switching the audio session; this removed the reproducible 1.23-second flat waveform gap around the fifth second after startup. The user confirmed the final real-device behavior was good. Occasional household noise may still be transcribed as a short utterance such as `嗯`; the user accepted this residual ASR limitation, and isolated short user utterances remain supported. |
| H10 | Typed draft isolation             | Keep text in the input while speaking; voice turn submits independently and the typed draft remains unchanged                                                         | Voice does not unexpectedly overwrite or send typed work                        | PASS        | On the real iPad Safari UAT page, draft `H10-DRAFT-KEEP-20260717` remained unchanged while a live voice turn was recognized, submitted as a separate user message, answered once by the Agent, and spoken by Voice Pilot. The draft was neither overwritten nor submitted. During the same run, a fixed Markdown response `来源：[国务院通知](https://www.gov.cn/)` exposed that raw link destinations were being spoken. Display text and speech text are now separated at the shared speech-queue boundary: Markdown links retain their labels, bare URLs become `链接`, and replay/summary/approval paths use the same normalized text. A real iPad WebSocket observation recorded the final TTS payload as exactly `来源：国务院通知`, with no URL, while the draft still remained intact. Screenshot: `/tmp/ipad-h10-url-speech-result.png`. |
| H11 | Agent busy and second turn        | Capture stops while Agent is working/speaking, does not resume early, then starts a fresh ASR turn after completion and accepts a second utterance                    | Listening/Waiting transitions match what the user hears                         | PASS        | On the real iPad Safari UAT page, the first Agent response released capture before entering Waiting/Speaking. After TTS provider completion, queued PCM playback, and the assistant-end cue all finished, the state machine returned to Listening and started a fresh microphone capture. The user then said `第二轮语音测试，请回复收到。`; it was submitted as one new turn, the Agent replied exactly `收到。`, and the user confirmed hearing it once. An `agentBecameIdle` event preceded the completed assistant message by 2 ms, briefly changing the logical phase to Listening, but no microphone capture reached `capture:listening` before `assistantTextReady` changed the phase to Speaking and released capture again. A later ambient sound was transcribed as a separate `嗯。`, matching the accepted H9 residual ASR limitation rather than a duplicated second turn. Screenshot: `/tmp/ipad-h11-second-turn.png`. |
| H12 | Real TTS response                 | A new prose response is spoken once, historical messages are not replayed, queued audio finishes before capture resumes, and stop halts playback                      | Speech is intelligible, timely, and not duplicated                              | PASS        | Real iPad Safari playback covered short and long new prose responses. Enabling Voice Pilot did not replay historical messages; each new response produced one TTS request and one audible playback. Diagnostics showed capture released before speech, provider completion followed by draining the queued PCM and assistant-end cue, and microphone capture resumed only after playback completed. For explicit stop coverage, a five-sentence response was playing with more than one second of queued PCM when the user pressed the panel close button. The user confirmed that sound stopped immediately. At the same timestamp diagnostics recorded runtime cleanup, player stop with queued audio reset to zero, and audio-session release; later callbacks belonged to already-scheduled sources ending silently, produced no new state transition, and the machine remained Idle. Draft `H10-DRAFT-KEEP-20260717` remained unchanged. Screenshot: `/tmp/ipad-h12-stopped-playback.png`. |
| H13 | Code-heavy summary and fallback   | Code/table/diagnostic replies request a concise speech summary; provider-summary failure uses a deterministic fallback without hanging                                | Spoken summary is useful and directs the user to visual detail when appropriate | PASS        | Real iPad Safari testing used an Agent reply containing a TypeScript function and a Markdown table. The full code and table remained visible, while the TTS payload was a concise generated summary explaining the function and reporting that typecheck and tests passed; raw code was not spoken. For the failure path, UAT instrumentation intercepted exactly one `voice_summary_request` on the live Relay WebSocket and injected a schema-valid failure response without disturbing Agent, ASR, or TTS connections. The controller immediately sent the deterministic code fallback `这条回复包含代码，我先概括：它给出了一段实现或配置，请查看屏幕确认细节。` to TTS, then returned to Listening without hanging. Draft `H10-DRAFT-KEEP-20260717` remained unchanged throughout. |
| H14 | Voice session commands            | Exercise Repeat and Exit; ordinary similar phrases remain normal user text                                                                                            | Commands are discoverable and do not trigger accidentally                       | PASS | Repeat and Exit passed on real iPad; speculative Pause/Resume/Cancel/Redo/Status commands removed |
| H15 | Spoken approval decisions         | For real pending tools, hear a concise operation-specific prompt and say Allow, Always Allow, and Deny in separate disposable sessions                                | Prompt gives enough context to decide without reading raw parameter noise       | NOT_STARTED |          |
| H16 | Multiple/external approvals       | Queue two approvals, announce position once, resolve one externally, and verify Voice Pilot advances or waits without speaking stale requests                         | Queue progression is understandable and never repeats resolved work             | NOT_STARTED |          |
| H17 | Session switching and isolation   | Switch Agent sessions while Voice Pilot is active; only the owning session captures/speaks; returning restores coherent state; terminal is unaffected                 | No hidden session records audio or steals playback                              | NOT_STARTED |          |
| H18 | Orientation and keyboard layout   | In portrait/landscape and soft/hardware keyboard states, panel, transcript, stop control, input, and system keyboard remain visible and operable                      | Controls do not crowd content or cause unexpected keyboard changes              | NOT_STARTED |          |
| H19 | Network/provider failure recovery | Drop ASR, TTS, Relay, and development-machine connectivity separately; surface the active failure, stop capture/playback, reconnect, and restart                      | Errors identify the failing stage and recovery does not require guesswork       | NOT_STARTED |          |
| H20 | Background, lock, and wake lock   | Background Safari and lock/unlock the iPad during Listening/Waiting/Speaking; verify privacy-safe suspension and an explicit, coherent resume path                    | No unexpected recording or stale audio occurs after returning                   | NOT_STARTED |          |
| H21 | Disable and resource cleanup      | Disable Voice Pilot in every active phase; microphone indicator clears, tracks/context/sockets/timers stop, pending approval state clears, and refresh stays disabled | The user can reliably tell that listening has ended                             | NOT_STARTED |          |
| H22 | Echo and feedback-loop resistance | Use iPad speakers and microphone together; TTS/earcons are not recognized as a new user turn and do not create repeated Agent responses                               | Echo handling is acceptable in the intended physical setup                      | NOT_STARTED |          |

### I. Chat Bubbles

Chat bubbles are a high-risk visual surface. Cover representative message types
rather than only a happy-path text reply.

| ID  | Item                         | Agent checks                                      | User confirms                                                          | Status | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --- | ---------------------------- | ------------------------------------------------- | ---------------------------------------------------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| I1  | User text bubble             | Send a normal text prompt                         | User bubble spacing, wrapping, and alignment are acceptable            | PASS   | Sent multiple text prompts in `iPad UAT Codex`; prompts rendered in the chat timeline without viewport overflow.                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| I2  | Assistant text bubble        | Receive a normal assistant reply                  | Assistant bubble is readable and visually distinct                     | PASS   | Received normal assistant replies for `pwd` and Markdown-only prompts; text was readable in iPad landscape.                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| I3  | Streaming bubble             | Observe an in-progress assistant response         | Streaming state does not jump or flicker badly                         | PASS   | Sent 80-line text request and captured `工作中` state while output was growing. Screenshot: `/tmp/dev-anywhere-ipad-i3-long-stream-attempt.png`.                                                                                                                                                                                                                                                                                                                                                                                                   |
| I4  | Long text bubble             | Render long paragraphs and long unbroken text     | Wrapping does not overflow or crush adjacent UI                        | PASS   | Markdown prompt included `SuperLongTokenForIPadBubbleWrappingCheck_ABCDEFGHIJKLMNOPQRSTUVWXYZ_0123456789`; DOM confirmed token present and no main-content viewport overflow.                                                                                                                                                                                                                                                                                                                                                                      |
| I5  | Code block bubble            | Render code/diff-like content                     | Code is readable; horizontal scroll or wrapping behavior is sane       | PASS   | Rendered TypeScript code block and command blocks; `pre`/`code` rects stayed inside viewport.                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| I6  | Tool/activity bubble         | Render agent/tool activity entries                | Status, icons, and labels are understandable                           | PASS   | Codex command activity rendered `运行命令` and `已运行` blocks for `pwd` and `/tmp` write commands.                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| I7  | Approval/request bubble      | Trigger or inspect approval-related bubble/banner | It is visible without covering header or input awkwardly               | PASS   | `/tmp` write command triggered approval banner with `始终允许` / `拒绝` / `允许`; screenshot `/tmp/dev-anywhere-ipad-g5-approval-banner.png`.                                                                                                                                                                                                                                                                                                                                                                                                      |
| I8  | Error/empty/reconnect bubble | Render an error or reconnect state if available   | Copy and layout are clear and not noisy                                | PASS   | Empty state `开始对话` was visible in the temporary Codex chat. A dev-injected error activity rendered as a compact activity row, and expanded details stayed within the chat rail without covering the input. Screenshots: `/tmp/dev-anywhere-ipad-i8-error-activity-bubble-valid.png`, `/tmp/dev-anywhere-ipad-i8-error-activity-details.png`.                                                                                                                                                                                                   |
| I9  | Image/file bubble            | Attach or display image/file-related chat content | Preview/download affordances fit iPad and do not timeout in normal use | PASS   | Injected real local paths from this repo: `docs/assets/readme-mobile-create.png` and `README.md`. Chat rendered inline preview/download buttons without path overflow. Image preview opened through the formal preview URL, loaded successfully, and fit the iPad landscape dialog. File download showed `已开始下载 /Users/catli/MyApps/dev-anywhere/README.md`. Screenshots: `/tmp/dev-anywhere-ipad-i9-inline-file-image-links.png`, `/tmp/dev-anywhere-ipad-i9-image-preview-dialog.png`, `/tmp/dev-anywhere-ipad-i9-file-download-toast.png`. |
| I10 | Scroll with many bubbles     | Use a chat with many mixed bubbles                | Scroll, back-to-bottom, and keyboard interactions remain stable        | PASS   | Mixed conversation contains user prompts, assistant text, command activity, approval, Markdown/code/table, and 80-line long reply. Final screenshot `/tmp/dev-anywhere-ipad-i-long-bubbles-final.png` showed bottom content and input still reachable.                                                                                                                                                                                                                                                                                             |

### J. Portrait With Hardware Keyboard

Run this if landscape hardware-keyboard testing passes and the user can keep
the keyboard attached while rotating.

| ID  | Item                            | Agent checks                                         | User confirms                                | Status | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --- | ------------------------------- | ---------------------------------------------------- | -------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| J1  | Rotation with keyboard attached | Re-read viewport after rotation                      | Layout does not enter a confused mode        | PASS   | User rotated the real iPad to portrait while keeping the hardware keyboard attached. Viewport became `834x1124`; sidebar stayed `280x1124`, PTY stayed `554x1075`, keyboard inset remained `0`, auxiliary controls remained absent, and the user reported no visual abnormality. Screenshot: `/tmp/dev-anywhere-ipad-j1-portrait-hardware.png`.                                                                                                                                |
| J2  | Portrait keyboard input         | User types in PTY                                    | No accidental soft keyboard; first key works | PASS   | Without another screen tap after rotation, the user typed `echo portrait-hw`. The complete command and `portrait-hw` output appeared, adaptive modality remained `hardware`, PTY retained focus, and no soft keyboard or auxiliary bar appeared.                                                                                                                                                                                                                               |
| J3  | Portrait touch plus keyboard    | User mixes touch focus/selection with keyboard input | Focus remains predictable                    | PASS   | In portrait with the hardware keyboard attached, the user long-pressed `portrait-hw`; xterm selection was exactly `portrait-hw`, selection controls appeared, modality remained `hardware`, and no soft keyboard appeared. The user then tapped the current prompt and typed `echo portrait-mix`; selection cleared, PTY focus returned, and complete command/output appeared without layout changes. Screenshot: `/tmp/dev-anywhere-ipad-j3-portrait-selection-hardware.png`. |

### K. iPad Browser Policy

DEV Anywhere supports Safari on iPad. Identifiable third-party browsers are
stopped before the application shell mounts because their native input UI cannot
be made reliable from webpage code.

| ID  | Item                                  | Agent checks                                                      | User confirms                                                                                 | Status | Evidence                                                                                                             |
| --- | ------------------------------------- | ----------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------- |
| K1  | Safari access                         | Open the local DEV Anywhere URL in real iPad Safari               | The normal application shell loads                                                            | PASS   | Real-device screenshot: `/tmp/dev-anywhere-ipad-safari-allowed-real.png`.                                            |
| K2  | Chrome gate                           | Open the same URL in real iPad Chrome                             | A Safari-required page appears before Relay or PTY initialization                             | PASS   | Chrome `150.0.7871.51` displayed the compatibility page. Screenshot: `/tmp/dev-anywhere-ipad-browser-gate-real.png`. |
| K3  | Safari desktop mode and installed app | Evaluate desktop-site Safari hints and standalone display mode    | Both remain supported                                                                         | PASS   | Unit coverage uses the Safari `MacIntel + maxTouchPoints` environment and standalone mode.                           |
| K4  | Browser-specific PTY cleanup          | Remove Chrome accessory-bar geometry from production PTY behavior | Safari keeps the general keyboard, IME, and scroll fixes without Chrome-specific spacer logic | PASS   | PTY bottom overscroll and accessory classification were removed; full Web tests pass.                                |

## Current Run Notes

- Initial agent observations were made before this checklist existed. They are
  recorded as `OBSERVED`, not `PASS`, unless later retested and explicitly
  accepted.
- Landscape hardware-keyboard items D1-D8 have been accepted.
- Landscape touch-input auxiliary bar C3 has been accepted after its one-row
  layout and keyboard hint were verified on the real iPad.
- Portrait hardware-keyboard regression items J1-J3 have been accepted,
  including rotation, first-key input, and touch-selection/input mixing.
- Portrait E1-E6, touch-selection F1-F2, image preview F3, file download F4,
  and wrapped-path fallback F5 are accepted. Remaining `OBSERVED` items await human
  visual confirmation.
- Font size, Agent management, Voice Pilot, and chat-bubble coverage were added
  explicitly after user review and must not be skipped. Agent permission gates
  G7-G18 are complete; H5-H22 still require the full real-device voice protocol.
- Latest user-away pass refreshed automated landscape evidence for B1-B8 only.
  These remain `OBSERVED` because screenshot/DOM checks cannot confirm visual
  comfort or touch feel.
- Chrome on iPad was investigated after its native password/autofill toolbar
  made the terminal unusable. Tested web input attributes either left the bar
  intact or broke Chinese IME. Chrome is therefore outside the iPad support
  policy; the experimental input matrix and Chrome-specific PTY geometry were
  removed.
- Device-level screenshot capture is available through
  `pymobiledevice3 developer dvt screenshot --userspace`; unlike WebKit
  screenshots, it captures Chrome and system UI. A probe screenshot was saved
  at `/tmp/dev-anywhere-ipad-dvt-screenshot.png`.
- `meta[name=color-scheme]` synchronizes to the effective `light`/`dark` theme;
  Safari runtime verification showed
  `rootColorScheme=light` and `metaColorScheme=light`.
- Extra user-away Safari probes:
  - In `auto` input mode, focused PTY helper textarea reported
    `autocorrect=off`, `autocapitalize=off`, `spellcheck=false`,
    `colorScheme=light`, but `autocomplete=null`.
  - Temporarily switching to `hardware` input mode and reloading made the
    focused PTY helper textarea report `autocomplete=off` and
    `enterkeyhint=send`; input mode was restored to `auto` afterward.
  - Theme matrix was verified on the real iPad Safari target: forced `dark`
    produced `data-theme=dark`, root/meta `color-scheme=dark`; forced `light`
    produced `data-theme=light`, root/meta `color-scheme=light`; restored
    `auto` produced no stored theme and root/meta `color-scheme=light` on the
    current iPad.
- Near the end of the portrait selection pass, all new iPad requests to the
  local Vite origin began failing with Safari `TypeError: Load failed`, including
  a plain `/src/lib/ansi-keys.ts` fetch. The already-loaded app and WebKit target
  remained alive, while the same URL still returned HTTP 200 from the Mac.
  A repeated image preview failed during that interval. Connectivity later
  recovered without an application change, and the same preview then loaded the
  real `1179x2556` image successfully; record the interval as local-network test
  contamination.
