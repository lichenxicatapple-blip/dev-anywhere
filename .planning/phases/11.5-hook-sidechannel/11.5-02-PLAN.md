---
phase: 11.5
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - apps/relay/src/handlers/proxy.ts
  - apps/relay/src/__tests__/unit/proxy-to-client-types.test.ts
autonomous: true
requirements: [PTY-08]
must_haves:
  truths:
    - "relay 将 terminal.hook_event / terminal.permission_request / terminal.status 三类 proxy→client envelope 原样透传"
    - "relay 不透传 terminal.permission_decision（方向相反，属于 client→proxy）"
    - "proxy-to-client-types 单测守住 allowlist 完整性"
  artifacts:
    - path: "apps/relay/src/handlers/proxy.ts"
      provides: "PROXY_TO_CLIENT_TYPES Set 增 3 项"
      contains: "terminal.hook_event"
    - path: "apps/relay/src/__tests__/unit/proxy-to-client-types.test.ts"
      provides: "EXPECTED_TYPES 同步追加 3 项，双向守护"
      contains: "terminal.hook_event"
  key_links:
    - from: "apps/proxy/src/serve/relay-connection.ts (sendEnvelope)"
      to: "apps/web/src/services/relay-client.ts"
      via: "relay 的 proxy.ts handler"
      pattern: "PROXY_TO_CLIENT_TYPES.has(type)"
---

<objective>
在 `apps/relay/src/handlers/proxy.ts` 的 `PROXY_TO_CLIENT_TYPES` Set 追加三个 proxy→client 方向的 envelope type，并同步更新 `apps/relay/src/__tests__/unit/proxy-to-client-types.test.ts` 的 `EXPECTED_TYPES` 数组。memory `project_relay_control_allowlist` 明确：不加就静默丢。

Purpose: relay 代码"不动"的前提是 allowlist 维护完整；本 plan 是本 phase 的 relay 唯一触点。

Output: relay allowlist + 守护测试 PASS。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.5-hook-sidechannel/11.5-CONTEXT.md
@.planning/phases/11.5-hook-sidechannel/11.5-RESEARCH.md
@.planning/phases/11.5-hook-sidechannel/11.5-PATTERNS.md
@.planning/phases/11.5-hook-sidechannel/11.5-01-SUMMARY.md

<interfaces>
From apps/relay/src/handlers/proxy.ts:10-26:
```typescript
export const PROXY_TO_CLIENT_TYPES = new Set([
  "proxy_info", "terminal_title", "terminal_resize", "pty_state",
  "dir_list_response", "dir_create_response", "session_create_response",
  "session_history_messages", "pending_approvals_push", "command_list_push",
  "file_tree_push", "session_history_response", "session_list",
  "session_snapshot", "turn_result",
]);
```

From apps/relay/src/__tests__/unit/proxy-to-client-types.test.ts:14-30:
```typescript
const EXPECTED_TYPES = [
  // 需与 PROXY_TO_CLIENT_TYPES 一一对应
];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 追加 3 个 type 到 relay allowlist + 守护测试</name>
  <files>apps/relay/src/handlers/proxy.ts, apps/relay/src/__tests__/unit/proxy-to-client-types.test.ts</files>
  <read_first>
    - apps/relay/src/handlers/proxy.ts（至少读 1-60 行，理解 allowlist 用法）
    - apps/relay/src/__tests__/unit/proxy-to-client-types.test.ts（整个文件，双向守护模式）
    - .planning/phases/11.5-hook-sidechannel/11.5-RESEARCH.md（Example 4 段）
    - .planning/phases/11.5-hook-sidechannel/11.5-PATTERNS.md（relay/proxy.ts 小节）
  </read_first>
  <behavior>
    - Test: `PROXY_TO_CLIENT_TYPES.has("terminal.hook_event")` 为 true
    - Test: `PROXY_TO_CLIENT_TYPES.has("terminal.permission_request")` 为 true
    - Test: `PROXY_TO_CLIENT_TYPES.has("terminal.status")` 为 true
    - Test: `PROXY_TO_CLIENT_TYPES.has("terminal.permission_decision")` 为 false（方向相反，不在此 allowlist）
    - Test: `EXPECTED_TYPES` 与 `PROXY_TO_CLIENT_TYPES` 双向一致（原有 `it("has no unexpected extra types")` 继续守住）
  </behavior>
  <action>
  **Step 1 — 先改测试（RED）**：`apps/relay/src/__tests__/unit/proxy-to-client-types.test.ts` 的 `EXPECTED_TYPES` 数组末尾追加三个字符串，并新增一个 `it("does NOT include client→proxy terminal.permission_decision")`：

  ```typescript
  const EXPECTED_TYPES = [
    "proxy_info",
    "terminal_title",
    "terminal_resize",
    "dir_list_response",
    "dir_create_response",
    "command_list_push",
    "file_tree_push",
    "session_history_response",
    "pty_state",
    "session_list",
    "session_create_response",
    "session_history_messages",
    "pending_approvals_push",
    "session_snapshot",
    "turn_result",
    "terminal.hook_event",
    "terminal.permission_request",
    "terminal.status",
  ];
  ```

  在该 `describe` 内末尾追加：

  ```typescript
  it("does NOT include client→proxy terminal.permission_decision", () => {
    expect(PROXY_TO_CLIENT_TYPES.has("terminal.permission_decision")).toBe(false);
  });
  ```

  **Step 2 — 运行测试（应 RED）**：
  ```bash
  pnpm --filter @cc-anywhere/relay run test -- --run src/__tests__/unit/proxy-to-client-types.test.ts
  ```

  **Step 3 — 改源码（GREEN）**：`apps/relay/src/handlers/proxy.ts` 的 `PROXY_TO_CLIENT_TYPES` Set 末尾追加：

  ```typescript
  "terminal.hook_event",
  "terminal.permission_request",
  "terminal.status",
  ```

  不加 `terminal.permission_decision`（方向 client→proxy，走 router，不经此 allowlist）。

  **Step 4 — 复跑测试（应 GREEN）**：
  ```bash
  pnpm --filter @cc-anywhere/relay run test -- --run src/__tests__/unit/proxy-to-client-types.test.ts
  ```

  **不要**：
  - 不要删除现有 EXPECTED_TYPES 条目；只做追加
  - 不要改现有测试断言逻辑（双向守护结构保持）
  - 不要把 `terminal.permission_decision` 写进 allowlist
  </action>
  <verify>
    <automated>pnpm --filter @cc-anywhere/relay run test -- --run src/__tests__/unit/proxy-to-client-types.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep `terminal.hook_event` apps/relay/src/handlers/proxy.ts 命中 1 行（在 Set 里）
    - grep `terminal.permission_request` apps/relay/src/handlers/proxy.ts 命中 1 行
    - grep `terminal.status` apps/relay/src/handlers/proxy.ts 命中 1 行
    - grep `terminal.permission_decision` apps/relay/src/handlers/proxy.ts **零命中**
    - EXPECTED_TYPES 数组长度 = PROXY_TO_CLIENT_TYPES Set size（原有 double-guard 测试 PASS）
    - 新增 `it("does NOT include client→proxy terminal.permission_decision")` PASS
  </acceptance_criteria>
  <done>
    allowlist 增 3 项；双向守护测试全绿；permission_decision 显式不在 allowlist。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proxy → relay → client | relay 纯透传，allowlist 是唯一过滤点 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.5-01 | Tampering | relay allowlist | accept | 原有 allowlist 模式 + 单测守护；本 plan 不改变安全模型 |
| — | — | 其它 T-11.5-02..07 | n/a | 本 plan 仅配置，不引入新威胁面 |
</threat_model>

<verification>
`pnpm --filter @cc-anywhere/relay run test -- --run` 全绿；后续 plan 从 proxy 发 envelope 时，relay log 不再出现 "dropped message: unknown type"。
</verification>

<success_criteria>
- allowlist 含 3 个新 type；不含 permission_decision
- 双向守护测试继续通过
</success_criteria>

<output>
完成后生成 `.planning/phases/11.5-hook-sidechannel/11.5-02-SUMMARY.md`，记录 allowlist 最终条目列表。
</output>
