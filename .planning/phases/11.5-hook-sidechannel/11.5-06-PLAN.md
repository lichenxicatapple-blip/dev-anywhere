---
phase: 11.5
plan: 06
type: execute
wave: 3
depends_on: [03]
files_modified:
  - apps/proxy/src/terminal/pty-manager.ts
  - apps/proxy/src/terminal.ts
  - apps/proxy/src/__tests__/unit/pty-manager.test.ts
autonomous: true
requirements: [PTY-08]
must_haves:
  truths:
    - "PtyManagerOptions 新增必填字段 sessionId"
    - "pty.spawn 的 env 注入 CC_ANYWHERE_SESSION_ID = sessionId"
    - "pty.spawn 的 env 注入 CC_ANYWHERE_HOOK_PORT（从 ~/.cc-anywhere/run/hook-port 文件读）；文件不存在时 env 不含此 key（D-15 降级）"
    - "terminal.ts.startPtyManager 传 sessionId: this.sessionId!（createSession 后已赋值）"
    - "每个 claude 子进程看到自己独有的 CC_ANYWHERE_SESSION_ID，hook curl 展开后不串扰"
  artifacts:
    - path: "apps/proxy/src/terminal/pty-manager.ts"
      provides: "PtyManagerOptions.sessionId + env 注入"
      contains: "CC_ANYWHERE_SESSION_ID"
    - path: "apps/proxy/src/terminal.ts"
      provides: "传 sessionId 给 PtyManager"
      contains: "sessionId: this.sessionId"
  key_links:
    - from: "serve.ts writes ~/.cc-anywhere/run/hook-port"
      to: "pty-manager reads same file and injects CC_ANYWHERE_HOOK_PORT"
      via: "file system"
      pattern: "readFileSync(HOOK_PORT_PATH, 'utf8').trim()"
    - from: "pty.spawn env"
      to: "claude → hook curl subprocess"
      via: "POSIX exec env inheritance"
      pattern: "$CC_ANYWHERE_SESSION_ID in curl command"
---

<objective>
把 `CC_ANYWHERE_SESSION_ID` 和 `CC_ANYWHERE_HOOK_PORT` 注入 `pty.spawn` 的 env；这是"同机多 terminal 不串扰"+"hook endpoint 路由"的前提（D-02 + D-03）。

依赖：Plan 03（`HOOK_PORT_PATH` 常量）。

Purpose: Memory 多次强调 `project_osc_title_extraction` 之前因环境变量链断裂丢过事件。这里必须实测注入生效；env 透到 claude → 透到 hook curl 子进程（RESEARCH.md Pitfall 2 的 A1 假设）。

Output: PtyManager 接受 sessionId + hookPort 注入；terminal.ts 传参；pty-manager.test.ts 扩展断言覆盖 env。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.5-hook-sidechannel/11.5-CONTEXT.md
@.planning/phases/11.5-hook-sidechannel/11.5-RESEARCH.md
@.planning/phases/11.5-hook-sidechannel/11.5-PATTERNS.md
@.planning/phases/11.5-hook-sidechannel/11.5-03-SUMMARY.md

<interfaces>
From apps/proxy/src/terminal/pty-manager.ts:18-55 (current):
```typescript
interface PtyManagerOptions {
  claudeArgs: string[];
  tap: (data: string) => void;
  stdin: NodeJS.ReadStream;
  stdout: NodeJS.WriteStream;
  onSessionExit?: (code: number) => void | Promise<void>;
  onResize?: (cols: number, rows: number) => void;
}

// in start():
const child = pty.spawn(claudePath, this.claudeArgs, {
  name, cols, rows, cwd,
  env: process.env as Record<string, string>,
});
```

From apps/proxy/src/common/paths.ts (new, Plan 03):
```typescript
export const HOOK_PORT_PATH: string;
```

From apps/proxy/src/terminal.ts:185-247 (current):
```typescript
private sessionId?: string; // set after createSession()
private startPtyManager(): void {
  this.ptyManager = new PtyManager({ claudeArgs, tap, stdin, stdout, onResize, onSessionExit });
  // ...
}
```

From apps/proxy/src/__tests__/unit/pty-manager.test.ts (existing):
```typescript
// createManager helper + "spawns claude with correct args and terminal dimensions" test
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PtyManager 接受 sessionId + 注入 hook env</name>
  <files>apps/proxy/src/terminal/pty-manager.ts, apps/proxy/src/__tests__/unit/pty-manager.test.ts</files>
  <read_first>
    - apps/proxy/src/terminal/pty-manager.ts（完整读；修改 PtyManagerOptions + start() env）
    - apps/proxy/src/__tests__/unit/pty-manager.test.ts 1-120 行（helper 结构 + 第一个测试参考断言模式）
    - .planning/phases/11.5-hook-sidechannel/11.5-RESEARCH.md（Example 2 + Pitfall 2）
    - .planning/phases/11.5-hook-sidechannel/11.5-PATTERNS.md（pty-manager.ts 小节 + Shared Patterns § Test file layout 最后一个代码块）
    - .planning/phases/11.5-hook-sidechannel/11.5-03-SUMMARY.md（HOOK_PORT_PATH 位置）
  </read_first>
  <behavior>
    - Test: createManager 传 sessionId="test-session" → manager.start() → `pty.spawn` 被调用时 env 包含 `CC_ANYWHERE_SESSION_ID: "test-session"`
    - Test: 当 HOOK_PORT_PATH 存在且内容为 "56789" 时，env 包含 `CC_ANYWHERE_HOOK_PORT: "56789"`
    - Test: 当 HOOK_PORT_PATH 不存在时，env 不包含 `CC_ANYWHERE_HOOK_PORT` key
    - Test: 现有测试（spawns claude with correct args / forwards stdin）保持 PASS，helper 带上 sessionId 默认值不破坏
    - Test: env 仍包含 process.env 的其他 key（不覆盖）
  </behavior>
  <action>
  **Step 1 — 改 `apps/proxy/src/__tests__/unit/pty-manager.test.ts`**：

  a. `createManager` helper（76-92 行）的 overrides 类型增加 `sessionId?: string`，`new PtyManager({...})` 传入 `sessionId: overrides.sessionId ?? "test-session-default"`。

  b. 扩展第一个 it `"spawns claude with correct args and terminal dimensions"` 的断言，让它顺带验证 SESSION_ID env 注入：

  ```typescript
  expect(pty.spawn).toHaveBeenCalledWith(
    expect.stringContaining("claude"),
    ["--help"],
    expect.objectContaining({
      cols: 120, rows: 40,
      env: expect.objectContaining({
        CC_ANYWHERE_SESSION_ID: "test-session-default",
      }),
    }),
  );
  ```

  c. 在 file 底部 `describe("PtyManager", ...)` 内追加新测试块：

  ```typescript
  describe("hook env injection", () => {
    // 每个子测试独立临时 hook-port 文件
    let tmpDir: string;
    let originalHookPort: string | undefined;

    beforeEach(async () => {
      const { mkdtempSync } = await import("node:fs");
      const { tmpdir } = await import("node:os");
      const { join } = await import("node:path");
      tmpDir = mkdtempSync(join(tmpdir(), "pty-manager-hook-"));
      // HOOK_PORT_PATH 是 import const，无法运行时改；改用 env override or vi.mock paths.js 的方式
    });

    // 方案：在 helper 里通过 vi.doMock 覆盖 paths.ts 导出的 HOOK_PORT_PATH
    it("injects CC_ANYWHERE_HOOK_PORT when hook-port file exists", async () => {
      const { writeFileSync } = await import("node:fs");
      const { join } = await import("node:path");
      const hookPortPath = join(tmpDir, "hook-port");
      writeFileSync(hookPortPath, "56789\n", "utf-8");

      vi.resetModules();
      vi.doMock("#src/common/paths.js", async () => {
        const actual = await vi.importActual<typeof import("#src/common/paths.js")>(
          "#src/common/paths.js",
        );
        return { ...actual, HOOK_PORT_PATH: hookPortPath };
      });

      const { PtyManager } = await import("#src/terminal/pty-manager.js");
      const pty = await import("node-pty");
      const stdin = createMockStdin(true);
      const stdout = createMockStdout(120, 40);
      const manager = new PtyManager({
        claudeArgs: [],
        tap: vi.fn(),
        stdin,
        stdout,
        sessionId: "s-hook",
      });
      manager.start();

      expect(pty.spawn).toHaveBeenCalledWith(
        expect.any(String),
        [],
        expect.objectContaining({
          env: expect.objectContaining({
            CC_ANYWHERE_SESSION_ID: "s-hook",
            CC_ANYWHERE_HOOK_PORT: "56789",
          }),
        }),
      );

      vi.doUnmock("#src/common/paths.js");
    });

    it("omits CC_ANYWHERE_HOOK_PORT when file missing", async () => {
      const { join } = await import("node:path");
      const missingPath = join(tmpDir, "does-not-exist");

      vi.resetModules();
      vi.doMock("#src/common/paths.js", async () => {
        const actual = await vi.importActual<typeof import("#src/common/paths.js")>(
          "#src/common/paths.js",
        );
        return { ...actual, HOOK_PORT_PATH: missingPath };
      });

      const { PtyManager } = await import("#src/terminal/pty-manager.js");
      const pty = await import("node-pty");
      const stdin = createMockStdin(true);
      const stdout = createMockStdout(120, 40);
      const manager = new PtyManager({
        claudeArgs: [],
        tap: vi.fn(),
        stdin,
        stdout,
        sessionId: "s-no-hook",
      });
      manager.start();

      const spawnCall = (pty.spawn as unknown as ReturnType<typeof vi.fn>).mock.calls[0];
      const env = spawnCall[2].env as Record<string, string>;
      expect(env.CC_ANYWHERE_SESSION_ID).toBe("s-no-hook");
      expect(env.CC_ANYWHERE_HOOK_PORT).toBeUndefined();

      vi.doUnmock("#src/common/paths.js");
    });
  });
  ```

  跑一遍确认 RED（sessionId 是必填，老代码应该编译错误）：
  ```bash
  pnpm --filter @cc-anywhere/proxy run test -- --run src/__tests__/unit/pty-manager.test.ts
  ```

  **Step 2 — 改 `apps/proxy/src/terminal/pty-manager.ts`**：

  a. 顶部新增 import：
  ```typescript
  import { existsSync, readFileSync } from "node:fs";
  import { HOOK_PORT_PATH } from "../common/paths.js";
  ```

  b. `PtyManagerOptions` 增字段：
  ```typescript
  interface PtyManagerOptions {
    claudeArgs: string[];
    tap: (data: string) => void;
    stdin: NodeJS.ReadStream;
    stdout: NodeJS.WriteStream;
    sessionId: string;  // 必填，用于 CC_ANYWHERE_SESSION_ID env 注入 + hook 路由
    onSessionExit?: (code: number) => void | Promise<void>;
    onResize?: (cols: number, rows: number) => void;
  }
  ```

  c. constructor 存 sessionId：
  ```typescript
  private readonly sessionId: string;
  // ...
  constructor(options: PtyManagerOptions) {
    this.claudeArgs = options.claudeArgs;
    this.tap = options.tap;
    this.stdin = options.stdin;
    this.stdout = options.stdout;
    this.sessionId = options.sessionId;
    this.onSessionExit = options.onSessionExit;
    this.onResize = options.onResize;
  }
  ```

  d. `start()` 里构造 env：把现有 `env: process.env as Record<string, string>` 替换为：
  ```typescript
  const hookPort = readHookPort();
  const child = pty.spawn(claudePath, this.claudeArgs, {
    name: process.env.TERM ?? "xterm-256color",
    cols,
    rows,
    cwd: process.env.INIT_CWD || process.cwd(),
    env: {
      ...process.env,
      CC_ANYWHERE_SESSION_ID: this.sessionId,
      ...(hookPort ? { CC_ANYWHERE_HOOK_PORT: hookPort } : {}),
    } as Record<string, string>,
  });
  ```

  e. 文件底部追加私有辅助（或顶层 helper）：
  ```typescript
  // 读 hook-port 文件；不存在或空串返回 null，上游由 caller 决定是否注入
  function readHookPort(): string | null {
    if (!existsSync(HOOK_PORT_PATH)) return null;
    const content = readFileSync(HOOK_PORT_PATH, "utf-8").trim();
    return content.length > 0 ? content : null;
  }
  ```

  **Step 3 — 跑测试 GREEN**：
  ```bash
  pnpm --filter @cc-anywhere/proxy run test -- --run src/__tests__/unit/pty-manager.test.ts
  ```

  **不要**：
  - 不要把 hookPort 从 env 或者 constructor 传进来——每次 spawn 时读文件最简单、重启 serve 后会换端口也能生效
  - 不要抛异常当 hook-port 文件不存在（D-15 降级要求 silent skip）
  - 不要手写 require —— 用文件顶部 import
  </action>
  <verify>
    <automated>pnpm --filter @cc-anywhere/proxy run test -- --run src/__tests__/unit/pty-manager.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep `sessionId: string` apps/proxy/src/terminal/pty-manager.ts 命中 ≥2 处（interface + private field）
    - grep `CC_ANYWHERE_SESSION_ID` apps/proxy/src/terminal/pty-manager.ts 命中 1 处
    - grep `CC_ANYWHERE_HOOK_PORT` apps/proxy/src/terminal/pty-manager.ts 命中 1 处
    - grep `HOOK_PORT_PATH` apps/proxy/src/terminal/pty-manager.ts 命中 1 处（import 或使用）
    - pty-manager.test.ts 所有 it（原有 + 新增 2 个 hook env 测试）全 PASS
  </acceptance_criteria>
  <done>
    PtyManagerOptions 接受 sessionId；spawn env 注入两个变量；单测覆盖有/无 hook-port 两种场景。
  </done>
</task>

<task type="auto">
  <name>Task 2: terminal.ts 把 sessionId 传给 PtyManager</name>
  <files>apps/proxy/src/terminal.ts</files>
  <read_first>
    - apps/proxy/src/terminal.ts 180-250 行（createSession + startPtyManager 全貌）
    - .planning/phases/11.5-hook-sidechannel/11.5-PATTERNS.md（terminal.ts 小节）
  </read_first>
  <action>
  `apps/proxy/src/terminal.ts` 的 `startPtyManager()` 构造 `new PtyManager({...})` 调用点（第 223 行附近）：在 options 对象里追加 `sessionId: this.sessionId!`（此时 `createSession()` 已 await 完，`this.sessionId` 非空；`!` 断言合法）。

  改后：
  ```typescript
  this.ptyManager = new PtyManager({
    claudeArgs: this.claudeArgs,
    tap: (data) => this.handlePtyData(data),
    stdin: process.stdin,
    stdout: process.stdout,
    sessionId: this.sessionId!,  // createSession 已完成，此处非空
    onResize: ...,
    onSessionExit: ...,
  });
  ```

  不动其他字段顺序，保持 diff 最小。

  **不要**：
  - 不要把 sessionId 的断言改成 optional chaining—如果此时 undefined 说明 createSession 流程错了，应让 PtyManager 抛而非 silently 注 undefined 进 env
  - 不要在构造之外还额外调 setSessionId；PtyManager 的 sessionId 是构造参数，运行期不变
  </action>
  <verify>
    <automated>pnpm --filter @cc-anywhere/proxy run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - grep `sessionId: this.sessionId` apps/proxy/src/terminal.ts 命中 1 行（在 startPtyManager 内）
    - `pnpm --filter @cc-anywhere/proxy run typecheck` 无 error
  </acceptance_criteria>
  <done>
    terminal.ts 通过类型检查；新 sessionId 必填约束被正确满足。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pty.spawn env → claude → hook curl subprocess | POSIX exec env 继承；RESEARCH Pitfall 2 确认 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.5-07 | Information disclosure / routing | env 不继承 → hook 拿不到 sessionId | mitigate | 单测覆盖 env 注入；Plan 09 integration test 实测跨进程继承链（A1 假设验证） |
| — | — | T-11.5-01..06 | n/a | 本 plan 不触发其他威胁面 |
</threat_model>

<verification>
pty-manager.test.ts 绿 + typecheck 绿；后续 Plan 09 integration 实测 env 真的继承到 curl 子进程。
</verification>

<success_criteria>
- PtyManagerOptions 类型新增 sessionId 必填
- pty.spawn env 在有 hook-port 文件时注两个 key，无文件时只注 SESSION_ID
- terminal.ts 构造 PtyManager 传 sessionId
</success_criteria>

<output>
完成后生成 `.planning/phases/11.5-hook-sidechannel/11.5-06-SUMMARY.md`，记录 env 注入策略 + Plan 09 需要实测继承链。
</output>
