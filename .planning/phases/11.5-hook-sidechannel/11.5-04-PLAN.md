---
phase: 11.5
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - apps/proxy/src/serve/hook-endpoint.ts
  - apps/proxy/src/serve/__tests__/hook-endpoint.test.ts
autonomous: true
requirements: [PTY-08]
must_haves:
  truths:
    - "startHookEndpoint 只监听 127.0.0.1 的随机端口，非 loopback 请求不可能到达"
    - "非 PermissionRequest 的 hook 请求 handler 立即 200 + 发 envelope（`terminal.hook_event` + optional `terminal.status`），<50ms 返回"
    - "PermissionRequest 挂住 HTTP response 直到 approvalManager.resolve(requestId, decision) 被调用或 request.close 发生"
    - "server.setTimeout(0) / requestTimeout = 0 / keepAliveTimeout = 0 三件套确保 24h 挂住不被 Node 默认 timeout 切断"
    - "request.on('close') 触发 approvalManager.drop(requestId)，不 leak pending"
    - "body JSON.parse 失败不抛；log warn + 200 ok，避免拖慢 claude"
    - "missing sessionId query 返回 400"
  artifacts:
    - path: "apps/proxy/src/serve/hook-endpoint.ts"
      provides: "startHookEndpoint(deps) → { port, close() }"
      contains: "server.setTimeout(0)"
    - path: "apps/proxy/src/serve/__tests__/hook-endpoint.test.ts"
      provides: "真实 http.Server + 真实 HTTP POST 的端到端单测"
      contains: "127.0.0.1"
  key_links:
    - from: "hook curl subprocess"
      to: "hook-endpoint.handleRequest"
      via: "POST /hook?sessionId=...&event=..."
      pattern: "JSON body → buildMessage envelope → relay.sendEnvelope"
---

<objective>
实现本地 HTTP endpoint：127.0.0.1 + random port，处理 Claude Code hook 子进程 POST 过来的事件。非 PermissionRequest 立即 ack + 发 `terminal.hook_event` / `terminal.status` envelope；PermissionRequest 挂住 response，委托给 HookApprovalManager（下一 plan 实现）。包含 derivePhase 纯函数。

依赖：Plan 01（schema）。不依赖 Plan 03（paths/daemon-id）——hook-endpoint 不读路径也不写 settings.json。

Purpose: 整个 hook 侧信道的运行时入口。正确处理长挂 request + 快速 ack 是 PTY-08 能工作的前提；错了 claude 会被拖慢或挂死。

Output: `hook-endpoint.ts` 完整实现 + 真实 HTTP 的单测。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11.5-hook-sidechannel/11.5-CONTEXT.md
@.planning/phases/11.5-hook-sidechannel/11.5-RESEARCH.md
@.planning/phases/11.5-hook-sidechannel/11.5-PATTERNS.md
@.planning/phases/11.5-hook-sidechannel/11.5-01-SUMMARY.md

<interfaces>
From packages/shared (newly added by Plan 01):
```typescript
// discriminated-union members
"terminal.hook_event" | "terminal.permission_request" | "terminal.status"
```

From packages/shared/src/builders/index.ts:
```typescript
export function buildMessage(type, sessionId, timestamp, payload, source): MessageEnvelope
```

From apps/proxy/src/serve/relay-connection.ts (existing):
```typescript
class RelayConnection {
  sendEnvelope(envelope: MessageEnvelope): void;
  // ...
}
```

HookApprovalManager interface (implemented in Plan 05, but contract defined here):
```typescript
interface HookApprovalManagerLike {
  register(requestId: string, sessionId: string, resolve: (decision: "allow" | "deny") => void): void;
  drop(requestId: string, reason: string): void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hook-endpoint + derivePhase + 真实 HTTP 单测</name>
  <files>apps/proxy/src/serve/hook-endpoint.ts, apps/proxy/src/serve/__tests__/hook-endpoint.test.ts</files>
  <read_first>
    - apps/proxy/src/serve/relay-connection.ts（sendEnvelope 签名）
    - apps/proxy/src/serve.ts 44-55 行（buildMessage 用法 + "proxy" source 字符串）
    - .planning/phases/11.5-hook-sidechannel/11.5-RESEARCH.md（Pattern: Hook endpoint 段 — 完整源码 + § HTTP Framework Choice + Pitfall 3/5）
    - .planning/phases/11.5-hook-sidechannel/11.5-CONTEXT.md（D-01/D-02/D-05/D-09/D-10/D-14）
    - .planning/phases/11.5-hook-sidechannel/11.5-PATTERNS.md（hook-endpoint.ts 小节 + status-deriver 小节）
  </read_first>
  <behavior>
    - Test: 启动后返回一个 &gt;0 的 port；bind 127.0.0.1；GET /anything 返回 404
    - Test: POST /other 返回 404
    - Test: POST /hook 不带 sessionId query 返回 400
    - Test: POST /hook?sessionId=s1&event=PreToolUse body `{"hook_event_name":"PreToolUse","tool_name":"Read","tool_input":{"file":"/tmp/x"}}` 返回 200，且 `relay.sendEnvelope` 被调用两次：一次 `terminal.hook_event`（PreToolUse），一次 `terminal.status`（tool_use）
    - Test: `Stop` 事件发 `terminal.hook_event` + `terminal.status` phase=idle
    - Test: `PostToolUse` 事件发 hook_event + status phase=thinking
    - Test: 非 PermissionRequest 的 ack 在 100ms 内完成（用 `performance.now()`）
    - Test: body 非 JSON 时 handler 不抛，仍返 200，relay 不被调用
    - Test: PermissionRequest 事件：调用 approvalManager.register，HTTP response 挂住（不完成）；调用 register 回调 resolver("allow") 后 response 完成，body 是 `{"hookSpecificOutput":{"hookEventName":"PermissionRequest","decision":{"behavior":"allow"}}}`
    - Test: PermissionRequest 同时发了 `terminal.permission_request` envelope，带 nanoid(12) 生成的 requestId，register 的 requestId === envelope.payload.requestId
    - Test: 客户端断开（req.destroy）触发 approvalManager.drop(requestId, "request closed")
    - Test: server.setTimeout / keepAliveTimeout / requestTimeout 在返回时全为 0（用 `server.timeout === 0` 等判断）
    - Test: close() 优雅关闭后再 POST 返回 connection refused 或 error
  </behavior>
  <action>
  **Step 1 — 写测试 `apps/proxy/src/serve/__tests__/hook-endpoint.test.ts`**。用真实 http POST (不 mock http 模块)，mock 只做在 relay 和 approvalManager：

  ```typescript
  import { describe, it, expect, beforeEach, afterEach, vi } from "vitest";
  import { request as httpRequest } from "node:http";
  import { startHookEndpoint } from "../hook-endpoint.js";

  interface MockRelay {
    sendEnvelope: ReturnType<typeof vi.fn>;
  }
  interface MockApprovalManager {
    register: ReturnType<typeof vi.fn>;
    drop: ReturnType<typeof vi.fn>;
  }

  function postRaw(port: number, path: string, body: string): Promise<{ status: number; body: string }> {
    return new Promise((resolve, reject) => {
      const req = httpRequest(
        { hostname: "127.0.0.1", port, path, method: "POST" },
        (res) => {
          let data = "";
          res.on("data", (chunk) => (data += chunk.toString()));
          res.on("end", () => resolve({ status: res.statusCode ?? 0, body: data }));
        },
      );
      req.on("error", reject);
      req.write(body);
      req.end();
    });
  }

  function postAndDestroy(port: number, path: string, body: string): { closedPromise: Promise<void>; abort: () => void } {
    let req: ReturnType<typeof httpRequest>;
    const closedPromise = new Promise<void>((resolve) => {
      req = httpRequest({ hostname: "127.0.0.1", port, path, method: "POST" }, (res) => {
        res.on("close", () => resolve());
      });
      req.on("error", () => resolve());
      req.write(body);
    });
    return { closedPromise, abort: () => req!.destroy() };
  }

  describe("startHookEndpoint", () => {
    let relay: MockRelay;
    let approvalManager: MockApprovalManager;
    let endpoint: { port: number; close: () => Promise<void> };

    beforeEach(async () => {
      relay = { sendEnvelope: vi.fn() };
      approvalManager = { register: vi.fn(), drop: vi.fn() };
      endpoint = await startHookEndpoint({ relay: relay as unknown as never, approvalManager: approvalManager as unknown as never });
    });

    afterEach(async () => {
      await endpoint.close();
    });

    it("listens on 127.0.0.1 with a random port", () => {
      expect(endpoint.port).toBeGreaterThan(0);
    });

    it("rejects GET with 404", async () => {
      const result = await new Promise<number>((resolve, reject) => {
        const req = httpRequest(
          { hostname: "127.0.0.1", port: endpoint.port, path: "/hook", method: "GET" },
          (res) => resolve(res.statusCode ?? 0),
        );
        req.on("error", reject);
        req.end();
      });
      expect(result).toBe(404);
    });

    it("rejects POST on wrong path with 404", async () => {
      const { status } = await postRaw(endpoint.port, "/wrong", "{}");
      expect(status).toBe(404);
    });

    it("returns 400 if sessionId query is missing", async () => {
      const { status } = await postRaw(endpoint.port, "/hook", "{}");
      expect(status).toBe(400);
    });

    it("handles PreToolUse: 200 ack + hook_event + status envelopes", async () => {
      const body = JSON.stringify({
        hook_event_name: "PreToolUse",
        tool_name: "Read",
        tool_input: { file: "/tmp/x" },
      });
      const { status } = await postRaw(endpoint.port, "/hook?sessionId=s1&event=PreToolUse", body);
      expect(status).toBe(200);

      const calls = relay.sendEnvelope.mock.calls.map((c) => c[0]);
      const types = calls.map((env) => env.type);
      expect(types).toContain("terminal.hook_event");
      expect(types).toContain("terminal.status");

      const hookEv = calls.find((env) => env.type === "terminal.hook_event");
      expect(hookEv.sessionId).toBe("s1");
      expect(hookEv.payload.hookEventName).toBe("PreToolUse");
      expect(hookEv.payload.toolName).toBe("Read");
      expect(hookEv.payload.toolInput).toEqual({ file: "/tmp/x" });

      const statusEv = calls.find((env) => env.type === "terminal.status");
      expect(statusEv.payload.phase).toBe("tool_use");
    });

    it("derives phase idle for Stop, thinking for PostToolUse, idle for SessionStart", async () => {
      const cases: Array<[string, string]> = [
        ["Stop", "idle"],
        ["PostToolUse", "thinking"],
        ["SessionStart", "idle"],
        ["UserPromptSubmit", "thinking"],
      ];
      for (const [event, expectedPhase] of cases) {
        relay.sendEnvelope.mockClear();
        await postRaw(
          endpoint.port,
          `/hook?sessionId=s1&event=${event}`,
          JSON.stringify({ hook_event_name: event }),
        );
        const calls = relay.sendEnvelope.mock.calls.map((c) => c[0]);
        const status = calls.find((env) => env.type === "terminal.status");
        expect(status?.payload.phase).toBe(expectedPhase);
      }
    });

    it("returns 200 and skips relay when body is not JSON", async () => {
      const { status } = await postRaw(endpoint.port, "/hook?sessionId=s1", "garbage");
      expect(status).toBe(200);
      expect(relay.sendEnvelope).not.toHaveBeenCalled();
    });

    it("non-PermissionRequest handler returns in under 100ms", async () => {
      const start = performance.now();
      await postRaw(endpoint.port, "/hook?sessionId=s1", JSON.stringify({ hook_event_name: "PreToolUse" }));
      const elapsed = performance.now() - start;
      expect(elapsed).toBeLessThan(100);
    });

    it("PermissionRequest holds response and pushes terminal.permission_request envelope", async () => {
      let resolver: ((decision: "allow" | "deny") => void) | null = null;
      approvalManager.register.mockImplementation((_req, _sess, resolve) => {
        resolver = resolve;
      });

      const responsePromise = postRaw(
        endpoint.port,
        "/hook?sessionId=s1&event=PermissionRequest",
        JSON.stringify({ hook_event_name: "PermissionRequest", tool_name: "Bash", tool_input: { command: "ls" } }),
      );

      // give event loop a tick to register
      await new Promise((r) => setTimeout(r, 30));

      expect(approvalManager.register).toHaveBeenCalledTimes(1);
      const [requestIdArg, sessionIdArg] = approvalManager.register.mock.calls[0];
      expect(sessionIdArg).toBe("s1");
      expect(requestIdArg).toHaveLength(12);

      const pushedEnvelope = relay.sendEnvelope.mock.calls
        .map((c) => c[0])
        .find((env) => env.type === "terminal.permission_request");
      expect(pushedEnvelope).toBeDefined();
      expect(pushedEnvelope.payload.requestId).toBe(requestIdArg);
      expect(pushedEnvelope.payload.toolName).toBe("Bash");

      // 未 resolve 前 response 不应 ready
      expect(resolver).not.toBeNull();
      resolver!("allow");

      const { status, body } = await responsePromise;
      expect(status).toBe(200);
      const parsed = JSON.parse(body);
      expect(parsed.hookSpecificOutput.hookEventName).toBe("PermissionRequest");
      expect(parsed.hookSpecificOutput.decision.behavior).toBe("allow");
    });

    it("request.close triggers approvalManager.drop", async () => {
      approvalManager.register.mockImplementation(() => {});
      const { closedPromise, abort } = postAndDestroy(
        endpoint.port,
        "/hook?sessionId=s1&event=PermissionRequest",
        JSON.stringify({ hook_event_name: "PermissionRequest" }),
      );
      await new Promise((r) => setTimeout(r, 30));
      abort();
      await closedPromise;
      await new Promise((r) => setTimeout(r, 30));
      expect(approvalManager.drop).toHaveBeenCalledTimes(1);
    });

    it("configures all three Node http timeouts to 0", async () => {
      // 通过 endpoint 读取实际 server 实例
      // 检测方式：关掉后观察 server.timeout 等属性；这里用白盒：endpoint 返回 server 句柄
      // 或者在 factory 内部验证通过（见 action 中实现：server 暴露出来）
      const withServer = endpoint as unknown as { _server?: { timeout: number; keepAliveTimeout: number; requestTimeout: number } };
      expect(withServer._server).toBeDefined();
      expect(withServer._server?.timeout).toBe(0);
      expect(withServer._server?.keepAliveTimeout).toBe(0);
      expect(withServer._server?.requestTimeout).toBe(0);
    });
  });
  ```

  跑测试确认 RED。

  **Step 2 — 写 `apps/proxy/src/serve/hook-endpoint.ts`**：

  ```typescript
  import { createServer, type IncomingMessage, type ServerResponse, type Server } from "node:http";
  import { nanoid } from "nanoid";
  import { buildMessage } from "@cc-anywhere/shared";
  import { serviceLogger } from "../common/logger.js";

  // 依赖抽象出来便于测试；生产装配在 serve.ts。
  interface HookApprovalManagerLike {
    register(requestId: string, sessionId: string, resolve: (decision: "allow" | "deny") => void): void;
    drop(requestId: string, reason: string): void;
  }
  interface RelayLike {
    sendEnvelope(envelope: ReturnType<typeof buildMessage>): void;
  }
  interface HookEndpointDeps {
    approvalManager: HookApprovalManagerLike;
    relay: RelayLike;
  }

  // Claude Code hook 事件名 → phase 的纯映射；无对应时返回 null（不广播 status）。
  export type HookPhase = "idle" | "thinking" | "tool_use" | "waiting" | "error";
  export function derivePhase(hookEventName: string): HookPhase | null {
    switch (hookEventName) {
      case "PreToolUse": return "tool_use";
      case "PostToolUse": return "thinking";
      case "Stop": return "idle";
      case "UserPromptSubmit": return "thinking";
      case "SessionStart": return "idle";
      default: return null;
    }
  }

  // 启动本机 hook HTTP endpoint，listen 127.0.0.1 + port=0（random）。
  // 三个 timeout 全 0：PermissionRequest 需要挂住 24h，任何内部默认超时都会打断 claude。
  // 返回 { port, close } 给 serve.ts 做 lifecycle 管理。
  export async function startHookEndpoint(
    deps: HookEndpointDeps,
  ): Promise<{ port: number; close: () => Promise<void> } & { _server: Server }> {
    const server = createServer((req, res) => handleRequest(req, res, deps));
    server.setTimeout(0);
    server.keepAliveTimeout = 0;
    server.requestTimeout = 0;

    const port = await new Promise<number>((resolve, reject) => {
      server.once("error", reject);
      server.listen(0, "127.0.0.1", () => {
        const addr = server.address();
        if (typeof addr === "object" && addr) resolve(addr.port);
        else reject(new Error("Failed to read hook endpoint port"));
      });
    });

    serviceLogger.info({ port }, "Hook endpoint listening on 127.0.0.1");
    return {
      port,
      _server: server,
      close: () =>
        new Promise<void>((resolve) => {
          server.close(() => resolve());
        }),
    };
  }

  function handleRequest(req: IncomingMessage, res: ServerResponse, deps: HookEndpointDeps): void {
    if (req.method !== "POST" || !req.url?.startsWith("/hook")) {
      res.writeHead(404).end();
      return;
    }
    const url = new URL(req.url, "http://127.0.0.1");
    const sessionId = url.searchParams.get("sessionId");
    if (!sessionId) {
      res.writeHead(400).end("missing sessionId");
      return;
    }

    let body = "";
    req.on("data", (chunk: Buffer) => {
      body += chunk.toString();
    });
    req.on("end", () => {
      let event: Record<string, unknown>;
      try {
        event = JSON.parse(body);
      } catch (err) {
        serviceLogger.warn({ err: String(err), bodyLen: body.length }, "Hook body parse failed");
        res.writeHead(200).end("ok");
        return;
      }

      const hookEventName = typeof event.hook_event_name === "string" ? event.hook_event_name : "";
      const toolName = typeof event.tool_name === "string" ? event.tool_name : undefined;
      const toolInput =
        event.tool_input && typeof event.tool_input === "object"
          ? (event.tool_input as Record<string, unknown>)
          : undefined;
      const transcriptPath = typeof event.transcript_path === "string" ? event.transcript_path : undefined;

      if (hookEventName === "PermissionRequest") {
        handlePermissionRequest(res, req, sessionId, { toolName, toolInput, transcriptPath }, deps);
        return;
      }

      res.writeHead(200).end("ok");
      deps.relay.sendEnvelope(
        buildMessage(
          "terminal.hook_event",
          sessionId,
          Date.now(),
          { sessionId, hookEventName, toolName, toolInput, transcriptPath },
          "proxy",
        ),
      );

      const phase = derivePhase(hookEventName);
      if (phase) {
        deps.relay.sendEnvelope(
          buildMessage(
            "terminal.status",
            sessionId,
            Date.now(),
            { sessionId, phase },
            "proxy",
          ),
        );
      }
    });
  }

  function handlePermissionRequest(
    res: ServerResponse,
    req: IncomingMessage,
    sessionId: string,
    payload: { toolName?: string; toolInput?: Record<string, unknown>; transcriptPath?: string },
    deps: HookEndpointDeps,
  ): void {
    const requestId = nanoid(12);

    deps.approvalManager.register(requestId, sessionId, (decision) => {
      const responseBody = JSON.stringify({
        hookSpecificOutput: {
          hookEventName: "PermissionRequest",
          decision: { behavior: decision },
        },
      });
      res.writeHead(200, { "Content-Type": "application/json" }).end(responseBody);
    });

    req.on("close", () => {
      deps.approvalManager.drop(requestId, "request closed");
    });

    deps.relay.sendEnvelope(
      buildMessage(
        "terminal.permission_request",
        sessionId,
        Date.now(),
        { sessionId, requestId, ...payload },
        "proxy",
      ),
    );
  }
  ```

  **Step 3 — 跑测试 GREEN**：
  ```bash
  pnpm --filter @cc-anywhere/proxy run test -- --run src/serve/__tests__/hook-endpoint.test.ts
  ```

  **不要**：
  - 不要引入 express/fastify（RESEARCH § HTTP Framework Choice 明确用 `node:http`）
  - 不要用 regex 拆 query，`new URL(req.url, "http://127.0.0.1")` 是标准方式
  - 不要把 derivePhase 放进另一个文件——与 hook-endpoint 共用状态机，放同文件导出更清晰
  - 不要给 `data` 事件累积 body 加长度上限——hook 对象正常很小；DoS 风险由 127.0.0.1-only 缓解（同机信任）
  </action>
  <verify>
    <automated>pnpm --filter @cc-anywhere/proxy run test -- --run src/serve/__tests__/hook-endpoint.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep `127.0.0.1` apps/proxy/src/serve/hook-endpoint.ts 命中 ≥2 处（listen + URL parse base）
    - grep `server.setTimeout(0)` apps/proxy/src/serve/hook-endpoint.ts 命中 1 行
    - grep `keepAliveTimeout = 0` apps/proxy/src/serve/hook-endpoint.ts 命中 1 行
    - grep `requestTimeout = 0` apps/proxy/src/serve/hook-endpoint.ts 命中 1 行
    - grep `PermissionRequest` apps/proxy/src/serve/hook-endpoint.ts 命中 ≥1 处（dispatch 分支）
    - grep `hookSpecificOutput` apps/proxy/src/serve/hook-endpoint.ts 命中 1 处（响应 body 构造）
    - grep `derivePhase` apps/proxy/src/serve/hook-endpoint.ts 命中 2+（export + 调用）
    - hook-endpoint.test.ts 所有 it PASS（≥11 个）
  </acceptance_criteria>
  <done>
    endpoint 在 127.0.0.1 上 listen；正常 event 快速 ack + 两条 envelope；PermissionRequest 挂住直到 resolver；timeout 三件套全 0。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| claude CLI subprocess curl → hook-endpoint | 本机 loopback；信任本机进程 |
| hook-endpoint → RelayConnection | 内部；envelope schema 守护 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.5-04 | DoS | hook handler blocks claude | mitigate | 非 PermissionRequest <100ms 返回（单测覆盖）；JSON parse 失败不阻塞，仍返 200 |
| T-11.5-05 | Spoofing | 本机另一进程伪造 hook POST | accept | `server.listen(0, "127.0.0.1")` 只 loopback；同机进程信任是项目基线假设（proxy 本就跑在用户机） |
| T-11.5-01 | DoS | 超大 toolInput | accept | hook 对象正常 <10KB；loopback-only 进一步限制攻击面；不做 body size cap |
| — | — | 其它 T-11.5-02/03/06/07 | n/a | 本 plan 不涉及 settings.json / pending map / env 继承 |
</threat_model>

<verification>
hook-endpoint.test.ts 全绿；`pnpm -r run typecheck` 通过；endpoint close() 不留 socket 泄漏（可用 `lsof -i :<port>` 在手动回归时核对）。
</verification>

<success_criteria>
- startHookEndpoint 返回 `{ port, close, _server }`
- 6 种 hook 事件全支持 + derivePhase 5 分支正确
- PermissionRequest 挂住 + resolver 正确回 hookSpecificOutput
- request.close → approvalManager.drop
- 三个 timeout 都是 0
</success_criteria>

<output>
完成后生成 `.planning/phases/11.5-hook-sidechannel/11.5-04-SUMMARY.md`，记录 endpoint 接口、derivePhase 映射表、timeout 策略。
</output>
