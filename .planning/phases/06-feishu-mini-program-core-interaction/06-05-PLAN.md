---
phase: 06-feishu-mini-program-core-interaction
plan: 05
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - apps/relay/src/router.ts
  - apps/relay/src/handlers/client.ts
  - apps/relay/src/handlers/proxy.ts
  - apps/relay/src/buffer-compressor.ts
  - apps/proxy/src/serve.ts
  - apps/proxy/src/relay-connection.ts
  - apps/proxy/src/session-manager.ts
  - apps/proxy/src/__tests__/session-history.test.ts
autonomous: true
requirements:
  - FEISHU-01
  - FEISHU-03
  - FEISHU-04

must_haves:
  truths:
    - "Relay routes terminal_frame and pty_state envelopes like existing message types"
    - "Relay routes new control messages (dir_list, command_list, file_tree, session_history) bidirectionally"
    - "Permission responses (tool_approve/tool_deny) are forwarded immediately without queuing (D-32)"
    - "proxy_register includes name field, proxy_list_response returns name (D-23)"
    - "Proxy serves terminal frames at throttled intervals when PTY content changes"
    - "Proxy handles dir_list_request from relay and responds with directory entries"
    - "Proxy handles session_history_request by scanning ~/.claude/projects/"
    - "Proxy handles command_list push on session creation"
    - "Proxy handles file_tree push on session creation"
    - "Session history scanning finds Claude Code session files"
  artifacts:
    - path: "apps/relay/src/router.ts"
      provides: "Routes terminal_frame and pty_state through buffer and to clients"
      contains: "terminal_frame"
    - path: "apps/proxy/src/serve.ts"
      provides: "Terminal frame push, dir_list, session_history, command_list, file_tree handlers"
      contains: "terminal_frame"
    - path: "apps/proxy/src/session-manager.ts"
      provides: "Session mode field in list response"
      contains: "mode"
  key_links:
    - from: "apps/relay/src/router.ts"
      to: "apps/relay/src/buffer-compressor.ts"
      via: "terminal_frame triggers compression like pty_snapshot"
      pattern: "terminal_frame"
    - from: "apps/proxy/src/serve.ts"
      to: "apps/proxy/src/relay-connection.ts"
      via: "relayConnection.send for terminal frames"
      pattern: "terminal_frame"
---

<objective>
Wire relay server to route Phase 6 message types, and extend proxy serve.ts to push terminal frames, handle control messages (dir_list, session_history, command_list, file_tree), and send proxy name on register.

Purpose: The relay must route the new terminal_frame/pty_state envelope types and the new control messages. The proxy must serve as the data source for all these features: pushing terminal frames periodically, responding to directory listing requests, pushing command lists on session creation, pushing file tree updates, responding to session history requests, and registering with a name. D-32 requires tool approval responses to bypass any queuing.

Output: Updated relay routing, updated proxy serve.ts with all Phase 6 message handling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-feishu-mini-program-core-interaction/06-CONTEXT.md
@.planning/phases/06-feishu-mini-program-core-interaction/06-RESEARCH.md

<interfaces>
From apps/relay/src/router.ts:
```typescript
export function routeProxyMessage(raw, proxyId, registry, logger): void;
export function routeClientMessage(raw, proxyId, clientWs, registry, logger): void;
const SNAPSHOT_TYPE = "pty_snapshot";
```

From apps/relay/src/handlers/client.ts:
```typescript
// Handles client_register, proxy_list_request, proxy_select, replay_request
```

From apps/relay/src/handlers/proxy.ts (if exists):
```typescript
// Handles proxy_register, proxy_disconnect
```

From apps/proxy/src/serve.ts:
```typescript
export function sendPtySnapshot(sessionId, sessionManager, relayConnection, ...);
export async function startService(): Promise<void>;
function handleClientConnection(socket, sessionManager, workerSockets, clientSockets, relayConnection);
```

From apps/proxy/src/relay-connection.ts:
```typescript
// RelayConnection.send(data: string)
// RelayConnection.onMessage(handler)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update relay to route new envelope types and control messages</name>
  <files>apps/relay/src/router.ts, apps/relay/src/handlers/client.ts, apps/relay/src/handlers/proxy.ts, apps/relay/src/buffer-compressor.ts</files>
  <read_first>
    - apps/relay/src/router.ts
    - apps/relay/src/handlers/client.ts
    - apps/relay/src/handlers/proxy.ts
    - apps/relay/src/buffer-compressor.ts
    - apps/relay/src/registry.ts
    - packages/shared/src/schemas/relay-control.ts
    - packages/shared/src/schemas/envelope.ts
  </read_first>
  <action>
In `apps/relay/src/router.ts`:

1. Add `terminal_frame` to compression trigger types alongside `pty_snapshot`:
```typescript
const SNAPSHOT_TYPES = new Set(["pty_snapshot", "terminal_frame"]);
```
Update the compression check from `if (type === SNAPSHOT_TYPE)` to `if (SNAPSHOT_TYPES.has(type))`.

2. No other changes needed in router.ts -- `routeProxyMessage` already handles any valid MessageEnvelope type generically (validate, buffer, forward). The new `terminal_frame` and `pty_state` types will pass schema validation and route automatically.

In `apps/relay/src/handlers/client.ts`:

1. Add handling for the new control messages from clients:
   - `dir_list_request`: Forward to the bound proxy's WebSocket. The proxyId is in the message. Look up the proxy WS from registry, forward the JSON string.
   - `session_history_request`: Forward to the bound proxy.
   - These are relay-transparent pass-through: relay doesn't process them, just routes.

2. Update `proxy_list_response` construction to include `name` field from proxy registration (D-23). The registry must store proxy name.

In `apps/relay/src/handlers/proxy.ts` (or wherever proxy_register is handled):

1. Update `proxy_register` handling to store `name` from the message.
2. Update `registry` to store proxy name alongside proxyId.

In `apps/relay/src/registry.ts`:

1. Extend proxy entry to include optional `name: string`.
2. Update `getProxies()` or equivalent to return name.

3. **D-32 Priority path**: In the client message handler, when the message is `tool_approve` or `tool_deny` (MessageEnvelope types), forward it to the proxy immediately without any queuing or buffering. This is already the behavior of `routeClientMessage` which sends directly to proxy WS. Verify this path exists and add a comment documenting D-32. If there is any queuing mechanism on the client-to-proxy path, ensure tool_approve/tool_deny bypass it.

For new relay control messages that flow from proxy to client (`dir_list_response`, `command_list_push`, `file_tree_push`, `session_history_response`):
   - These are sent by proxy as relay control messages (not envelopes).
   - The relay's proxy handler needs to recognize these and forward them to the bound client.
   - In the proxy message handler, after parsing as RelayControlMessage, check for these types and forward to bound clients.
  </action>
  <verify>
    <automated>cd /Users/admin/workspace/cc_anywhere && pnpm --filter @cc-anywhere/relay vitest run -x</automated>
  </verify>
  <acceptance_criteria>
    - router.ts contains `terminal_frame` in snapshot types set
    - handlers/client.ts routes `dir_list_request` to proxy
    - handlers/client.ts routes `session_history_request` to proxy
    - registry.ts stores proxy `name` field
    - handlers/proxy.ts stores `name` from proxy_register
    - proxy_list_response construction includes `name` from registry
    - All relay tests pass
  </acceptance_criteria>
  <done>Relay routes all Phase 6 message types. Terminal frames trigger buffer compression. Control messages pass through bidirectionally. Proxy name stored and returned in proxy_list_response.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend serve.ts with terminal frame push, control message handlers, and session history scanning</name>
  <files>apps/proxy/src/serve.ts, apps/proxy/src/relay-connection.ts, apps/proxy/src/session-manager.ts, apps/proxy/src/__tests__/session-history.test.ts</files>
  <read_first>
    - apps/proxy/src/serve.ts
    - apps/proxy/src/relay-connection.ts
    - apps/proxy/src/session-manager.ts
    - apps/proxy/src/terminal-tracker.ts
    - apps/proxy/src/command-discovery.ts
    - apps/proxy/src/file-watcher.ts
    - apps/proxy/src/dir-lister.ts
    - apps/proxy/src/osc-extractor.ts
    - reference/cc-connect/agent/claudecode/session.go
  </read_first>
  <behavior>
    - Test 1: scanSessionHistory returns array of session metadata from ~/.claude/projects/ structure
    - Test 2: scanSessionHistory handles missing ~/.claude/projects/ gracefully
    - Test 3: scanSessionHistory extracts session ID, title, project directory, and updatedAt timestamp
  </behavior>
  <action>
In `apps/proxy/src/serve.ts`:

1. **Terminal frame push (D-08, discretion: throttle + change detection):**
   After a PTY session is registered (`pty_register` handler), set up a periodic timer:
   ```typescript
   const FRAME_PUSH_INTERVAL_MS = 200; // 5fps, per RESEARCH.md recommendation
   ```
   - Every 200ms, check `tracker.hasGridChanged()`
   - If changed, call `tracker.extractGrid()`, build a `terminal_frame` MessageEnvelope, and send via `relayConnection.send()`
   - Store the interval handle per session, clear on deregister/terminate
   - Also integrate OSC extraction: in the PtyManager `onData` callback, pass raw data through `extractOscSignals()`. If result is non-null and state is not `mid_pause`, build and send a `pty_state` envelope via relay.

2. **proxy_register name (D-23):**
   When connecting to relay, include `name` in the proxy_register message. Get name from `process.env.CC_ANYWHERE_PROXY_NAME || os.hostname()`.
   ```typescript
   import { hostname } from "node:os";
   const proxyName = process.env.CC_ANYWHERE_PROXY_NAME || hostname();
   ```

3. **Handle relay control messages from clients (via relay forwarding):**
   In the relay message handler (where relay messages arrive at proxy), add cases for:

   - `dir_list_request`: Call `listDirectory(msg.path)` (from dir-lister.ts). Validate path does not contain `..` and is an absolute path. Respond with `dir_list_response` via relay.

   - `session_history_request`: Call `scanSessionHistory()` (new function, see below). Respond with `session_history_response` via relay.

   - `session_create` with cwd: Already handled in Plan 03 changes to JsonSession.

4. **Command list push on session creation (D-28):**
   After a JSON session is created (in `session_create_request` handler, json mode), call `discoverCommands(workDir)` and send `command_list_push` control message via relay. Set up a 6-hour refresh timer per D-28.

5. **File tree push on session creation (D-29):**
   After a JSON session is created, create a `FileWatcher` for the session's working directory. Call `getInitialTree(2)` and push each directory's entries as `file_tree_push` control messages. Register the `onUpdate` callback to push subsequent `file_tree_push` messages.

6. **D-41 Re-initialization on reconnect:**
   When relay connection re-establishes (relay-connection.ts onReconnect or similar), re-push command list and file tree for all active sessions.

7. **Session history scanning (D-37):**
   Create a new exported function `scanSessionHistory()`:
   ```typescript
   export async function scanSessionHistory(): Promise<Array<{
     id: string;
     title: string;
     projectDir: string;
     updatedAt: number;
   }>>
   ```
   - Scan `~/.claude/projects/` directory for session metadata files
   - Reference cc-connect's `scanSessionMeta` approach from `reference/cc-connect/agent/claudecode/session.go`
   - The exact file format will be discovered during implementation. At minimum, each session has an ID, a project directory, and a timestamp.
   - If the directory structure is not as expected, log a warning and return empty array.

8. **Session list mode field (D-07):**
   In `SessionManager`, ensure `listSessions()` returns the `mode` field ("pty" | "json") for each session. Update `session-manager.ts` if needed to include mode in the returned session objects.

In `apps/proxy/src/relay-connection.ts`:
   - Ensure `onMessage` handler can dispatch relay control messages to the appropriate handler in serve.ts (may need a callback registration mechanism).
  </action>
  <verify>
    <automated>cd /Users/admin/workspace/cc_anywhere && pnpm --filter @cc-anywhere/proxy vitest run -x</automated>
  </verify>
  <acceptance_criteria>
    - serve.ts contains `FRAME_PUSH_INTERVAL_MS` constant
    - serve.ts contains `extractGrid` call
    - serve.ts contains `extractOscSignals` call
    - serve.ts contains `hostname()` or `os.hostname()` for proxy name
    - serve.ts contains `dir_list_request` handling with path validation rejecting `..`
    - serve.ts contains `session_history_request` handling
    - serve.ts contains `command_list_push` sending after session creation
    - serve.ts contains `file_tree_push` sending
    - serve.ts contains `discoverCommands` call
    - session-manager.ts returns `mode` field in session list
    - session-history.test.ts has at least 3 test cases
    - All proxy tests pass
  </acceptance_criteria>
  <done>Serve.ts pushes terminal frames at 200ms intervals with change detection, handles all Phase 6 control messages (dir_list, session_history, command_list, file_tree), sends proxy name on register, pushes initial command/file data on session creation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client->relay->proxy (dir_list_request) | Arbitrary path from untrusted client |
| proxy->relay (terminal_frame) | Terminal content sent over network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Information Disclosure | dir_list_request path traversal | mitigate | Validate path in serve.ts: reject paths containing `..` segments, require absolute paths |
| T-06-14 | Information Disclosure | session_history_response | accept | Session history metadata (titles, project dirs) is intended for the authenticated user. Relay only forwards to bound client. |
| T-06-15 | Denial of Service | terminal_frame push bandwidth | mitigate | 200ms throttle (5fps) + change detection (hasGridChanged) prevents unnecessary sends. Grid size bounded by terminal dimensions. |
| T-06-16 | Repudiation | tool_approve/deny not logged | mitigate | Existing pino logger in serve.ts logs all IPC messages including approval responses |
</threat_model>

<verification>
1. `pnpm --filter @cc-anywhere/relay vitest run -x` passes
2. `pnpm --filter @cc-anywhere/proxy vitest run -x` passes
3. `pnpm -r build` all packages build
</verification>

<success_criteria>
Relay routes terminal_frame and pty_state envelopes plus all new control messages. Proxy pushes terminal frames at 5fps with change detection, handles all control message requests, sends proxy name, pushes command lists and file trees on session creation.
</success_criteria>

<output>
After completion, create `.planning/phases/06-feishu-mini-program-core-interaction/06-05-SUMMARY.md`
</output>
