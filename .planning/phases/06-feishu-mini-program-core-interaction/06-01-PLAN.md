---
phase: 06-feishu-mini-program-core-interaction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/schemas/session.ts
  - packages/shared/src/schemas/envelope.ts
  - packages/shared/src/schemas/relay-control.ts
  - packages/shared/src/schemas/__tests__/envelope.test.ts
  - packages/shared/src/schemas/__tests__/relay-control.test.ts
autonomous: true
requirements:
  - FEISHU-01
  - FEISHU-03
  - FEISHU-04

must_haves:
  truths:
    - "terminal_frame envelope type exists and validates TermSpan arrays"
    - "pty_state envelope type exists and validates semantic state events"
    - "SessionCreatePayload accepts optional cwd field"
    - "proxy_register accepts optional name field"
    - "proxy_list_response includes name in proxy entries"
    - "New relay control messages (dir_list, command_list, file_tree, session_history, pty_state_ctrl) validate correctly"
  artifacts:
    - path: "packages/shared/src/schemas/session.ts"
      provides: "TerminalFramePayloadSchema, PtyStatePayloadSchema, SessionCreatePayload.cwd"
      contains: "TerminalFramePayloadSchema"
    - path: "packages/shared/src/schemas/envelope.ts"
      provides: "terminal_frame and pty_state envelope types"
      contains: "terminal_frame"
    - path: "packages/shared/src/schemas/relay-control.ts"
      provides: "dir_list_request, dir_list_response, command_list_push, file_tree_push, session_history_request, session_history_response, pty_state_ctrl"
      contains: "dir_list_request"
  key_links:
    - from: "packages/shared/src/schemas/envelope.ts"
      to: "packages/shared/src/schemas/session.ts"
      via: "import TerminalFramePayloadSchema, PtyStatePayloadSchema"
      pattern: "TerminalFramePayloadSchema"
---

<objective>
Extend the shared protocol schemas with all new message types required by Phase 6.

Purpose: Phase 6 introduces terminal grid frames, PTY semantic state events, directory listing, command list push, file tree push, session history, and proxy naming. All downstream plans (proxy, relay, mini program) depend on these schema definitions being in place first.

Output: Updated schema files in packages/shared with new Zod schemas, types, and passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-feishu-mini-program-core-interaction/06-CONTEXT.md
@.planning/phases/06-feishu-mini-program-core-interaction/06-RESEARCH.md

<interfaces>
<!-- Existing schemas that will be extended -->

From packages/shared/src/schemas/session.ts:
```typescript
export const SessionCreatePayloadSchema = z.object({
  name: z.string().optional(),
});
export const PtySnapshotPayloadSchema = z.object({
  data: z.string(),
});
```

From packages/shared/src/schemas/envelope.ts:
```typescript
export const MessageEnvelopeSchema = z.discriminatedUnion("type", [
  // 16 existing types: user_input, assistant_message, thinking, tool_use_request,
  // tool_approve, tool_deny, tool_result, session_create, session_list,
  // session_switch, session_terminate, session_status, heartbeat, error,
  // auth, sync_request, sync_response, pty_snapshot
]);
```

From packages/shared/src/schemas/relay-control.ts:
```typescript
export const RelayControlSchema = z.discriminatedUnion("type", [
  // Existing: proxy_register, proxy_register_response, proxy_list_request,
  // proxy_list_response, proxy_select, relay_error, client_register,
  // client_register_response, replay_request, replay_response,
  // gap_unrecoverable, proxy_offline, proxy_disconnect, proxy_online
]);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend session.ts with TerminalFramePayload, PtyStatePayload, and SessionCreate cwd</name>
  <files>packages/shared/src/schemas/session.ts, packages/shared/src/schemas/__tests__/envelope.test.ts</files>
  <read_first>
    - packages/shared/src/schemas/session.ts
    - packages/shared/src/schemas/envelope.ts
    - packages/shared/src/schemas/__tests__/envelope.test.ts
  </read_first>
  <behavior>
    - Test 1: TerminalFramePayloadSchema validates { lines: [[{ text: "hello", fg: "#00FF00", bold: true }]] }
    - Test 2: TerminalFramePayloadSchema validates { lines: [[{ text: " " }]] } (minimal span, no optional fields)
    - Test 3: TerminalFramePayloadSchema rejects { lines: "not an array" }
    - Test 4: PtyStatePayloadSchema validates { state: "working" }
    - Test 5: PtyStatePayloadSchema validates { state: "approval_wait", tool: "Bash" }
    - Test 6: PtyStatePayloadSchema validates { state: "turn_complete", title: "task done" }
    - Test 7: PtyStatePayloadSchema rejects { state: "invalid_state" }
    - Test 8: SessionCreatePayloadSchema accepts { name: "test", cwd: "/home/user/project" }
    - Test 9: SessionCreatePayloadSchema accepts { name: "test" } (cwd optional)
    - Test 10: SessionListPayloadSchema session entries include optional mode field ("pty" | "json")
  </behavior>
  <action>
In `packages/shared/src/schemas/session.ts`:

1. Add `TermSpanSchema`:
```typescript
export const TermSpanSchema = z.object({
  text: z.string(),
  fg: z.string().optional(),
  bg: z.string().optional(),
  bold: z.boolean().optional(),
});
export type TermSpan = z.infer<typeof TermSpanSchema>;
```

2. Add `TerminalFramePayloadSchema`:
```typescript
export const TerminalFramePayloadSchema = z.object({
  lines: z.array(z.array(TermSpanSchema)),
});
export type TerminalFramePayload = z.infer<typeof TerminalFramePayloadSchema>;
```

3. Add `PtyStatePayloadSchema` (D-38):
```typescript
export const PtyStatePayloadSchema = z.object({
  state: z.enum(["working", "turn_complete", "approval_wait"]),
  title: z.string().optional(),
  tool: z.string().optional(),
});
export type PtyStatePayload = z.infer<typeof PtyStatePayloadSchema>;
```

4. Extend `SessionCreatePayloadSchema` with `cwd` (D-22):
```typescript
export const SessionCreatePayloadSchema = z.object({
  name: z.string().optional(),
  cwd: z.string().optional(),
});
```

5. Extend `SessionListPayloadSchema` session entries with `mode` field (D-07):
```typescript
sessions: z.array(
  z.object({
    sessionId: z.string(),
    name: z.string().optional(),
    state: z.enum(sessionStateValues),
    mode: z.enum(["pty", "json"]).optional(),
  }),
),
```

Write tests for all new schemas in the existing test file.
  </action>
  <verify>
    <automated>cd /Users/admin/workspace/cc_anywhere && pnpm --filter @cc-anywhere/shared vitest run src/schemas/__tests__/envelope.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - session.ts contains `export const TermSpanSchema`
    - session.ts contains `export const TerminalFramePayloadSchema`
    - session.ts contains `export const PtyStatePayloadSchema`
    - session.ts contains `cwd: z.string().optional()`
    - session.ts contains `mode: z.enum(["pty", "json"]).optional()`
    - Test file contains tests for TerminalFramePayloadSchema validation
    - Test file contains tests for PtyStatePayloadSchema validation
    - All tests pass
  </acceptance_criteria>
  <done>TerminalFramePayloadSchema, PtyStatePayloadSchema, and extended SessionCreate/SessionList schemas exist with passing tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend envelope.ts with terminal_frame and pty_state types, extend relay-control.ts with Phase 6 control messages</name>
  <files>packages/shared/src/schemas/envelope.ts, packages/shared/src/schemas/relay-control.ts, packages/shared/src/schemas/__tests__/relay-control.test.ts</files>
  <read_first>
    - packages/shared/src/schemas/envelope.ts
    - packages/shared/src/schemas/relay-control.ts
    - packages/shared/src/schemas/__tests__/relay-control.test.ts
    - packages/shared/src/schemas/__tests__/envelope.test.ts
  </read_first>
  <behavior>
    - Test 1: MessageEnvelopeSchema validates { type: "terminal_frame", sessionId, seq, source, payload: { lines: [[{text:"x"}]] } }
    - Test 2: MessageEnvelopeSchema validates { type: "pty_state", sessionId, seq, source, payload: { state: "working" } }
    - Test 3: RelayControlSchema validates { type: "dir_list_request", proxyId: "p1", path: "/home" }
    - Test 4: RelayControlSchema validates { type: "dir_list_response", entries: [{name: "src", isDir: true}], path: "/home" }
    - Test 5: RelayControlSchema validates { type: "command_list_push", commands: [{name: "/compact", description: "...", source: "builtin"}] }
    - Test 6: RelayControlSchema validates { type: "file_tree_push", path: "/home/src", entries: [{name: "index.ts", isDir: false}] }
    - Test 7: RelayControlSchema validates { type: "session_history_request" }
    - Test 8: RelayControlSchema validates { type: "session_history_response", sessions: [{id: "abc", title: "test", projectDir: "/home", updatedAt: 123}] }
    - Test 9: proxy_register now accepts optional name field
    - Test 10: proxy_list_response proxies array includes optional name field
  </behavior>
  <action>
In `packages/shared/src/schemas/envelope.ts`:

1. Import `TerminalFramePayloadSchema` and `PtyStatePayloadSchema` from `./session.js`.

2. Add two new entries to the `MessageEnvelopeSchema` discriminatedUnion array (total becomes 18 types):
```typescript
z.object({
  type: z.literal("terminal_frame"),
  sessionId: z.string(),
  seq: z.number().int().nonnegative(),
  source: z.enum(["proxy", "client", "relay"]),
  payload: TerminalFramePayloadSchema,
}),
z.object({
  type: z.literal("pty_state"),
  sessionId: z.string(),
  seq: z.number().int().nonnegative(),
  source: z.enum(["proxy", "client", "relay"]),
  payload: PtyStatePayloadSchema,
}),
```

In `packages/shared/src/schemas/relay-control.ts`:

1. Extend `proxy_register` with optional `name` field (D-23):
```typescript
z.object({ type: z.literal("proxy_register"), proxyId: z.string().min(1), name: z.string().optional() }),
```

2. Extend `proxy_list_response` proxies to include optional `name` (D-23):
```typescript
z.object({
  type: z.literal("proxy_list_response"),
  proxies: z.array(z.object({ proxyId: z.string(), name: z.string().optional() })),
}),
```

3. Add new control message types (D-21, D-28, D-29, D-37):
```typescript
// D-21: Directory listing
z.object({ type: z.literal("dir_list_request"), proxyId: z.string().min(1), path: z.string() }),
z.object({
  type: z.literal("dir_list_response"),
  entries: z.array(z.object({ name: z.string(), isDir: z.boolean() })),
  path: z.string(),
}),

// D-28: Command list push from proxy
z.object({
  type: z.literal("command_list_push"),
  commands: z.array(z.object({
    name: z.string(),
    description: z.string(),
    argumentHint: z.string().optional(),
    source: z.string(),
  })),
}),

// D-29: File tree push (initial + incremental)
z.object({
  type: z.literal("file_tree_push"),
  path: z.string(),
  entries: z.array(z.object({ name: z.string(), isDir: z.boolean() })),
}),

// D-37: Session history browsing
z.object({ type: z.literal("session_history_request") }),
z.object({
  type: z.literal("session_history_response"),
  sessions: z.array(z.object({
    id: z.string(),
    title: z.string(),
    projectDir: z.string(),
    updatedAt: z.number(),
  })),
}),
```

Write tests for all new schemas in the relay-control test file and envelope test file.
  </action>
  <verify>
    <automated>cd /Users/admin/workspace/cc_anywhere && pnpm --filter @cc-anywhere/shared vitest run -x</automated>
  </verify>
  <acceptance_criteria>
    - envelope.ts contains `z.literal("terminal_frame")`
    - envelope.ts contains `z.literal("pty_state")`
    - relay-control.ts contains `z.literal("dir_list_request")`
    - relay-control.ts contains `z.literal("dir_list_response")`
    - relay-control.ts contains `z.literal("command_list_push")`
    - relay-control.ts contains `z.literal("file_tree_push")`
    - relay-control.ts contains `z.literal("session_history_request")`
    - relay-control.ts contains `z.literal("session_history_response")`
    - relay-control.ts proxy_register object contains `name: z.string().optional()`
    - All tests pass
  </acceptance_criteria>
  <done>MessageEnvelopeSchema has 18 types (terminal_frame + pty_state added), RelayControlSchema has all Phase 6 control messages, all tests green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proxy->relay | Proxy sends new message types (terminal_frame, pty_state) that relay must validate |
| client->relay | Client may send dir_list_request with arbitrary path values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | dir_list_request path field | mitigate | Proxy-side path validation in Plan 04 -- relay only routes, proxy restricts to allowed roots |
| T-06-02 | Tampering | terminal_frame payload size | accept | Terminal grids are bounded by terminal dimensions (cols x rows). No unbounded growth. |
| T-06-03 | Denial of Service | command_list_push array size | accept | Proxy controls list generation. Bounded by actual commands on disk. |
</threat_model>

<verification>
1. `pnpm --filter @cc-anywhere/shared vitest run -x` passes
2. `pnpm --filter @cc-anywhere/shared build` succeeds (types exported correctly)
3. MessageEnvelopeSchema discriminatedUnion has 18 entries
4. RelayControlSchema includes all 7 new control message types
</verification>

<success_criteria>
All new schema types (terminal_frame, pty_state, dir_list_*, command_list_push, file_tree_push, session_history_*, proxy name, cwd) are defined, exported, and tested in the shared package. Downstream packages can import and use these types.
</success_criteria>

<output>
After completion, create `.planning/phases/06-feishu-mini-program-core-interaction/06-01-SUMMARY.md`
</output>
