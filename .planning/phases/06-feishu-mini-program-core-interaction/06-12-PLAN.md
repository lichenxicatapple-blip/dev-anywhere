---
phase: 06-feishu-mini-program-core-interaction
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/feishu/src/services/websocket.ts
  - apps/feishu/src/stores/chat-store.ts
  - apps/feishu/src/pages/spike-picker/index.tsx
  - apps/feishu/src/utils/relative-time.ts
  - apps/feishu/src/services/message-parser.ts
autonomous: true
gap_closure: true
requirements:
  - FEISHU-01

must_haves:
  truths:
    - "WebSocket.connect() closes existing socket before creating a new one, preventing duplicate connections"
    - "UPDATE_TOOL_RESULT matches tool calls by toolIndex, not toolName, so duplicate-name tools get correct results"
    - "spike-picker compiles without reference errors"
    - "routeStreamEvent uses typeof guard instead of unsafe 'as string' assertion"
  artifacts:
    - path: "apps/feishu/src/services/websocket.ts"
      provides: "Duplicate-safe connect method"
      contains: "this.task.close"
    - path: "apps/feishu/src/stores/chat-store.ts"
      provides: "toolIndex-based UPDATE_TOOL_RESULT"
      contains: "toolIndex"
    - path: "apps/feishu/src/pages/spike-picker/index.tsx"
      provides: "Compiling spike page"
      contains: "setInsertedTokens"
    - path: "apps/feishu/src/services/message-parser.ts"
      provides: "Type-safe stream event routing"
      contains: "typeof event.text"
  key_links:
    - from: "apps/feishu/src/stores/chat-store.ts"
      to: "apps/feishu/src/pages/chat/index.tsx"
      via: "chatDispatch UPDATE_TOOL_RESULT with toolIndex"
      pattern: "toolIndex.*number"
---

<objective>
Fix foundational bugs identified by code review (CR-02, CR-03, CR-04) and harden message-parser type safety (WR-04) before wiring relay messages to pages.

Purpose: Plan 13 wires relay messages to stores and dispatches UPDATE_TOOL_RESULT. If that action still matches by toolName, duplicate tool calls (two Read calls, two Bash calls) will silently lose results. WebSocket duplicate connections on foreground resume would cause message duplication. These must be fixed first.

Output: Four files corrected, all compiling cleanly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-feishu-mini-program-core-interaction/06-REVIEW.md
</context>

<interfaces>
From apps/feishu/src/stores/chat-store.ts (current, to be modified):
```typescript
export type ChatAction =
  | { type: "APPEND_ASSISTANT_TEXT"; text: string }
  | { type: "ADD_USER_MESSAGE"; message: ChatMessage }
  | { type: "MARK_TURN_COMPLETE" }
  | { type: "ADD_TOOL_CALL"; messageId: string; toolCall: ToolCallInfo }
  | { type: "UPDATE_TOOL_RESULT"; messageId: string; toolName: string; output: string }
  // ... rest
```

From apps/feishu/src/services/websocket.ts (current, to be modified):
```typescript
connect(url: string): void {
  this.url = url;
  this.closed = false;
  this.doConnect();  // does NOT close existing task
}
```

From apps/feishu/src/services/message-parser.ts (current, to be modified):
```typescript
case "assistant":
  return {
    type: "APPEND_ASSISTANT_TEXT",
    text: (event.text as string) ?? "",  // unsafe assertion
  };
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix WebSocket duplicate connections and message-parser type safety</name>
  <files>apps/feishu/src/services/websocket.ts, apps/feishu/src/services/message-parser.ts, apps/feishu/src/utils/relative-time.ts</files>
  <read_first>
    apps/feishu/src/services/websocket.ts
    apps/feishu/src/services/message-parser.ts
    apps/feishu/src/utils/relative-time.ts
    apps/feishu/src/app.tsx
  </read_first>
  <action>
    **websocket.ts (CR-02 fix):** Modify `connect()` to close any existing socket before creating a new one. Before calling `this.doConnect()`, add:
    1. If `this.task` is not null, call `this.task.close({})` and set `this.task = null`
    2. If `this.reconnectTimer` is not null, call `clearTimeout(this.reconnectTimer)` and set `this.reconnectTimer = null`
    3. Reset `this.connected = false` before doConnect

    Also fix WR-06: Change `send()` to return boolean indicating success. If `!this.task || !this.connected`, log warning and return false instead of silently dropping.

    Also fix WR-07 in app.tsx: Consolidate the two separate `ws.onStatusChange` handlers into a single handler that both dispatches SET_CONNECTED and calls relay.register() on connect. This is a minor cleanup.

    **message-parser.ts (WR-04 fix):** Replace unsafe `(event.text as string) ?? ""` with proper typeof guard: `typeof event.text === "string" ? event.text : ""`

    **relative-time.ts (IN-06 fix):** Fix English pluralization: "1 days ago" should be "1 day ago". Apply same fix for "1 hours ago" -> "1 hour ago" and "1 mins ago" -> "1 min ago".
  </action>
  <verify>
    <automated>cd /Users/admin/workspace/cc_anywhere && npx tsc --noEmit -p apps/feishu/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - websocket.ts connect() contains `this.task.close` before doConnect
    - websocket.ts connect() contains `clearTimeout(this.reconnectTimer)` before doConnect
    - websocket.ts send() returns boolean, contains `console.warn` for dropped messages
    - message-parser.ts contains `typeof event.text === "string"` instead of `as string`
    - relative-time.ts handles singular forms ("1 day ago", "1 hour ago", "1 min ago")
    - app.tsx has exactly one onStatusChange handler that dispatches SET_CONNECTED and calls relay.register()
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>WebSocket cannot create duplicate connections on foreground resume. Message parser uses safe type guards. Relative time formats singular correctly. App has single consolidated status handler.</done>
</task>

<task type="auto">
  <name>Task 2: Fix chat-store UPDATE_TOOL_RESULT matching and spike-picker compile error</name>
  <files>apps/feishu/src/stores/chat-store.ts, apps/feishu/src/pages/spike-picker/index.tsx</files>
  <read_first>
    apps/feishu/src/stores/chat-store.ts
    apps/feishu/src/pages/spike-picker/index.tsx
  </read_first>
  <action>
    **chat-store.ts (CR-03 fix):** Change `UPDATE_TOOL_RESULT` action type from `{ messageId: string; toolName: string; output: string }` to `{ messageId: string; toolIndex: number; output: string }`. In the reducer, change the toolCalls.map to match by index (`i === action.toolIndex`) instead of by toolName (`tc.toolName === action.toolName`). This prevents data loss when a message contains multiple tool calls with the same name (e.g., two `Read` calls).

    **spike-picker/index.tsx (CR-04 fix):** Replace `setInsertedPaths` with `setInsertedTokens` on lines 335 and 346. The state variable is declared as `const [insertedTokens, setInsertedTokens] = useState<string[]>([])` on line 250, but the setter is referenced by the wrong name in two places.
  </action>
  <verify>
    <automated>cd /Users/admin/workspace/cc_anywhere && npx tsc --noEmit -p apps/feishu/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - chat-store.ts ChatAction union contains `{ type: "UPDATE_TOOL_RESULT"; messageId: string; toolIndex: number; output: string }`
    - chat-store.ts reducer UPDATE_TOOL_RESULT case uses `i === action.toolIndex` not `tc.toolName === action.toolName`
    - spike-picker/index.tsx line 335 contains `setInsertedTokens` not `setInsertedPaths`
    - spike-picker/index.tsx line 346 contains `setInsertedTokens` not `setInsertedPaths`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>UPDATE_TOOL_RESULT correctly matches tools by index. Spike-picker compiles without reference errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relay->client | Messages from relay parsed in RelayClient, used by stores |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12-01 | Tampering | websocket.ts send() | mitigate | Return false when disconnected instead of silently dropping, callers can detect and handle |
| T-06-12-02 | Info Disclosure | message-parser.ts | mitigate | typeof guard prevents unexpected data types from leaking through as string |
</threat_model>

<verification>
1. `npx tsc --noEmit -p apps/feishu/tsconfig.json` passes with zero errors
2. grep confirms `toolIndex: number` in ChatAction union
3. grep confirms `this.task.close` in websocket.ts connect()
4. grep confirms `typeof event.text` in message-parser.ts
5. grep confirms `setInsertedTokens` (not `setInsertedPaths`) in spike-picker
</verification>

<success_criteria>
- All four code review critical/warning issues addressed (CR-02, CR-03, CR-04, WR-04)
- Additional cleanups: WR-06 (send return value), WR-07 (consolidated handler), IN-06 (relative-time singular)
- TypeScript compiles cleanly
- No behavioral regressions (fixes are localized)
</success_criteria>

<output>
After completion, create `.planning/phases/06-feishu-mini-program-core-interaction/06-12-SUMMARY.md`
</output>
