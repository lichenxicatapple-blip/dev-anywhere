---
phase: 06-feishu-mini-program-core-interaction
plan: 09
type: execute
wave: 5
depends_on: ["06-08"]
files_modified:
  - apps/feishu/src/components/tool-approval-card/index.tsx
  - apps/feishu/src/components/tool-approval-card/index.css
  - apps/feishu/src/components/slash-command-picker/index.tsx
  - apps/feishu/src/components/slash-command-picker/index.css
  - apps/feishu/src/components/file-path-picker/index.tsx
  - apps/feishu/src/components/file-path-picker/index.css
  - apps/feishu/src/components/quote-preview-bar/index.tsx
  - apps/feishu/src/components/quote-preview-bar/index.css
  - apps/feishu/src/pages/chat/index.tsx
  - apps/feishu/src/pages/chat/index.css
  - apps/feishu/src/components/input-bar/index.tsx
autonomous: false
requirements:
  - FEISHU-01
  - FEISHU-03

must_haves:
  truths:
    - "Tool approval card shows tool name, param preview, and three action buttons (allow, allow-all, deny)"
    - "PTY mode shows tool approval as modal overlay, JSON mode inline in message flow"
    - "Slash command picker triggers on / input, filters in real-time, fills command on select"
    - "File path picker triggers on @ input, browses directories, inserts file path on select"
    - "Message quoting: long press bubble -> quote -> preview bar -> send with XML format"
    - "Settings menu slides up from bottom with permission mode and font size controls"
  artifacts:
    - path: "apps/feishu/src/components/tool-approval-card/index.tsx"
      provides: "Tool approval card with three buttons"
      contains: "ToolApprovalCard"
    - path: "apps/feishu/src/components/slash-command-picker/index.tsx"
      provides: "Slash command autocomplete picker"
      contains: "SlashCommandPicker"
    - path: "apps/feishu/src/components/file-path-picker/index.tsx"
      provides: "File path browser and picker"
      contains: "FilePathPicker"
    - path: "apps/feishu/src/components/quote-preview-bar/index.tsx"
      provides: "Quote preview bar above input"
      contains: "QuotePreviewBar"
  key_links:
    - from: "apps/feishu/src/components/tool-approval-card/index.tsx"
      to: "apps/feishu/src/services/relay-client.ts"
      via: "sendEnvelope for tool_approve/tool_deny"
      pattern: "tool_approve"
    - from: "apps/feishu/src/components/slash-command-picker/index.tsx"
      to: "apps/feishu/src/stores/command-store.ts"
      via: "useCommandState for command list"
      pattern: "useCommandState"
    - from: "apps/feishu/src/components/file-path-picker/index.tsx"
      to: "apps/feishu/src/stores/file-store.ts"
      via: "useFileState for cached directory tree"
      pattern: "useFileState"
---

<objective>
Build the advanced chat page features: tool approval UI, slash command picker, file path picker, message quoting, and settings menu.

Purpose: These features complete the chat page interaction model. Tool approval (D-25/D-26) enables remote tool permission management. Slash/file pickers (D-28/D-29) reduce typing friction. Message quoting (D-40) enables contextual references. The settings menu provides font size and permission mode controls. All have been validated in spike code (spike-picker, spike-chat-json).

Output: All remaining chat page components integrated into the chat page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-feishu-mini-program-core-interaction/06-CONTEXT.md
@.planning/phases/06-feishu-mini-program-core-interaction/06-UI-SPEC.md

<interfaces>
From stores (Plan 06):
```typescript
// chat-store.ts
ToolApprovalRequest = { requestId, toolName, input, status: "pending"|"approved"|"denied" }
// command-store.ts
CommandEntry = { name, description, argumentHint, source }
// file-store.ts
DirEntry = { name, isDir }
tree: Map<string, DirEntry[]>
```

From services (Plan 06):
```typescript
RelayClient.sendEnvelope(envelope): void;
RelayClient.sendControl(msg): void;
```

From components (Plan 08):
```typescript
// input-bar: onSend, disabled, disabledReason, mode, onMenuPress
// chat page: subscribes to relay messages, dispatches to stores
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build tool approval card with three-button UI and PTY modal overlay (D-25, D-26, D-27)</name>
  <files>
    apps/feishu/src/components/tool-approval-card/index.tsx,
    apps/feishu/src/components/tool-approval-card/index.css,
    apps/feishu/src/pages/chat/index.tsx,
    apps/feishu/src/pages/chat/index.css
  </files>
  <read_first>
    - apps/feishu/src/stores/chat-store.ts
    - apps/feishu/src/services/relay-client.ts
    - apps/feishu/src/pages/chat/index.tsx
    - apps/feishu/src/pages/chat/index.css
    - apps/feishu/src/types/envelope.ts
  </read_first>
  <action>
**ToolApprovalCard component** (per UI-SPEC):
- Props: `approval: ToolApprovalRequest`, `onAllow: () => void`, `onAllowAll: () => void`, `onDeny: () => void`, `sessionMode: "pty" | "json"`
- Background: `#FFFBE6`, border: `1px solid #FAAD14`, border-radius: 8px, padding: 12px 16px
- Content:
  - Title: "Need Tool Approval" (16px semi-bold) -- per UI-SPEC copy contract
  - Tool name: bold 14px
  - **Parameter preview with two-layer rendering** (per UI-SPEC):
    - **Known tools (special rendering)**:
      - **Edit tool**: Show file path + diff view. `-` lines: `#FFF1F0` background + `#FF4D4F` text. `+` lines: `#F6FFED` background + `#52C41A` text.
      - **Bash tool**: Terminal style preview. Dark background `#1E1E1E` + green prompt `$` + white command text.
      - **Write tool**: File path + content preview truncated.
    - **Generic fallback** (unknown/MCP/custom tools): Formatted JSON params, 12px monospace `#666666`.
    - Max-height 120px, scrollable. All param text has `selectable` attribute.
  - Three buttons in a row (flex: 1 each, 8px gap):
    - "Allow": `#52C41A` background, `#FFFFFF` text
    - "Allow All": `#1890FF` background, `#FFFFFF` text
    - "Deny": `#FFFFFF` background, `#FF4D4F` text, `1px solid #FF4D4F` border
  - Button height: 36px, border-radius: 4px, font: 14px semi-bold
- After action: buttons become non-interactive (prevent double-tap), card transitions visually:
  - Allowed: collapse to ToolCallCard style (same as D-16 collapsed)
  - Denied: show red "Denied" marker

**JSON mode integration**:
- In ChatBubbleList, when `pendingApprovals` has items with status "pending", render ToolApprovalCard inline in the message flow (at the position of the tool_use_request)
- On "Allow" tap: dispatch UPDATE_APPROVAL_STATUS "approved", send `tool_approve` envelope via relay client
- On "Allow All" tap: dispatch UPDATE_APPROVAL_STATUS "approved", send `tool_approve` envelope with whitelist flag (add `whitelist: true` to payload or send as separate control message for proxy to add to session whitelist D-27)
- On "Deny" tap: dispatch UPDATE_APPROVAL_STATUS "denied", send `tool_deny` envelope

**PTY mode integration** (per UI-SPEC interaction contract):
- When terminal store `ptyState === "approval_wait"`:
  - Render full-page overlay: `position: fixed`, `rgba(0,0,0,0.4)` background, z-index above everything
  - Center ToolApprovalCard on overlay
  - Terminal viewport visible through semi-transparent overlay but not interactive (pointer-events: none on viewport)
  - On action: dismiss overlay, send `tool_approve`/`tool_deny` envelope
- Build the approval data from `pty_state` event: use `approvalTool` from terminal store as toolName. For PTY, parameter preview is limited (no structured params available), show tool name only with note "Parameters available in terminal view".

**Param summary helper**:
Create a helper function `summarizeToolInput(toolName: string, input: Record<string, unknown>): { type: "edit" | "bash" | "write" | "generic"; summary: string; details: unknown }`:
- If toolName matches "Edit" or "edit_file" or similar: extract file path and old_string/new_string for diff
- If toolName matches "Bash" or "bash" or "execute": extract command string
- If toolName matches "Write" or "write_file": extract file path and content preview
- Otherwise: generic JSON stringify
Reference cc-connect's `summarizeInput` pattern (D-30).
  </action>
  <verify>
    <automated>cd /Users/admin/workspace/cc_anywhere && pnpm --filter @cc-anywhere/feishu build:lark 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - components/tool-approval-card/index.tsx exports `ToolApprovalCard`
    - components/tool-approval-card/index.css contains `#FFFBE6` background
    - components/tool-approval-card/index.css contains `#FAAD14` border
    - components/tool-approval-card/index.tsx has three button handlers: onAllow, onAllowAll, onDeny
    - components/tool-approval-card/index.tsx contains special rendering for Edit tool (diff colors `#FFF1F0`, `#F6FFED`)
    - components/tool-approval-card/index.tsx contains special rendering for Bash tool (dark background `#1E1E1E`)
    - pages/chat/index.tsx renders ToolApprovalCard inline for JSON mode
    - pages/chat/index.tsx renders overlay with ToolApprovalCard for PTY mode
    - pages/chat/index.css contains overlay styles `rgba(0,0,0,0.4)`
    - Taro build succeeds
  </acceptance_criteria>
  <done>Tool approval card with three-button UI, special param rendering for Edit/Bash/Write, PTY modal overlay, JSON inline rendering. Approval actions send tool_approve/deny envelopes.</done>
</task>

<task type="auto">
  <name>Task 2: Build slash command picker, file path picker, quote preview, and settings menu (D-28, D-29, D-40)</name>
  <files>
    apps/feishu/src/components/slash-command-picker/index.tsx,
    apps/feishu/src/components/slash-command-picker/index.css,
    apps/feishu/src/components/file-path-picker/index.tsx,
    apps/feishu/src/components/file-path-picker/index.css,
    apps/feishu/src/components/quote-preview-bar/index.tsx,
    apps/feishu/src/components/quote-preview-bar/index.css,
    apps/feishu/src/components/input-bar/index.tsx,
    apps/feishu/src/pages/chat/index.tsx,
    apps/feishu/src/pages/chat/index.css
  </files>
  <read_first>
    - apps/feishu/src/pages/spike-picker/index.tsx
    - apps/feishu/src/pages/spike-picker/index.css
    - apps/feishu/src/stores/command-store.ts
    - apps/feishu/src/stores/file-store.ts
    - apps/feishu/src/stores/chat-store.ts
    - apps/feishu/src/components/input-bar/index.tsx
    - apps/feishu/src/pages/chat/index.tsx
  </read_first>
  <action>
**SlashCommandPicker component** (evolve from spike-picker, D-28):
- Props: `commands: CommandEntry[]`, `filter: string`, `onSelect: (cmd: CommandEntry) => void`, `visible: boolean`
- Appears above input bar as slide-up panel (per UI-SPEC):
  - Background: `#FFFFFF`, top border `1px solid #E8E8E8`, max-height 50vh, scrollable
  - Slide-up animation: 200ms ease-out
- Title bar: "Commands" (13px semi-bold) left, command count (11px `#999999`) right, bottom divider `#F0F0F0`
- Each item (per UI-SPEC):
  - Line 1: command name (14px semi-bold monospace `#333333`) + source tag pill (10px `#999999`, `#F5F5F5` bg)
  - Line 2: description (12px `#999999`, single-line truncate)
  - Padding: 8px 16px, bottom divider `#F5F5F5`
  - Press state: `#F0F7FF` background
- Real-time filter: match by command name only (not description, per UI-SPEC)
- Empty state: "No matching commands" (per UI-SPEC copy)
- On select: fill command text + space into input, show argumentHint as input placeholder, close panel, keep input focused

**FilePathPicker component** (evolve from spike-picker, D-29):
- Props: `tree: Map<string, DirEntry[]>`, `currentPath: string`, `filter: string`, `onSelect: (path: string) => void`, `onNavigate: (path: string) => void`, `visible: boolean`
- Same position and base style as SlashCommandPicker (above input bar)
- Title: "Files" (13px semi-bold)
- Breadcrumb bar: horizontal scrollable, path segments clickable. Current segment `#333333` semi-bold, parent segments `#1890FF` clickable, separator `/` `#CCCCCC`
- Real-time filter: match file names in current directory based on input after last `/`
- Each item: folder icon `[D]` or file icon `[F]` (12px `#999999`) + name (14px). Folders `#333333` with trailing `>`, files `#666666`.
- Click folder: navigate into (call onNavigate, send `dir_list_request` if not cached)
- Click file: insert `@filepath` into input, close panel
- Client-side cache from file store (D-29): read from tree Map, on cache miss send dir_list_request

**QuotePreviewBar component** (D-40):
- Props: `quote: QuotedMessage`, `onCancel: () => void`
- Position: fixed above input bar (between picker panel and input)
- Layout: horizontal, background `#FFFFFF`, top border `1px solid #E8E8E8`, padding 8px 16px
- Left: 4px blue vertical line `#1890FF`
- Content: source prefix ("Claude:" or "You:", 12px semi-bold `#333333`) + quoted text (12px `#999999`, single-line truncate)
- Right: close button "x" (12px `#999999`), on tap calls `onCancel`

**Input bar integration** (update from Plan 08):
- Detect `/` at position 0 of input -> show SlashCommandPicker
- Detect `@` preceded by space or at start -> show FilePathPicker
- Command/file selection closes picker, fills text, keeps focus
- **Atomic delete behavior**:
  - For commands: on backspace, if cursor is right after the command name and space, delete entire command name
  - For file paths: on backspace, if the `@filepath` is partially deleted, remove entire `@filepath`
- Show QuotePreviewBar when `chatState.quotedMessage` is not null
- On send with quote: inject `<quote from="assistant">...</quote>` XML before user text in the message payload

**Settings menu** (slide-up panel from input bar menu button):
- Trigger: menu button (`...`) in input bar
- Panel: bottom slide-up, rounded top corners, semi-transparent overlay
- Animations: bounce slideUp 300ms + overlay fadeIn 200ms, close slideDown 250ms + fadeOut 250ms
- Content:
  - Permission mode: three chips (Default / Auto Accept / Plan), single-select. Sends control message to proxy on change.
  - Font size: A-/A+ buttons. Adjusts `fontSizeIndex` in terminal store. Works for both PTY and JSON modes.

**Long-press quote trigger** (D-40):
- Add long-press handler to UserBubble and AssistantBubble
- On long-press: show context menu with "Quote" option (per UI-SPEC copy: "Quote")
- On "Quote" select: dispatch SET_QUOTE to chat store, show QuotePreviewBar

**QuotedBlock in UserBubble** (D-40):
- When UserBubble has `quotedMessage`, render a QuotedBlock at top:
  - Background: `rgba(255,255,255,0.2)`, border-radius 8px, padding 6px 8px
  - Left: 3px blue line `rgba(255,255,255,0.6)`
  - Source prefix: 11px semi-bold `rgba(255,255,255,0.8)`
  - Quote text: 12px `rgba(255,255,255,0.7)`, max 2 lines, truncate
  - Gap to main text: 8px
  </action>
  <verify>
    <automated>cd /Users/admin/workspace/cc_anywhere && pnpm --filter @cc-anywhere/feishu build:lark 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - components/slash-command-picker/index.tsx exports `SlashCommandPicker`
    - components/slash-command-picker/index.tsx filters by command name
    - components/slash-command-picker/index.css contains `50vh` max-height
    - components/file-path-picker/index.tsx exports `FilePathPicker` with breadcrumb navigation
    - components/file-path-picker/index.tsx handles folder navigation and file selection
    - components/quote-preview-bar/index.tsx exports `QuotePreviewBar`
    - components/quote-preview-bar/index.css contains `#1890FF` vertical line
    - input-bar/index.tsx detects `/` at position 0 and `@` with space prefix
    - pages/chat/index.tsx integrates all picker components
    - pages/chat/index.tsx has settings menu slide-up panel
    - pages/chat/index.tsx has long-press quote handler on bubbles
    - Taro build succeeds
  </acceptance_criteria>
  <done>All advanced chat features complete: slash command picker with real-time filter, file path picker with directory browsing, message quoting with XML injection, settings menu with permission mode and font size, atomic delete for commands/files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify chat features and tool approval</name>
  <what-built>Complete chat page with tool approval, slash/file pickers, message quoting, and settings menu</what-built>
  <action>Human verification checkpoint. Follow the steps in how-to-verify.</action>
  <how-to-verify>
    1. Run `cd /Users/admin/workspace/cc_anywhere && pnpm --filter @cc-anywhere/feishu dev:lark`
    2. Navigate to a chat session
    3. **Tool approval (JSON mode)**: If a tool_use_request arrives, verify approval card shows with tool name, param preview, and three buttons. Tap "Allow" -> card collapses. Verify special rendering for Edit tool (diff colors), Bash tool (dark terminal style).
    4. **Tool approval (PTY mode)**: Verify dark overlay covers page, approval card centered on top, terminal visible through overlay.
    5. **Slash commands**: Type `/` at start of input -> command picker slides up. Type more characters -> list filters. Tap a command -> fills input, shows argument hint as placeholder.
    6. **File paths**: Type `@` -> file picker slides up with breadcrumb navigation. Navigate folders. Tap a file -> `@filepath` inserted.
    7. **Message quoting**: Long-press a bubble -> "Quote" option appears. Tap -> preview bar above input. Send message -> quote appears embedded in user bubble.
    8. **Settings menu**: Tap `...` button -> panel slides up with permission mode chips and font size controls.
    9. Verify all animations are smooth (slide-up, fade, bubble entrance).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user tap -> tool_approve/deny | User grants/denies tool execution permission |
| quote XML injection | Quoted text inserted as XML in message payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-25 | Elevation of Privilege | Tool approval whitelist "Allow All" | mitigate | Whitelist is session-scoped (D-27), auto-cleared on session end. User explicitly chose "Allow All" for this tool type. |
| T-06-26 | Tampering | Quote XML injection | accept | The `<quote>` XML tag is consumed by Claude Code as context, not executed. Claude Code already handles XML input safely. No risk of command injection. |
| T-06-27 | Spoofing | Double-tap on approval buttons | mitigate | Buttons become non-interactive after first tap (status changes from "pending" to "approved"/"denied"). No duplicate sends. |
</threat_model>

<verification>
1. `pnpm --filter @cc-anywhere/feishu build:lark` succeeds
2. Tool approval card renders correctly for Edit/Bash/generic tools
3. Slash picker filters and fills commands
4. File picker navigates directories and inserts paths
5. Quote preview bar shows and sends correctly
6. Settings menu opens and controls work
</verification>

<success_criteria>
All advanced chat features integrated: tool approval with three buttons and special param rendering, slash command picker with real-time filter, file path picker with directory browsing, message quoting with preview bar and XML injection, settings menu with permission mode and font size. Full Phase 6 feature set complete.
</success_criteria>

<output>
After completion, create `.planning/phases/06-feishu-mini-program-core-interaction/06-09-SUMMARY.md`
</output>
