---
phase: 06-feishu-mini-program-core-interaction
plan: 11
type: execute
wave: 7
depends_on: ["06-09"]
files_modified:
  - apps/feishu/src/components/slash-command-picker/index.tsx
  - apps/feishu/src/components/slash-command-picker/index.css
  - apps/feishu/src/components/file-path-picker/index.tsx
  - apps/feishu/src/components/file-path-picker/index.css
  - apps/feishu/src/components/quote-preview-bar/index.tsx
  - apps/feishu/src/components/quote-preview-bar/index.css
  - apps/feishu/src/components/directory-picker/index.tsx
  - apps/feishu/src/components/directory-picker/index.css
  - apps/feishu/src/components/input-bar/index.tsx
  - apps/feishu/src/pages/chat/index.tsx
  - apps/feishu/src/pages/chat/index.css
  - apps/feishu/src/pages/session-list/index.tsx
  - apps/feishu/src/__tests__/directory-picker.test.ts
autonomous: false
requirements:
  - FEISHU-01
  - FEISHU-03

must_haves:
  truths:
    - "Slash command picker triggers on / input, filters in real-time, fills command on select"
    - "File path picker triggers on @ input, browses directories, inserts file path on select"
    - "Message quoting: long press bubble -> quote -> preview bar -> send with XML format"
    - "Settings menu slides up from bottom with permission mode, font size controls, and PC large window toggle"
    - "DirectoryPicker path construction logic is unit tested"
    - "User can create a new JSON session with directory picker from session list page"
    - "Pickers and modals adapt to responsive layout with max-width constraints on desktop"
  artifacts:
    - path: "apps/feishu/src/components/slash-command-picker/index.tsx"
      provides: "Slash command autocomplete picker"
      contains: "SlashCommandPicker"
    - path: "apps/feishu/src/components/file-path-picker/index.tsx"
      provides: "File path browser and picker"
      contains: "FilePathPicker"
    - path: "apps/feishu/src/components/quote-preview-bar/index.tsx"
      provides: "Quote preview bar above input"
      contains: "QuotePreviewBar"
    - path: "apps/feishu/src/components/directory-picker/index.tsx"
      provides: "Directory browser for cwd selection"
      contains: "DirectoryPicker"
    - path: "apps/feishu/src/__tests__/directory-picker.test.ts"
      provides: "Unit tests for path construction logic"
      contains: "buildBreadcrumbSegments"
  key_links:
    - from: "apps/feishu/src/components/slash-command-picker/index.tsx"
      to: "apps/feishu/src/stores/command-store.ts"
      via: "useCommandState for command list"
      pattern: "useCommandState"
    - from: "apps/feishu/src/components/file-path-picker/index.tsx"
      to: "apps/feishu/src/stores/file-store.ts"
      via: "useFileState for cached directory tree"
      pattern: "useFileState"
    - from: "apps/feishu/src/components/directory-picker/index.tsx"
      to: "apps/feishu/src/services/relay-client.ts"
      via: "sendControl for dir_list_request"
      pattern: "dir_list_request"
---

<objective>
Build slash command picker, file path picker, message quoting, settings menu (with PC large window toggle), and directory picker, integrating them into the chat page and session list page. Includes unit-tested DirectoryPicker path logic.

Purpose: These features complete the full Phase 6 interaction model. Slash/file pickers (D-28/D-29) reduce typing friction. Message quoting (D-40) enables contextual references. The settings menu provides font size, permission mode controls, and a PC large window toggle (calls `tt.setWindowSize` on PC to resize the mini program window). DirectoryPicker (D-20) enables cwd selection when creating new sessions.

Output: All remaining chat page components and session creation flow integrated, completing the full Phase 6 feature set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-feishu-mini-program-core-interaction/06-CONTEXT.md
@.planning/phases/06-feishu-mini-program-core-interaction/06-UI-SPEC.md
@.planning/phases/06-feishu-mini-program-core-interaction/06-09-SUMMARY.md

<interfaces>
From stores (Plan 10):
```typescript
// command-store.ts
CommandEntry = { name, description, argumentHint, source }
// file-store.ts
DirEntry = { name, isDir }
tree: Map<string, DirEntry[]>
// chat-store.ts
QuotedMessage = { from: "assistant" | "user", text: string }
```

From services (Plan 06):
```typescript
RelayClient.sendEnvelope(envelope): void;
RelayClient.sendControl(msg): void;
```

From hooks (Plan 10):
```typescript
useScreenSize(): ScreenInfo
// ScreenInfo includes: category, className, windowWidth, windowHeight, deviceType, statusBarHeight, safeArea
```

From components (Plan 08/09):
```typescript
// input-bar: onSend, disabled, disabledReason, mode, onMenuPress
// chat page: subscribes to relay messages, dispatches to stores
// user-bubble, assistant-bubble: render messages
```

From app.css (Plan 10 -- responsive CSS variables):
```css
:root { --picker-max-width: 100%; --input-bar-padding-h: 16px; }
.screen-landscape { --input-bar-padding-h: 24px; }
.screen-desktop { --picker-max-width: 480px; --input-bar-padding-h: 32px; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build DirectoryPicker with tested path logic, slash command picker, and file path picker (D-20, D-28, D-29)</name>
  <files>
    apps/feishu/src/components/directory-picker/index.tsx,
    apps/feishu/src/components/directory-picker/index.css,
    apps/feishu/src/components/slash-command-picker/index.tsx,
    apps/feishu/src/components/slash-command-picker/index.css,
    apps/feishu/src/components/file-path-picker/index.tsx,
    apps/feishu/src/components/file-path-picker/index.css,
    apps/feishu/src/__tests__/directory-picker.test.ts
  </files>
  <read_first>
    - apps/feishu/src/pages/spike-picker/index.tsx
    - apps/feishu/src/pages/spike-picker/index.css
    - apps/feishu/src/stores/command-store.ts
    - apps/feishu/src/stores/file-store.ts
    - apps/feishu/src/services/relay-client.ts
    - apps/feishu/src/types/relay-control.ts
    - apps/feishu/src/hooks/use-screen-size.ts
    - apps/feishu/src/app.css
  </read_first>
  <behavior>
    - Test 1: buildBreadcrumbSegments("/") returns [{ label: "/", path: "/" }]
    - Test 2: buildBreadcrumbSegments("/home/user/project") returns 4 segments with correct paths
    - Test 3: buildBreadcrumbSegments("/home/user/project").last.path === "/home/user/project"
    - Test 4: buildParentPath("/home/user/project") returns "/home/user"
    - Test 5: buildParentPath("/") returns "/"
    - Test 6: joinPath("/home/user", "subdir") returns "/home/user/subdir"
  </behavior>
  <action>
**DirectoryPicker component** (D-20):

First, extract path construction logic into testable pure functions within the component file:
```typescript
export function buildBreadcrumbSegments(path: string): Array<{ label: string; path: string }> {
  if (path === "/") return [{ label: "/", path: "/" }];
  const parts = path.split("/").filter(Boolean);
  return [
    { label: "/", path: "/" },
    ...parts.map((part, i) => ({
      label: part,
      path: "/" + parts.slice(0, i + 1).join("/"),
    })),
  ];
}

export function buildParentPath(path: string): string {
  if (path === "/") return "/";
  const parent = path.substring(0, path.lastIndexOf("/"));
  return parent || "/";
}

export function joinPath(base: string, child: string): string {
  return base === "/" ? `/${child}` : `${base}/${child}`;
}
```

Write tests in `apps/feishu/src/__tests__/directory-picker.test.ts` for these pure functions.

**DirectoryPicker UI**:
- Modal overlay triggered when creating new session from session list page
- Title: "Select Working Directory"
- Breadcrumb navigation: horizontal scrollable path segments built with `buildBreadcrumbSegments`
- Directory list with folder icons `[D]`
- "Select" button: full-width, 44px, `#1890FF` background
- "Return" button: full-width, 44px, white background, `#666666` text
- Fetches directory listing from proxy via `dir_list_request` relay control message
- Initial path: home directory or `/`
- **Responsive**: On desktop, modal uses max-width 480px (`var(--picker-max-width)`) centered on screen

**SlashCommandPicker component** (evolve from spike-picker, D-28):
- Props: `commands: CommandEntry[]`, `filter: string`, `onSelect: (cmd: CommandEntry) => void`, `visible: boolean`
- Appears above input bar as slide-up panel (per UI-SPEC):
  - Background: `#FFFFFF`, top border `1px solid #E8E8E8`, max-height 50vh, scrollable
  - Slide-up animation: 200ms ease-out
- Title bar: "Commands" (13px semi-bold) left, command count (11px `#999999`) right, bottom divider `#F0F0F0`
- Each item (per UI-SPEC):
  - Line 1: command name (14px semi-bold monospace `#333333`) + source tag pill (10px `#999999`, `#F5F5F5` bg)
  - Line 2: description (12px `#999999`, single-line truncate)
  - Padding: 8px 16px, bottom divider `#F5F5F5`
  - Press state: `#F0F7FF` background
- Real-time filter: match by command name only (not description, per UI-SPEC)
- Empty state: "No matching commands" (per UI-SPEC copy)
- On select: fill command text + space into input, show argumentHint as input placeholder, close panel, keep input focused
- **Responsive**: On desktop, picker panel uses max-width `var(--picker-max-width)` (480px), centered horizontally

**FilePathPicker component** (evolve from spike-picker, D-29):
- Props: `tree: Map<string, DirEntry[]>`, `currentPath: string`, `filter: string`, `onSelect: (path: string) => void`, `onNavigate: (path: string) => void`, `visible: boolean`
- Same position and base style as SlashCommandPicker (above input bar)
- Title: "Files" (13px semi-bold)
- Breadcrumb bar: horizontal scrollable, path segments clickable. Current segment `#333333` semi-bold, parent segments `#1890FF` clickable, separator `/` `#CCCCCC`
- Real-time filter: match file names in current directory based on input after last `/`
- Each item: folder icon `[D]` or file icon `[F]` (12px `#999999`) + name (14px). Folders `#333333` with trailing `>`, files `#666666`.
- Click folder: navigate into (call onNavigate, send `dir_list_request` if not cached)
- Click file: insert `@filepath` into input, close panel
- Client-side cache from file store (D-29): read from tree Map, on cache miss send dir_list_request
- **Responsive**: Same max-width constraint as SlashCommandPicker on desktop
  </action>
  <verify>
    <automated>cd /Users/admin/workspace/cc_anywhere && pnpm --filter @cc-anywhere/feishu vitest run src/__tests__/directory-picker.test.ts -x && pnpm --filter @cc-anywhere/feishu build:lark 2>&1 | tail -5</automated>
  </verify>
  <done>DirectoryPicker with tested path construction, SlashCommandPicker with real-time filter, FilePathPicker with directory browsing. All with responsive max-width on desktop.</done>
</task>

<task type="auto">
  <name>Task 2: Build quote preview, settings menu with PC window toggle, integrate pickers into chat page and DirectoryPicker into session list (D-40, D-20)</name>
  <files>
    apps/feishu/src/components/quote-preview-bar/index.tsx,
    apps/feishu/src/components/quote-preview-bar/index.css,
    apps/feishu/src/components/input-bar/index.tsx,
    apps/feishu/src/pages/chat/index.tsx,
    apps/feishu/src/pages/chat/index.css,
    apps/feishu/src/pages/session-list/index.tsx
  </files>
  <read_first>
    - apps/feishu/src/components/input-bar/index.tsx
    - apps/feishu/src/pages/chat/index.tsx
    - apps/feishu/src/pages/chat/index.css
    - apps/feishu/src/pages/session-list/index.tsx
    - apps/feishu/src/stores/chat-store.ts
    - apps/feishu/src/stores/command-store.ts
    - apps/feishu/src/stores/file-store.ts
    - apps/feishu/src/components/slash-command-picker/index.tsx
    - apps/feishu/src/components/file-path-picker/index.tsx
    - apps/feishu/src/components/directory-picker/index.tsx
    - apps/feishu/src/components/user-bubble/index.tsx
    - apps/feishu/src/components/assistant-bubble/index.tsx
    - apps/feishu/src/hooks/use-screen-size.ts
  </read_first>
  <action>
**QuotePreviewBar component** (D-40):
- Props: `quote: QuotedMessage`, `onCancel: () => void`
- Position: fixed above input bar (between picker panel and input)
- Layout: horizontal, background `#FFFFFF`, top border `1px solid #E8E8E8`, padding 8px 16px
- Left: 4px blue vertical line `#1890FF`
- Content: source prefix ("Claude:" or "You:", 12px semi-bold `#333333`) + quoted text (12px `#999999`, single-line truncate)
- Right: close button "x" (12px `#999999`), on tap calls `onCancel`

**Input bar integration** (update from Plan 08):
- Detect `/` at position 0 of input -> show SlashCommandPicker
- Detect `@` preceded by space or at start -> show FilePathPicker
- Command/file selection closes picker, fills text, keeps focus
- **Atomic delete behavior**:
  - For commands: on backspace, if cursor is right after the command name and space, delete entire command name
  - For file paths: on backspace, if the `@filepath` is partially deleted, remove entire `@filepath`
- Show QuotePreviewBar when `chatState.quotedMessage` is not null
- On send with quote: inject `<quote from="assistant">...</quote>` XML before user text in the message payload

**Settings menu** (slide-up panel from input bar menu button):
- Trigger: menu button (`...`) in input bar
- Panel: bottom slide-up, rounded top corners, semi-transparent overlay
- Animations: bounce slideUp 300ms + overlay fadeIn 200ms, close slideDown 250ms + fadeOut 250ms
- Content:
  - Permission mode: three chips (Default / Auto Accept / Plan), single-select. Sends control message to proxy on change.
  - Font size: A-/A+ buttons. Adjusts `fontSizeIndex` in terminal store. Works for both PTY and JSON modes.
  - **PC large window toggle**: Only visible when `deviceType === "pc"` (from `useScreenSize()`). Shows a "PC Large Window" button that calls `tt.setWindowSize({ width: 900, height: 700 })` to resize the mini program to a large workbench size. This is useful when the user's PC defaults to a narrow sidebar (350px). The button text should read "Expand Window" and switch to "Shrink Window" when `windowWidth >= 860` (already in large mode), calling `tt.setWindowSize({ width: 350, height: 600 })` to return to sidebar.
- **Responsive**: On desktop, settings panel uses max-width 480px centered

**Long-press quote trigger** (D-40):
- Add long-press handler to UserBubble and AssistantBubble (update components from Plan 08)
- On long-press: show context menu with "Quote" option (per UI-SPEC copy: "Quote")
- On "Quote" select: dispatch SET_QUOTE to chat store, show QuotePreviewBar

**QuotedBlock in UserBubble** (D-40):
- When UserBubble has `quotedMessage`, render a QuotedBlock at top:
  - Background: `rgba(255,255,255,0.2)`, border-radius 8px, padding 6px 8px
  - Left: 3px blue line `rgba(255,255,255,0.6)`
  - Source prefix: 11px semi-bold `rgba(255,255,255,0.8)`
  - Quote text: 12px `rgba(255,255,255,0.7)`, max 2 lines, truncate
  - Gap to main text: 8px

**Session list DirectoryPicker integration** (D-20):
- Update session-list page: when user taps "+" button, show DirectoryPicker modal instead of directly creating a session
- On directory selection: send `session_create` with `cwd`, navigate to chat on response
- "Cancel" in DirectoryPicker dismisses modal without creating session
  </action>
  <verify>
    <automated>cd /Users/admin/workspace/cc_anywhere && pnpm --filter @cc-anywhere/feishu build:lark 2>&1 | tail -5</automated>
  </verify>
  <done>All advanced chat features and session creation flow complete: slash command picker with real-time filter, file path picker with directory browsing, message quoting with XML injection, settings menu with permission mode, font size, and PC large window toggle, DirectoryPicker in session list, atomic delete for commands/files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify pickers, quoting, settings with PC toggle, directory picker, and responsive behavior</name>
  <what-built>Complete picker suite, message quoting, settings menu with PC window toggle, DirectoryPicker for new sessions, all with responsive layout</what-built>
  <action>Human verification checkpoint. Follow the steps in how-to-verify.</action>
  <how-to-verify>
    1. Run `cd /Users/admin/workspace/cc_anywhere && pnpm --filter @cc-anywhere/feishu dev:lark`
    2. Navigate to a chat session
    3. **Slash commands**: Type `/` at start of input -> command picker slides up. Type more characters -> list filters. Tap a command -> fills input, shows argument hint as placeholder.
    4. **File paths**: Type `@` -> file picker slides up with breadcrumb navigation. Navigate folders. Tap a file -> `@filepath` inserted.
    5. **Message quoting**: Long-press a bubble -> "Quote" option appears. Tap -> preview bar above input. Send message -> quote appears embedded in user bubble.
    6. **Settings menu**: Tap `...` button -> panel slides up with permission mode chips and font size controls.
    7. **PC window toggle**: On Feishu PC client, open settings menu -> verify "Expand Window" / "Shrink Window" button is visible. Tap -> window resizes. On phone, verify this button is NOT shown.
    8. **Directory picker**: Go to session list, tap "+" -> DirectoryPicker modal appears with breadcrumb navigation. Select a directory -> new session created with that cwd.
    9. Verify all animations are smooth (slide-up, fade, bubble entrance).
    10. **Responsive check**: Resize to ~1000px width. Verify picker panels use max-width 480px centered. Verify settings panel centered. Verify DirectoryPicker modal centered with max-width.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| quote XML injection | Quoted text inserted as XML in message payload |
| directory picker -> dir_list_request | User browses directories on proxy machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-26 | Tampering | Quote XML injection | accept | The `<quote>` XML tag is consumed by Claude Code as context, not executed. Claude Code already handles XML input safely. No risk of command injection. |
| T-06-21 | Information Disclosure | Directory listing from proxy | accept | User is the owner of the proxy machine. Directory listing shows their own file system. Proxy-side validation (Plan 04/05) restricts traversal. |
</threat_model>

<verification>
1. `pnpm --filter @cc-anywhere/feishu vitest run src/__tests__/directory-picker.test.ts -x` passes
2. `pnpm --filter @cc-anywhere/feishu build:lark` succeeds
3. Slash picker filters and fills commands
4. File picker navigates directories and inserts paths
5. Quote preview bar shows and sends correctly
6. Settings menu opens and controls work
7. PC window toggle visible only on PC, calls tt.setWindowSize correctly
8. DirectoryPicker works for new session creation
9. Responsive max-width constraints apply on desktop viewport
</verification>

<success_criteria>
All advanced chat features integrated: slash command picker with real-time filter, file path picker with directory browsing, message quoting with preview bar and XML injection, settings menu with permission mode, font size, and PC large window toggle (deviceType-aware). DirectoryPicker with tested path logic for new session creation. All panels adapt to responsive layout with desktop max-width constraints. Full Phase 6 feature set complete.
</success_criteria>

<output>
After completion, create `.planning/phases/06-feishu-mini-program-core-interaction/06-11-SUMMARY.md`
</output>
