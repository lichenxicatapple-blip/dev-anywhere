---
phase: 07-project-scaffold-design-tokens
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - apps/web/src/pages/token-showcase.tsx
  - apps/web/src/app.tsx
autonomous: false
requirements:
  - FRONT-02

must_haves:
  truths:
    - "Token Showcase page displays all surface colors (#1E1E1E, #252526, #2D2D2D, #3C3C3C, #404040) as labeled swatches"
    - "Token Showcase page displays accent color (#00D4AA) and status colors (working, success, warning, error) as labeled swatches"
    - "Token Showcase page shows system font and Sarasa Fixed SC mono font samples"
    - "Token Showcase page shows spacing scale samples"
    - "Token Showcase page renders shadcn/ui Button in all variants (default, destructive, outline, secondary, ghost, link)"
    - "Token Showcase page is responsive across mobile, tablet, and desktop widths"
    - "Human verifies dark theme colors match VS Code aesthetic"
  artifacts:
    - path: "apps/web/src/pages/token-showcase.tsx"
      provides: "Token Showcase page component"
      min_lines: 80
    - path: "apps/web/src/app.tsx"
      provides: "App component rendering TokenShowcase"
      contains: "TokenShowcase"
  key_links:
    - from: "apps/web/src/app.tsx"
      to: "apps/web/src/pages/token-showcase.tsx"
      via: "import and render"
      pattern: "import.*TokenShowcase.*from.*token-showcase"
    - from: "apps/web/src/pages/token-showcase.tsx"
      to: "apps/web/src/components/ui/button.tsx"
      via: "import Button"
      pattern: "import.*Button.*from.*ui/button"
---

<objective>
Create the Token Showcase page that visually validates all design tokens: color palette, typography, spacing scale, border radius, and shadcn/ui Button component variants. This page serves as the first visible output of the v2.0 SPA and as ongoing reference for design token values.

Purpose: Visual verification that all D-01 through D-12 decisions are correctly implemented. The Token Showcase page is the "proof" that the scaffold works end-to-end.
Output: A Token Showcase page rendering in the browser at the dev server URL, ready for human visual approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-project-scaffold-design-tokens/07-CONTEXT.md
@.planning/phases/07-project-scaffold-design-tokens/07-01-SUMMARY.md

@apps/web/src/app.css
@apps/web/src/app.tsx
@apps/web/src/components/ui/button.tsx
@apps/web/src/lib/utils.ts
@apps/web/vite.config.ts

<interfaces>
<!-- From Plan 01 outputs -->

From apps/web/src/app.css (design tokens available as Tailwind utilities):
- bg-background (#1E1E1E), bg-card (#252526), bg-popover (#2D2D2D), bg-input (#3C3C3C)
- text-foreground (#D4D4D4), text-muted-foreground (#808080), text-primary (#00D4AA)
- border-border (#404040)
- bg-primary (#00D4AA), bg-secondary (#2D2D2D), bg-destructive (#F44747)
- Custom CSS vars: --color-status-working (#4FC1FF), --color-status-success (#00D4AA), --color-status-warning (#E8AB5A), --color-status-error (#F44747)
- font-sans (system-ui stack), font-mono (Sarasa Fixed SC stack)
- rounded-sm/md/lg/xl (based on --radius: 0.25rem / 4px)

From apps/web/src/components/ui/button.tsx:
- `Button` component with variants: default, destructive, outline, secondary, ghost, link
- `Button` sizes: default, sm, lg, icon
- Import: `import { Button } from "@/components/ui/button"`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Token Showcase page with all design token sections</name>
  <files>
    apps/web/src/pages/token-showcase.tsx,
    apps/web/src/app.tsx
  </files>
  <read_first>
    apps/web/src/app.css,
    apps/web/src/app.tsx,
    apps/web/src/components/ui/button.tsx,
    apps/web/src/lib/utils.ts,
    .planning/phases/07-project-scaffold-design-tokens/07-CONTEXT.md
  </read_first>
  <action>
Create `apps/web/src/pages/token-showcase.tsx` with the following sections (per D-12). The page itself uses the design tokens for layout and styling (eating our own dog food).

**Page layout:** Single column, max-width container centered, mobile-first responsive (per D-10/D-11). Use `max-w-4xl mx-auto p-3 sm:p-4 lg:p-6` for the outer wrapper.

**Section 1: Color Palette**
Header: "Color Palette"

Sub-section "Surface Hierarchy" -- render 5 color swatches in a responsive grid (`grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 gap-3`):
- Background: `bg-background` -- label "#1E1E1E / background"
- Card: `bg-card` -- label "#252526 / card"
- Popover: `bg-popover` -- label "#2D2D2D / popover"
- Input: `bg-input` -- label "#3C3C3C / input"
- Border: div with `border border-border bg-background` -- label "#404040 / border"

Each swatch: `h-20 rounded-md border border-border` with the appropriate bg class, and a label below in `text-sm text-muted-foreground`.

Sub-section "Accent & Semantic" -- render accent and text swatches:
- Primary: `bg-primary` -- label "#00D4AA / primary"
- Primary Foreground: `bg-primary-foreground text-primary` -- label "#1E1E1E / primary-foreground"
- Destructive: `bg-destructive` -- label "#F44747 / destructive"
- Muted: `bg-muted` -- label "#252526 / muted"
- Muted Foreground text sample: text in `text-muted-foreground` -- label "#808080 / muted-foreground"

Sub-section "Status Colors" -- render 4 status color swatches using inline style `backgroundColor: var(--color-status-*)`:
- Working: `var(--color-status-working)` -- label "#4FC1FF / working"
- Success: `var(--color-status-success)` -- label "#00D4AA / success"
- Warning: `var(--color-status-warning)` -- label "#E8AB5A / warning"
- Error: `var(--color-status-error)` -- label "#F44747 / error"

**Section 2: Typography**
Header: "Typography"

Render text samples at different sizes, each with its Tailwind class labeled:
- `text-3xl font-bold` -- "Heading 3XL (30px) -- font-sans"
- `text-2xl font-semibold` -- "Heading 2XL (24px) -- font-sans"
- `text-xl font-medium` -- "Heading XL (20px) -- font-sans"
- `text-base` -- "Body text (16px) -- The quick brown fox jumps over the lazy dog. -- font-sans"
- `text-sm text-muted-foreground` -- "Small muted text (14px) -- font-sans"

Mono font sample using `font-mono`:
- `text-base font-mono` -- "const greeting = 'Hello'; // Sarasa Fixed SC"
- `text-base font-mono` -- "const msg = '----'; // CJK alignment test"

**Section 3: Spacing Scale**
Header: "Spacing"

Render horizontal bars of increasing width to visualize spacing values. Each bar uses `bg-primary h-3 rounded-sm` with the width class:
- `w-1` (4px), `w-2` (8px), `w-3` (12px), `w-4` (16px), `w-6` (24px), `w-8` (32px), `w-12` (48px), `w-16` (64px)
Each labeled with `text-xs text-muted-foreground`.

**Section 4: Border Radius**
Header: "Border Radius"

Render 4 boxes (`w-16 h-16 bg-card border border-border`) with different radius classes:
- `rounded-sm` -- label "sm (2px)"
- `rounded-md` -- label "md (4px)"
- `rounded-lg` -- label "lg (6px)"
- `rounded-xl` -- label "xl (8px)"

**Section 5: Button Variants**
Header: "Button Variants"

Import `Button` from `@/components/ui/button` and render all variants in a flex wrap layout (`flex flex-wrap gap-3`):
- `<Button>Default</Button>`
- `<Button variant="destructive">Destructive</Button>`
- `<Button variant="outline">Outline</Button>`
- `<Button variant="secondary">Secondary</Button>`
- `<Button variant="ghost">Ghost</Button>`
- `<Button variant="link">Link</Button>`

And all sizes:
- `<Button size="sm">Small</Button>`
- `<Button size="default">Default</Button>`
- `<Button size="lg">Large</Button>`
- `<Button size="icon"><span>+</span></Button>`

**Update apps/web/src/app.tsx** to import and render TokenShowcase:
```typescript
import { TokenShowcase } from "./pages/token-showcase";

export function App() {
  return <TokenShowcase />;
}
```

Each section should be wrapped in a `<section>` with `space-y-4` and sections separated by `space-y-8`. Section headers use `text-xl font-semibold border-b border-border pb-2`.
  </action>
  <verify>
    <automated>cd /Users/admin/workspace/cc_anywhere && pnpm --filter web typecheck && pnpm --filter web build</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/pages/token-showcase.tsx contains `bg-background`
    - apps/web/src/pages/token-showcase.tsx contains `bg-card`
    - apps/web/src/pages/token-showcase.tsx contains `bg-popover`
    - apps/web/src/pages/token-showcase.tsx contains `bg-input`
    - apps/web/src/pages/token-showcase.tsx contains `border-border`
    - apps/web/src/pages/token-showcase.tsx contains `bg-primary`
    - apps/web/src/pages/token-showcase.tsx contains `text-muted-foreground`
    - apps/web/src/pages/token-showcase.tsx contains `--color-status-working`
    - apps/web/src/pages/token-showcase.tsx contains `--color-status-error`
    - apps/web/src/pages/token-showcase.tsx contains `font-mono`
    - apps/web/src/pages/token-showcase.tsx contains `Sarasa Fixed SC`
    - apps/web/src/pages/token-showcase.tsx contains `rounded-sm`
    - apps/web/src/pages/token-showcase.tsx contains `rounded-xl`
    - apps/web/src/pages/token-showcase.tsx contains `import { Button }` or `import { Button`
    - apps/web/src/pages/token-showcase.tsx contains `variant="destructive"`
    - apps/web/src/pages/token-showcase.tsx contains `variant="outline"`
    - apps/web/src/pages/token-showcase.tsx contains `variant="ghost"`
    - apps/web/src/pages/token-showcase.tsx contains `size="sm"`
    - apps/web/src/pages/token-showcase.tsx contains `size="lg"`
    - apps/web/src/app.tsx contains `TokenShowcase`
    - apps/web/src/pages/token-showcase.tsx has at least 80 lines
    - `pnpm --filter web typecheck` exits 0
    - `pnpm --filter web build` exits 0
  </acceptance_criteria>
  <done>
    Token Showcase page renders all design token sections: color palette (surface hierarchy, accent/semantic, status colors), typography (font-sans and font-mono samples with CJK), spacing scale, border radius, and Button variants/sizes. Page is responsive with mobile-first layout.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Token Showcase page</name>
  <files>apps/web/src/pages/token-showcase.tsx</files>
  <action>
    Start the Vite dev server with `pnpm --filter web dev` and present the Token Showcase page to the user for visual approval. The page must be accessible at http://localhost:5173. No code changes in this task -- this is a human verification checkpoint.
  </action>
  <what-built>
    Complete Token Showcase page displaying all design tokens: VS Code-style dark color palette, system + Sarasa Fixed SC fonts, spacing scale, compact border radius, and shadcn/ui Button variants.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `pnpm --filter web dev`
    2. Open `http://localhost:5173` in Chrome
    3. Verify Color Palette section:
       - Surface hierarchy shows 5 distinct dark grays progressing from darkest (#1E1E1E) to lightest (#404040 border)
       - Primary accent is teal-green (#00D4AA)
       - Status colors are visually distinct: blue (working), green (success), amber (warning), red (error)
    4. Verify Typography section:
       - Heading sizes are clearly differentiated (3XL > 2XL > XL > base > sm)
       - Body text uses system font (sans-serif)
       - Mono samples show "Sarasa Fixed SC" font (if relay is running with fonts) -- CJK and ASCII should align in a grid
    5. Verify Spacing section: bars increase in width progressively
    6. Verify Border Radius section: 4 boxes with visually increasing corner rounding (subtle, since base is 4px)
    7. Verify Button Variants section:
       - Default button has teal-green (#00D4AA) background with dark text
       - Destructive has red background
       - Outline has border with transparent background
       - Ghost/Link have no visible border
       - Size progression (sm < default < lg) is clear
    8. Responsive check: Toggle Chrome DevTools device toolbar (Cmd+Shift+M):
       - Mobile 390x844: single column layout, all sections readable
       - Desktop 1280x800: wider layout, color swatches in 5-column grid
    9. Overall aesthetic: does it feel like a VS Code / developer tool, NOT a consumer app?
  </how-to-verify>
  <verify>
    <automated>cd /Users/admin/workspace/cc_anywhere && pnpm --filter web build && test -f apps/web/dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms dark theme colors match VS Code aesthetic
    - Human confirms Button default variant shows #00D4AA accent color
    - Human confirms responsive layout works on mobile and desktop widths
  </acceptance_criteria>
  <done>
    Human has visually approved the Token Showcase page design and color scheme.
  </done>
  <resume-signal>Type "approved" to continue, or describe any color/layout/typography issues that need adjustment</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Token Showcase is a static display page with no user input or network interaction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | I (Information Disclosure) | Token Showcase page | accept | Design tokens are not secrets. They are intentionally visible for development reference. |
</threat_model>

<verification>
1. `pnpm --filter web typecheck` exits 0
2. `pnpm --filter web build` produces dist/ with index.html
3. Token Showcase page shows all 5 sections (colors, typography, spacing, radius, buttons)
4. Page is responsive across mobile/tablet/desktop widths
5. Human approves visual design matches VS Code aesthetic
</verification>

<success_criteria>
- D-12: Token Showcase page displays complete color palette, typography, spacing, radius, and Button variants
- D-10/D-11: Page is responsive with mobile-first layout using Tailwind v4 default breakpoints
- FRONT-02: All design tokens are visually verified as correct
- Human approval obtained for dark theme aesthetic
</success_criteria>

<output>
After completion, create `.planning/phases/07-project-scaffold-design-tokens/07-02-SUMMARY.md`
</output>
