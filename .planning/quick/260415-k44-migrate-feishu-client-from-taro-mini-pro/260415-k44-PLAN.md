---
phase: quick-260415-k44
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260415-k44-migrate-feishu-client-from-taro-mini-pro/MIGRATION-PLAN.md
autonomous: true
must_haves:
  truths:
    - "Migration plan document exists with all 11 required sections"
    - "Every feishu source file is classified (copy/adapt/rewrite/drop)"
    - "Taro-to-Web mapping tables are complete and actionable"
    - "Phased execution plan has clear ordering with rationale"
  artifacts:
    - path: ".planning/quick/260415-k44-migrate-feishu-client-from-taro-mini-pro/MIGRATION-PLAN.md"
      provides: "Complete migration plan document"
      min_lines: 400
---

<objective>
Create a comprehensive migration plan document for migrating CC Anywhere's feishu client from Taro mini program (apps/feishu) to pure React SPA + PWA (apps/web).

Purpose: The user needs a detailed, reviewable plan before any code is written. This document will be the authoritative reference for the actual migration execution.

Output: A single MIGRATION-PLAN.md covering project structure, module inventory, component/API mapping tables, CSS strategy, PWA config, routing, deployment, responsive design, and phased execution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260415-k44-migrate-feishu-client-from-taro-mini-pro/260415-k44-CONTEXT.md
@.planning/quick/260415-k44-migrate-feishu-client-from-taro-mini-pro/260415-k44-RESEARCH.md
@apps/feishu/src/app.tsx
@apps/feishu/src/app.css
@apps/feishu/src/services/websocket.ts
@apps/feishu/src/stores/app-store.ts
@apps/feishu/src/hooks/use-screen-size.ts
@apps/relay/src/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write MIGRATION-PLAN.md with all 11 required sections</name>
  <files>.planning/quick/260415-k44-migrate-feishu-client-from-taro-mini-pro/MIGRATION-PLAN.md</files>
  <action>
Create a comprehensive migration plan document. The document must cover all 11 sections from the task description, using the research findings (260415-k44-RESEARCH.md) and user decisions (260415-k44-CONTEXT.md) as source of truth.

Before writing, read the following files to build a complete understanding:
- ALL .tsx/.ts files in apps/feishu/src/ that have Taro imports (33 imports across ~25 files)
- ALL .css files in apps/feishu/src/ (2784 lines total across 22 files)
- apps/relay/src/server.ts (for static serving integration point)
- apps/feishu/package.json (for dependency inventory)

The document structure MUST include these 11 sections:

**1. New Project Structure (apps/web directory layout)**
- Full directory tree for apps/web/
- Map each directory to its purpose
- Show where shared package is referenced
- Include config files: vite.config.ts, tsconfig.json, index.html

**2. Module Inventory: Copy / Adapt / Rewrite / Drop**
Classify every file in apps/feishu/src/ into exactly one category:
- COPY: Zero Taro dependency, copy verbatim (26 files identified: phase-machine.ts, all utils/*, services/ensure-binding.ts, services/message-parser.ts, services/relay-client.ts, stores/chat-store.ts, stores/command-store.ts, stores/file-store.ts, stores/relay-store.ts, stores/session-store.ts, types/stream-json.ts, components/directory-picker/path-utils.ts, components/modal/index.tsx, components/toast/index.tsx, plus 13 clean test files)
- ADAPT: Has Taro imports but business logic is reusable -- replace Taro components/APIs with HTML/Web equivalents (all component .tsx files with View/Text/ScrollView imports, app.tsx, pages/*, hooks/use-screen-size.ts, services/websocket.ts, stores/app-store.ts, stores/terminal-store.ts)
- REWRITE: Fundamentally different in web context (safe-area-header -- no safe area insets in browser)
- DROP: Taro-only config files (app.config.ts, *.config.ts page configs, taro plugin configs)

Each file must have a one-line note explaining why it's in that category.

**3. Taro Component to HTML Element Mapping Table**
Complete mapping table from RESEARCH.md, enriched with actual usage context from the codebase:
- View -> div
- Text -> span
- ScrollView -> div with overflow-auto class
- RichText -> div with dangerouslySetInnerHTML
- Input -> input (note event shape change: e.detail.value -> e.target.value)
- Image -> img
- Include prop mapping differences (e.g., ScrollView scrollY -> no equivalent needed, just overflow-y-auto)

**4. Taro API to Web API Replacement Table**
Complete mapping from RESEARCH.md with file-level affected locations:
- Taro.navigateTo -> react-router navigate()
- Taro.reLaunch -> navigate(path, { replace: true })
- Taro.navigateBack -> navigate(-1)
- Taro.getStorageSync/setStorageSync/removeStorageSync -> localStorage
- Taro.getCurrentPages -> window.location.hash / useLocation()
- Taro.useDidShow -> visibilitychange event
- useRouter -> useParams + useSearchParams
- usePullDownRefresh -> remove (use refresh button)
- Taro.onWindowResize -> window resize event
- Taro.getSystemInfoSync -> window.innerWidth/Height
- Taro.connectSocket -> new WebSocket (already has H5 codepath)
- Taro.getApp().tt?.setWindowSize -> remove
- CommonEventFunction -> React.TouchEvent / React.MouseEvent

**5. CSS Migration Strategy (750px to Tailwind)**
- Explain the 750px designWidth -> real px conversion (divide by 2)
- List every CSS file with its line count and migration complexity (simple/medium/complex)
- Provide a mapping table for common 750-scale values to Tailwind utilities
- Document the dark mode migration: .chat-page-dark pattern -> Tailwind dark: variant
- Document app.css CSS variables -> Tailwind @theme directive migration
- Note: app.css already uses real px values (no conversion needed), component CSS files use 750-scale

**6. PWA Configuration**
- manifest.json fields (name, icons, display, theme_color, etc.)
- Service worker strategy: generateSW, autoUpdate, CacheFirst for static assets
- Icon requirements: 192x192, 512x512 PNG
- What NOT to cache (WebSocket, API calls)
- Future capabilities: Screen Wake Lock API, Web Speech API

**7. Routing Setup (react-router hash mode)**
- createHashRouter configuration
- Route table mapping existing Taro pages to hash routes
- Preserve existing URL structure: /pages/proxy-select, /pages/session-list, /pages/chat
- App shell component with Outlet for nested routing
- Navigation helper: toWebPath() function for Taro-style URLs

**8. Relay Static Serving Setup**
- Express.static() configuration in apps/relay/src/server.ts
- WEB_DIST_DIR environment variable
- SPA fallback route (though hash routing makes this mostly unnecessary)
- Font serving (already exists, no change needed)
- CORS/security headers if needed

**9. Responsive Design Breakpoints**
- Three-tier breakpoints from user decision: mobile(<768px) / tablet(768-1024px) / desktop(>1024px)
- For each breakpoint, describe layout differences:
  - Mobile: current feishu layout (single column, bottom input bar)
  - Tablet: potential side panel for session list
  - Desktop: wider content area, possible multi-column layout
- Tailwind responsive prefixes: sm:, md:, lg:
- Note: initial migration targets mobile-first (matching current feishu behavior), tablet/desktop are enhancement

**10. Phased Execution Plan**
Break the actual migration into ordered execution phases:
- Phase A: Scaffold apps/web (Vite + React + TS + Tailwind + PWA skeleton, all configs, index.html, main.tsx)
- Phase B: Copy clean files verbatim (all COPY-category files)
- Phase C: Migrate services layer (websocket.ts adapt, app-store.ts adapt, terminal-store.ts adapt)
- Phase D: Migrate components (mechanical View->div, Text->span replacement + CSS->Tailwind)
- Phase E: Migrate pages + app shell (app.tsx, routing, pages, phase-machine nav interface)
- Phase F: CSS to Tailwind conversion (bulk conversion of remaining CSS files)
- Phase G: Tests (adapt test files, ensure vitest works, run Playwright E2E)
- Phase H: Relay integration (static serving config, build pipeline)
- Phase I: PWA polish (icons, manifest tuning, offline shell)

For each phase: estimated file count, estimated effort, dependencies on prior phases.

**11. Deployment Flexibility**
Three deployment scenarios:
- Local development: Vite dev server on :5175, proxy WebSocket to relay on :3100
- Tunnel/ngrok: relay serves built static files + WebSocket on single port
- Cloud: Docker image with relay serving static files, single container

Include Vite proxy config for dev, and relay express.static config for production.

Additionally include these supplementary sections:

**Risks and Pitfalls** (from RESEARCH.md):
- Input event shape (e.detail.value -> e.target.value)
- ScrollView onScroll event differences
- 750-scale CSS values rendering at 2x
- .taro_page CSS class removal
- WebSocket reconnection in PWA background
- Vite HMR WebSocket vs app WebSocket
- RELAY_URL define constant (defineConstants -> Vite define)

**Dependencies to Add / Remove**:
- Complete list of new npm packages with versions
- Complete list of Taro packages to NOT carry over

**Assumptions Log** (from RESEARCH.md, verified):
- Tailwind v4 @theme directive replaces tailwind.config.js
- vite-plugin-pwa v1.2.0 compatible with Vite 6.x
  </action>
  <verify>
    <automated>test -f ".planning/quick/260415-k44-migrate-feishu-client-from-taro-mini-pro/MIGRATION-PLAN.md" && wc -l ".planning/quick/260415-k44-migrate-feishu-client-from-taro-mini-pro/MIGRATION-PLAN.md" | awk '{if ($1 >= 400) print "PASS: " $1 " lines"; else print "FAIL: only " $1 " lines"}'</automated>
  </verify>
  <done>
    - MIGRATION-PLAN.md exists with 400+ lines
    - All 11 required sections are present with actionable content
    - Every feishu/src file is classified in the module inventory
    - Mapping tables are complete with file-level affected locations
    - Phased execution plan has clear ordering and dependencies
    - No section contains placeholder or TODO content
  </done>
</task>

</tasks>

<verification>
- Document covers all 11 sections specified in the task constraints
- Module inventory accounts for every file in apps/feishu/src/
- Mapping tables are derived from actual codebase analysis, not generic
- Phased execution plan is realistic and ordered by dependency
</verification>

<success_criteria>
- MIGRATION-PLAN.md is a complete, reviewable document the user can approve before code execution begins
- Every source file in apps/feishu/src/ has a clear migration disposition
- The phased execution plan could be used to create GSD phase plans for the actual migration
</success_criteria>

<output>
After completion, create `.planning/quick/260415-k44-migrate-feishu-client-from-taro-mini-pro/260415-k44-SUMMARY.md`
</output>
