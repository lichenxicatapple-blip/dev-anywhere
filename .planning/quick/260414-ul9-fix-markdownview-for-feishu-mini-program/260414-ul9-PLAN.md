---
phase: quick
plan: 260414-ul9
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/feishu/src/components/markdown-view/index.tsx
  - apps/feishu/src/components/markdown-view/index.css
autonomous: true
requirements: []
must_haves:
  truths:
    - "Assistant message bubbles render markdown content visibly in both H5 and mini program runtime"
    - "Code blocks display with syntax highlighting colors"
    - "Inline code, headings, lists, tables, blockquotes, bold, italic all render correctly"
  artifacts:
    - path: "apps/feishu/src/components/markdown-view/index.tsx"
      provides: "Taro-compatible MarkdownView using RichText"
      contains: "RichText"
    - path: "apps/feishu/src/components/markdown-view/index.css"
      provides: "Styles compatible with RichText rendering"
  key_links:
    - from: "apps/feishu/src/components/markdown-view/index.tsx"
      to: "@tarojs/components"
      via: "import { View, RichText }"
      pattern: "RichText"
    - from: "apps/feishu/src/components/assistant-bubble/index.tsx"
      to: "apps/feishu/src/components/markdown-view/index.tsx"
      via: "import { MarkdownView }"
      pattern: "MarkdownView"
---

<objective>
Fix MarkdownView component to work in Feishu mini program runtime.

Purpose: The current implementation uses `<div>` and `dangerouslySetInnerHTML` which are React DOM APIs unavailable in mini program runtime, causing assistant message bubbles to render as blank. Replace with Taro's `RichText` component and convert highlight.js class-based styling to inline styles.

Output: Working MarkdownView that renders markdown with syntax-highlighted code blocks in both H5 and mini program environments.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/feishu/src/components/markdown-view/index.tsx
@apps/feishu/src/components/markdown-view/index.css
@apps/feishu/src/components/assistant-bubble/index.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace div+dangerouslySetInnerHTML with RichText and convert hljs to inline styles</name>
  <files>apps/feishu/src/components/markdown-view/index.tsx, apps/feishu/src/components/markdown-view/index.css</files>
  <action>
In `index.tsx`:

1. Replace the `<div>` + `dangerouslySetInnerHTML` render with Taro components:
   - Import `View` and `RichText` from `@tarojs/components`
   - Return `<View className={...}><RichText nodes={html} /></View>`

2. Convert highlight.js class-based coloring to inline styles. RichText in mini programs does NOT support external CSS class selectors on inner HTML nodes. The `renderer.code` function must inject `style` attributes directly into `<span>` tags produced by highlight.js.

   Create a `hljsClassToStyle` map object matching the atom-one-dark theme currently in the CSS file:
   ```
   const HLJS_STYLES: Record<string, string> = {
     "hljs-comment": "color:#5c6370;font-style:italic",
     "hljs-quote": "color:#5c6370;font-style:italic",
     "hljs-doctag": "color:#c678dd",
     "hljs-keyword": "color:#c678dd",
     "hljs-formula": "color:#c678dd",
     "hljs-section": "color:#e06c75",
     "hljs-name": "color:#e06c75",
     "hljs-selector-tag": "color:#e06c75",
     "hljs-deletion": "color:#e06c75",
     "hljs-subst": "color:#e06c75",
     "hljs-literal": "color:#56b6c2",
     "hljs-string": "color:#98c379",
     "hljs-regexp": "color:#98c379",
     "hljs-addition": "color:#98c379",
     "hljs-attribute": "color:#98c379",
     "hljs-attr": "color:#d19a66",
     "hljs-variable": "color:#d19a66",
     "hljs-template-variable": "color:#d19a66",
     "hljs-type": "color:#d19a66",
     "hljs-selector-class": "color:#d19a66",
     "hljs-selector-attr": "color:#d19a66",
     "hljs-selector-pseudo": "color:#d19a66",
     "hljs-number": "color:#d19a66",
     "hljs-symbol": "color:#61afe2",
     "hljs-bullet": "color:#61afe2",
     "hljs-link": "color:#61afe2",
     "hljs-meta": "color:#61afe2",
     "hljs-selector-id": "color:#61afe2",
     "hljs-title": "color:#61afe2",
     "hljs-built_in": "color:#e6c07b",
     "hljs-emphasis": "font-style:italic",
     "hljs-strong": "font-weight:bold",
   };
   ```

3. Write a `inlineHljsStyles(html: string): string` function that replaces `class="hljs-xxx"` with corresponding `style="..."` attributes in the highlighted HTML output. Use a regex replacement: for each `<span class="(hljs-[\w-]+)">`, look up the class in `HLJS_STYLES` and replace with `style="..."`. If a span has multiple classes (e.g., `class="hljs-meta hljs-string"`), handle both by combining styles.

4. Update `renderer.code` to:
   - Apply `inlineHljsStyles()` to the highlighted output
   - Use inline `style` on the `<pre>` and `<code>` tags instead of classes:
     ```
     return `<pre style="background:#282c34;border-radius:6px;padding:10px 12px;overflow-x:auto;margin:8px 0"><code style="font-family:Sarasa Fixed SC,monospace,Menlo,Courier;font-size:13px;line-height:1.5;color:#abb2bf">${inlined}</code></pre>`;
     ```

5. Also override `renderer.codespan` to use inline styles for inline code:
   ```
   renderer.codespan = function({ text }) {
     return `<code style="background:rgba(255,255,255,0.1);padding:1px 4px;border-radius:3px;color:#e06c75;word-break:break-all">${text}</code>`;
   };
   ```

In `index.css`:

6. Remove ALL `.markdown-view .hljs-*` rules (lines 83-118). These are now handled by inline styles and will have no effect inside RichText.

7. Remove `.markdown-view pre code` rules (lines 73-81) since code block styling is now inline.

8. Remove `.markdown-view code` inline code rules (lines 121-129) since inline code styling is now inline.

9. Keep all other structural CSS rules (.markdown-view base styles, p, h1-h4, table, ul/ol, blockquote, hr, strong, a, pre scrollbar rules). These will still work for the H5 runtime where RichText renders as a web component that respects parent CSS. For mini program runtime, the structural layout is handled by RichText's native rendering.

Important: The `MarkdownView` component interface (props: `text`, `className`) and the export name must remain identical so `AssistantBubble` continues to work with zero changes.
  </action>
  <verify>
    <automated>cd /Users/admin/workspace/cc_anywhere && pnpm --filter feishu run build:h5 2>&1 | tail -20</automated>
  </verify>
  <done>
    - MarkdownView uses `View` + `RichText` from `@tarojs/components` instead of `div` + `dangerouslySetInnerHTML`
    - Code blocks have inline style attributes for syntax highlighting colors (no CSS class dependency)
    - Inline code uses inline style attributes
    - H5 build succeeds without errors
    - Component interface unchanged (same props, same export name)
    - CSS file cleaned of now-unused hljs class rules
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>MarkdownView component rewritten to use Taro RichText with inline syntax highlighting styles</what-built>
  <how-to-verify>
    1. Run `pnpm --filter feishu run build:h5 && pnpm --filter feishu run serve:h5`
    2. Open http://localhost:5175/#/pages/chat/index in browser (Chrome DevTools mobile 390x844)
    3. Connect to a proxy session with an active Claude conversation
    4. Verify assistant messages display markdown content (not blank)
    5. Check that code blocks show syntax highlighting colors (keywords purple, strings green, etc.)
    6. Check that inline code renders with reddish color on translucent background
    7. Check headings, lists, bold, italic, blockquotes render correctly
    8. If possible, also test in Feishu developer tools with mini program build
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. RichText renders sanitized HTML nodes from marked output. No user-controlled input reaches the renderer without going through marked's parser.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | T (Tampering) | RichText nodes input | accept | marked output is not user-controlled raw HTML; markdown source is from Claude API responses, not untrusted user input |
</threat_model>

<verification>
- `pnpm --filter feishu run build:h5` succeeds
- No `dangerouslySetInnerHTML` or `<div>` in markdown-view/index.tsx
- `RichText` imported and used for rendering
- Syntax highlighting uses inline `style` attributes, not CSS classes
</verification>

<success_criteria>
Assistant message bubbles in the Feishu mini program (and H5 preview) display rendered markdown with syntax-highlighted code blocks instead of blank content.
</success_criteria>

<output>
After completion, create `.planning/quick/260414-ul9-fix-markdownview-for-feishu-mini-program/260414-ul9-SUMMARY.md`
</output>
